// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

register_chopper_boss_combat_actions( var_0 )
{
    if ( !isdefined( level.get_pavelow_boss_info ) )
        level.get_pavelow_boss_info = [];
}

get_player_aggro_score( var_0 )
{
    self endon( "death_or_disconnect" );
    self endon( "faux_spawn" );
    self endon( "end_launcher" );
    var_1 = multiplepubliceventsenabled( var_0 );
    var_2 = munition_remover( var_1 );
    thread get_pipe_room_spawnpoint( var_0, var_1, var_2 );
}

fadetogearingup()
{
    self waittill( "end_launcher" );
    wait 6;
    self notify( "cleanupImpactWatcher" );
}

get_pipe_room_spawnpoint( var_0, var_1, var_2 )
{
    self notify( "cleanupImpactWatcher" );
    self endon( "disconnect" );
    self endon( "cleanupImpactWatcher" );
    childthread fadetogearingup();

    for (;;)
    {
        self waittill( "bullet_first_impact", var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10 );

        if ( var_0 != var_7 )
            continue;

        var_11 = _id_127B6( var_9, var_8, var_1, var_7 );
        play_vo_internal( var_11, var_3, var_4, var_10, var_5, var_6, var_7, var_8, var_9, var_2 );
    }
}

play_vo_internal( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    if ( _id_1250F( var_5, var_0, var_1, var_2 ) )
        _id_11DA0( var_0, var_4, var_7, var_8 );
    else if ( _id_124E6( var_1 ) )
    {
        var_0 delete();
        return;
    }
    else if ( _id_124FC( var_1 ) )
        linktoent( var_0, var_1, var_2 );

    if ( isdefined( var_9 ) )
        [[ var_9 ]]( var_0, var_1, var_2, var_3, var_5 );
}

printdata( var_0, var_1, var_2, var_3, var_4 )
{

}

printer( var_0, var_1, var_2, var_3, var_4 )
{
    var_0 setscriptablepartstate( "effects", "impact" );

    if ( isdefined( var_1 ) && ( isplayer( var_1 ) || isagent( var_1 ) ) )
        var_5 = 1.1;
    else
        var_5 = 2;

    var_0.grenade = magicgrenademanual( "semtex_bolt_mp", var_0.origin, ( 0, 0, 0 ), var_5 );
    var_0.grenade.angles = var_0.angles;
    var_0.grenade linkto( var_0, "tag_origin" );
    thread iswztrain( var_0, var_5 );
}

printspawnmessage( var_0, var_1, var_2, var_3, var_4 )
{
    var_0 setscriptablepartstate( "effects", "impact" );
    thread _id_12C40( var_0, var_1, var_2, var_3 );
    thread _id_12C3F( var_0, var_1 );
    thread _id_12C3E( var_0 );
}

process_anim_ents( var_0, var_1, var_2, var_3, var_4 )
{
    if ( _id_12A8F( var_1, var_4 ) )
    {
        var_0 setscriptablepartstate( "impact", "active" );

        if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "weapons", "gas_createTrigger" ) )
            thread [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "weapons", "gas_createTrigger" ) ]]( var_0.origin, self, 5, 0.45 );

        thread _id_12A8E( var_0 );
    }
    else
        var_0 setscriptablepartstate( "impact", "dud" );
}

iswztrain( var_0, var_1 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_0.grenade scripts\engine\utility::_id_13409( var_1, "explode" );
    var_0 setscriptablepartstate( "effects", "explode" );
    var_2 = getcompleteweaponname( "semtex_bolt_mp" );
    var_3 = getcompleteweaponname( "semtex_bolt_splash_mp" );
    var_2.vandalize_attack_max_cooldown = var_0.weapon;
    var_3.vandalize_attack_max_cooldown = var_0.weapon;

    if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
    {
        var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::little_bird_mg_handleflarefire();
        var_0.stuckenemyentity dodamage( 110, var_0.origin, self, undefined, "MOD_EXPLOSIVE", var_2 );
        var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::little_bird_mg_handleflarerecharge();
    }

    radiusdamage( var_0.origin, 160, 110, 35, self, "MOD_EXPLOSIVE", var_3 );
    wait 0.4;

    if ( !var_0 _id_1314E() )
        return;

    var_0 delete();
}

_id_12C40( var_0, var_1, var_2, var_3 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_4 = getcompleteweaponname( "thermite_bolt_mp" );
    var_4.vandalize_attack_max_cooldown = var_0.weapon;

    if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
    {
        if ( var_1 scripts\cp_mp\vehicles\vehicle::isvehicle() || isdefined( var_1.classname ) && var_1.classname == "misc_turret" )
            var_5 = 1.0;
        else
            var_5 = 0.25;

        var_6 = int( 4.5 / var_5 );

        while ( isdefined( var_1 ) && isdefined( var_0 ) && isalive( var_1 ) && var_6 > 0 )
        {
            var_1 scripts\cp_mp\utility\damage_utility::little_bird_mg_handleflarefire();
            var_1 dodamage( 5, var_0.origin, self, var_0, "MOD_FIRE", var_4, var_3 );
            var_1 scripts\cp_mp\utility\damage_utility::little_bird_mg_handleflarerecharge();
            var_6--;
            wait( var_5 );
        }
    }
}

_id_12C3F( var_0, var_1 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_2 = int( 18.0 );
    var_3 = getcompleteweaponname( "thermite_bolt_radius_mp" );
    var_3.vandalize_attack_max_cooldown = var_0.weapon;
    var_0._id_12C43 = var_3.basename;

    while ( var_2 > 0 )
    {
        if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
            var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::adddamagemodifier( "thermiteBoltStuck", 0, 0, ::_id_12C37 );

        var_0 radiusdamage( var_0.origin, 50, 5, 3, self, "MOD_FIRE", var_3 );

        if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
            var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::removedamagemodifier( "thermiteBoltStuck", 0 );

        var_2--;
        wait 0.25;
    }
}

_id_12C37( var_0, var_1, var_2, var_3, var_4, var_5, var_6 )
{
    if ( !isdefined( var_0 ) )
        return 1;

    if ( !isdefined( var_0._id_12C43 ) || var_0._id_12C43 != "thermite_bolt_radius_mp" )
        return 1;

    if ( !isdefined( var_0.stuckenemyentity ) || var_0.stuckenemyentity != var_2 )
        return 1;

    return 0;
}

_id_12C3E( var_0 )
{
    var_0 endon( "entitydeleted" );
    wait 4.5;

    if ( !var_0 _id_1314E() )
        return;

    var_0 setscriptablepartstate( "effects", "burnEnd" );
    wait( randomfloatrange( 0.3, 2.0 ) );

    if ( !var_0 _id_1314E() )
        return;

    var_0 carriable_damage_wait();
    var_0 setmodel( "weapon_wm_sn_crossbow_bolt_fire_static_dst" );
}

_id_12A8E( var_0 )
{
    var_0 endon( "entitydeleted" );
    wait 0.5;

    if ( !var_0 _id_1314E() )
        return;

    var_0 delete();
}

_id_12A8F( var_0, var_1 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( !isplayer( var_0 ) && !isagent( var_0 ) )
        return 0;

    if ( isdefined( var_0.team ) && self.team == var_0.team )
        return 0;

    if ( var_1 == "riotshield" )
        return 0;

    return 1;
}

_id_127B6( var_0, var_1, var_2, var_3 )
{
    var_4 = movers_ref( var_2 );
    var_5 = spawn( "script_model", var_0 );
    var_5 setmodel( var_4 );
    var_5.angles = vectortoangles( var_1 );
    var_5 _id_1236F( var_2 );
    var_5.owner = self;
    var_5.bonustimeapplied = var_2;
    var_5.weapon = var_3;
    var_5.riotshieldiscurrentprimary = 1;

    if ( _id_12501( var_2 ) )
        var_5 = spawnintermissionatplayer( var_5 );

    var_5 thread carpunchcount();
    spawnprotectionexception( var_5 );
    return var_5;
}

_id_12501( var_0 )
{
    if ( var_0 == "bolt_default" || var_0 == "bolt_stun" )
        return 1;

    return 0;
}

spawnintermissionatplayer( var_0 )
{
    var_1 = var_0.origin + anglestoforward( var_0.angles ) * 15;
    var_2 = axistoangles( anglestoup( var_0.angles ), anglestoright( var_0.angles ), anglestoforward( var_0.angles ) );
    var_3 = spawn( "trigger_rotatable_radius", var_1, 0, 64, 79 );
    var_3.angles = var_2;
    var_3.targetname = "bolt_pickup";
    var_3 enablelinkto();
    var_3 linkto( var_0 );
    var_0.vo_enemy_heli = var_3;
    var_0 thread cargobaywatch();
    return var_0;
}

_id_11ED8( var_0 )
{
    var_0 notify( "removePickup" );

    if ( isdefined( var_0.vo_enemy_heli ) )
        var_0.vo_enemy_heli delete();
}

cargobaywatch()
{
    self endon( "entitydeleted" );
    self endon( "removePickup" );
    wait 2;

    for (;;)
    {
        self.vo_enemy_heli waittill( "trigger", var_0 );

        if ( !isplayer( var_0 ) )
            continue;

        if ( !var_0 scripts\cp_mp\utility\player_utility::_isalive() )
            continue;

        if ( isdefined( self.stuckenemyentity ) && isalive( self.stuckenemyentity ) )
            continue;

        var_1 = monitorplayerimpactend( var_0 getweaponslistprimaries() );

        if ( !isdefined( var_1 ) )
            continue;

        if ( var_0 cargo_truck_mg_removegunnerdamagemod( var_1 ) )
            self delete();
    }
}

monitorplayerimpactend( var_0 )
{
    foreach ( var_2 in var_0 )
    {
        if ( var_2 hasattachment( "ammo_crossbow" ) && self.bonustimeapplied == "bolt_default" )
            return var_2;

        if ( var_2 hasattachment( "mag_sn_t9crossbow" ) && self.bonustimeapplied == "bolt_default" )
            return var_2;

        if ( var_2 hasattachment( "boltstun_crossbow" ) && self.bonustimeapplied == "bolt_stun" )
            return var_2;
    }

    return undefined;
}

cargo_truck_mg_removegunnerdamagemod( var_0 )
{
    var_1 = weaponmaxammo( var_0 );
    var_2 = self getweaponammostock( var_0 );

    if ( var_2 >= var_1 )
        return 0;

    var_3 = int( min( var_1, var_2 + 1 ) );
    self setweaponammostock( var_0, var_3 );

    if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "damage", "hudIconType" ) )
        self [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "damage", "hudIconType" ) ]]( "crossbowbolt" );

    return 1;
}

_id_1236F( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_fire":
            thread _id_12370( 5 );
            self.gunship_watchintrodisown = 0;
            break;
        case "bolt_explo":
            self.gunship_watchintrodisown = 0;
            break;
        case "bolt_stun":
            thread _id_12370( 0.5 );
            self.gunship_watchintrodisown = 0;
            break;
        default:
            self.gunship_watchintrodisown = 1;
    }
}

_id_12370( var_0 )
{
    self endon( "entitydeleted" );
    wait( var_0 );
    self.gunship_watchintrodisown = 1;
    spawnprotectionexception();
}

spawnprotectionexception( var_0 )
{
    if ( isdefined( var_0 ) )
        var_1 = [ var_0 ];
    else
        var_1 = [];

    foreach ( var_3 in level.get_pavelow_boss_info )
    {
        if ( !isdefined( var_3 ) )
            continue;

        if ( isdefined( var_3 ) )
        {
            if ( var_1.size >= 24 && var_3.gunship_watchintrodisown )
            {
                var_3 delete();
                continue;
            }

            var_1[var_1.size] = var_3;
        }
    }

    level.get_pavelow_boss_info = var_1;
}

multiplepubliceventsenabled( var_0 )
{
    var_1 = "";

    foreach ( var_3 in var_0.attachments )
    {
        if ( issubstr( var_3, "bolt" ) )
        {
            var_1 = var_3;
            break;
        }
    }

    switch ( var_1 )
    {
        case "boltcarbon_crossbow":
            return "bolt_carbon";
        case "boltexplo_crossbow":
            return "bolt_explo";
        case "boltfire_crossbow":
            return "bolt_fire";
        case "boltstun_crossbow":
            return "bolt_stun";
        default:
            return "bolt_default";
    }
}

munition_remover( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_carbon":
            return ::printdata;
        case "bolt_explo":
            return ::printer;
        case "bolt_fire":
            return ::printspawnmessage;
        case "bolt_stun":
            return ::process_anim_ents;
        default:
            return;
    }
}

movers_ref( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_carbon":
            return "weapon_wm_sn_crossbow_bolt_carbon_static";
        case "bolt_explo":
            return "weapon_wm_sn_crossbow_bolt_explosive_static";
        case "bolt_fire":
            return "weapon_wm_sn_crossbow_bolt_fire_static";
        case "bolt_stun":
            return "weapon_wm_sn_crossbow_bolt_stun_static";
        default:
            return "weapon_wm_sn_crossbow_bolt_static";
    }
}

_id_1250F( var_0, var_1, var_2, var_3 )
{
    if ( !isdefined( var_2 ) && isdefined( var_3 ) )
        return 1;

    if ( !isdefined( var_0 ) )
        return 0;

    switch ( var_0 )
    {
        case "glass_solid":
        case "glass_pane":
            return 1;
    }

    if ( var_1.bonustimeapplied == "bolt_explo" )
        return 0;

    if ( sat_sound_think( var_2 ) )
        return 1;

    if ( sat_signal_shift( var_2 ) )
        return 1;

    switch ( var_0 )
    {
        case "asphalt_wet":
        case "asphalt_dry":
        case "riotshield":
            return 1;
        case "metal_car":
        case "metal_tank":
        case "metal_helicopter":
        case "metal_thin":
        case "metal_thick":
        case "metal_grate":
            if ( var_1.bonustimeapplied == "bolt_fire" )
                return 0;
            else
                return 1;
        default:
            return 0;
    }
}

_id_11DA0( var_0, var_1, var_2, var_3 )
{
    var_4 = scripts\engine\math::vector_reflect( var_2, var_1 );
    var_5 = abs( vectordot( var_2, var_1 ) );
    var_6 = scripts\engine\math::factor_value( 2300, 1000, var_5 );
    var_4 = var_4 * var_6;
    var_0 physicslaunchserver( var_3, var_4 );
}

_id_124E6( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isagent( var_0 ) && var_0 resetsuper() && !isalive( var_0 ) && !isdefined( var_0 getcorpseentity() ) )
        return 1;

    return 0;
}

_id_124FC( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.classname ) && var_0.classname == "weapon_scavenger_bag_mp" )
        return 0;

    return 1;
}

resetsuper()
{
    return istrue( isdefined( self.unittype ) && self.unittype == "suicidebomber" );
}

linktoent( var_0, var_1, var_2 )
{
    if ( ( isplayer( var_1 ) || isagent( var_1 ) ) && !isalive( var_1 ) )
    {
        var_3 = var_1 getcorpseentity();

        if ( isdefined( var_3 ) )
            var_1 = var_3;
    }

    if ( isplayer( var_1 ) && var_0.bonustimeapplied != "bolt_stun" )
    {
        var_0 hidefromplayer( var_1 );

        if ( isdefined( var_0.vo_enemy_heli ) )
            var_0.vo_enemy_heli hidefromplayer( var_1 );
    }

    if ( isdefined( var_2 ) )
        var_0 linkto( var_1, var_2 );
    else
        var_0 linkto( var_1 );

    if ( dmzextractcost( var_1 ) )
    {
        var_0.stuckenemyentity = var_1;
        var_0 thread _id_11EE9( var_1 );
    }

    var_0 notsolid();
    var_0 thread carriable_detonate_neurotoxin( var_1 );
    var_0 thread carnage_enemydummycleanup( var_1, "vehicle_deleted" );
    var_0 thread carnage_enemydummycleanup( var_1, "detonated" );
    var_0 thread carnage_enemydummycleanup( var_1, "beginC130" );
}

_id_1314E()
{
    if ( isdefined( self ) && istrue( self.riotshieldiscurrentprimary ) )
        return 1;
    else
    {

    }
}

_id_11EE9( var_0 )
{
    self endon( "entitydeleted" );
    var_0 scripts\engine\utility::_id_133F0( "entitydeleted", "death", "disconnect" );

    if ( !_id_1314E() )
        return;

    self.stuckenemyentity = undefined;

    if ( isdefined( var_0 ) && isdefined( var_0.nocorpse ) )
        self delete();
}

carriable_detonate_neurotoxin( var_0 )
{
    self endon( "entitydeleted" );
    var_0 scripts\engine\utility::_id_133EF( "entitydeleted", "disconnect" );

    if ( !_id_1314E() )
        return;

    carriable_damage_wait();
}

carriable_damage_wait( var_0 )
{
    if ( !isdefined( var_0 ) )
        var_0 = ( 0, 0, 100 );

    if ( self islinked() )
        self unlink();

    if ( !isdefined( self.model ) || self.model == "tag_origin" )
        return;

    self solid();
    self physicslaunchserver( self.origin, var_0 );
}

carnage_enemydummycleanup( var_0, var_1 )
{
    self endon( "entitydeleted" );
    var_0 waittill( var_1 );

    if ( !_id_1314E() )
        return;

    self delete();
}

carpunchcount()
{
    self waittill( "entitydeleted" );

    if ( isdefined( self.vo_enemy_heli ) )
        self.vo_enemy_heli delete();

    if ( isdefined( self.grenade ) )
        self.grenade delete();
}

sat_sound_think( var_0 )
{
    if ( !isplayer( var_0 ) )
        return 0;

    if ( scripts\mp\utility\player::isenemy( var_0 ) )
        return 0;
    else
        return 1;
}

sat_signal_shift( var_0 )
{
    if ( !isagent( var_0 ) )
        return 0;

    if ( isdefined( var_0.agentteam ) && self.team == var_0.agentteam )
        return 1;

    return 0;
}

dmzextractcost( var_0 )
{
    var_1 = 0;

    if ( isplayer( var_0 ) || isagent( var_0 ) )
        var_1 = 1;

    if ( var_0 scripts\cp_mp\vehicles\vehicle::isvehicle() )
        var_1 = 1;

    if ( isdefined( var_0.classname ) )
    {
        if ( var_0.classname == "misc_turret" )
            var_1 = 1;

        if ( var_0.classname == "script_model" )
        {
            if ( isdefined( var_0.streakinfo ) && ( var_0.streakinfo.streakname == "uav" || var_0.streakinfo.streakname == "gunship" ) )
                var_1 = 1;
        }
    }

    if ( isdefined( var_0.equipmentref ) )
    {
        if ( var_0.equipmentref == "equip_tac_cover" )
            var_1 = 1;
    }

    return var_1;
}
