// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    if ( !getdvarint( "scr_br_alt_mode_inflation", 0 ) || scripts\mp\utility\game::onfieldupgradeendbuffer() == "truckwar" )
        return;

    level.chopperoccupied = spawnstruct();
    level.chopperoccupied.disabled = 0;
    level.chopperoccupied.cost = getdvarint( "scr_br_alt_mode_inflation_cost", 45 );
    level.chopperoccupied.init_jugg_maze = getdvarfloat( "scr_br_alt_mode_inflation_drop_percent", 1.0 );
    level.chopperoccupied.init_intro_armor = getdvarint( "scr_br_alt_mode_inflation_drop_max", -1 );
    level.chopperoccupied._id_11F3E = getdvarfloat( "scr_br_alt_mode_inflation_respawn_delay", 10.0 );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "playerDropPlunderOnDeath", ::playerdropplunderondeath );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "circleTimer", ::circletimer );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "postPlunder", ::_id_11B17 );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "markPlayerAsEliminatedOnKilled", ::ss_entergulag );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "playerGulagAutoWinWait", ::x1opsnpcs );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "triggerRespawnOverlay", ::_id_12E9D );
    scripts\mp\gametypes\br_gametypes.gsc::_id_11DDC( "assignSpectatorToSpectatePlayer", ::assignspectatortospectateplayer );

    if ( !getdvarint( "scr_br_alt_mode_inflation_gulag", 0 ) )
        scripts\mp\gametypes\br_gametypes.gsc::helihint_wait( "gulag" );

    scripts\mp\gametypes\br_gametypes.gsc::helihint_wait( "useTokenToReviveTeammate" );
    scripts\mp\gametypes\br_gametypes.gsc::helihint_wait( "gulagWinnerRestoreLoadoutUseGulag" );
    thread refundondeath();
}

refundondeath()
{
    while ( !isdefined( level.onplayerspawncallbacks ) )
        waitframe();

    scripts\mp\utility\spawn_event_aggregator::registeronplayerspawncallback( ::onplayerspawned );
}

onplayerspawned()
{
    _id_130AD();
}

waypoint_inactive_vfx()
{
    return istrue( level.br_prematchstarted ) && !istrue( self.clean_up_search ) && !level.chopperoccupied.disabled && self.plundercount >= level.chopperoccupied.cost;
}

playerdropplunderondeath( var_0, var_1 )
{
    var_2 = self.plundercount;

    if ( istrue( self.respawningfromtoken ) )
    {
        var_2 = var_2 - level.chopperoccupied.cost;

        if ( var_2 < 0 )
            var_2 = 0;
    }

    var_3 = int( var_2 * level.chopperoccupied.init_jugg_maze );
    var_2 = var_2 - var_3;
    scripts\mp\gametypes\br_plunder.gsc::playersetplundercount( var_2, on_killed_tutorial() );

    if ( level.chopperoccupied.init_intro_armor >= 0 )
        var_3 = int( min( level.chopperoccupied.init_intro_armor, var_3 ) );

    scripts\mp\gametypes\br_plunder.gsc::init_reach_pipe_room( var_3, var_0 );
    return 1;
}

circletimer( var_0 )
{
    if ( !var_0 )
        scripts\mp\gametypes\br_gulag.gsc::_id_12450();

    var_1 = scripts\mp\gametypes\br_gulag.gsc::nextstar();

    if ( !level.chopperoccupied.disabled && var_0 >= var_1 )
    {
        level.chopperoccupied.disabled = 1;

        foreach ( var_3 in level.players )
        {
            if ( !isdefined( var_3 ) || !isalive( var_3 ) )
                continue;

            var_3 _id_130AD();
            scripts\mp\gametypes\br_killstreaks.gsc::getfilterformodifier( var_3, "cash_deploy_closed", undefined, 2 );
        }
    }

    return 0;
}

_id_11B17( var_0 )
{
    var_1 = 0;

    if ( isdefined( var_0 ) )
        var_1 = var_0._id_125BB;

    _id_130AD( var_1 );
}

_id_130AD( var_0 )
{
    if ( waypoint_inactive_vfx() )
    {
        if ( !scripts\mp\gametypes\br_public.gsc::hasrespawntoken() )
        {
            scripts\mp\gametypes\br_pickups.gsc::addrespawntoken( 1 );

            if ( !istrue( var_0 ) )
                thread scripts\mp\hud_message::showsplash( "br_inflation_respawn_token_pickup" );
        }
    }
    else if ( scripts\mp\gametypes\br_public.gsc::hasrespawntoken() )
    {
        scripts\mp\gametypes\br_pickups.gsc::removerespawntoken();

        if ( !istrue( var_0 ) )
            thread scripts\mp\hud_message::showsplash( "br_inflation_respawn_token_lost" );
    }
}

_id_12554( var_0 )
{
    waittillframeend;
    scripts\mp\utility\lower_message::setlowermessageomnvar( 9, int( gettime() + var_0 * 1000 ) );
    scripts\mp\gametypes\br_gulag.gsc::_id_123BF( 1 );
    thread playertracking( var_0 );
}

playertracking( var_0 )
{
    self endon( "disconnect" );

    if ( isdefined( var_0 ) )
        wait( var_0 );

    scripts\mp\gametypes\br_gulag.gsc::_id_123BF( 0 );
    scripts\mp\utility\lower_message::setlowermessageomnvar( 0 );
}

on_killed_tutorial()
{
    var_0 = spawnstruct();
    var_0._id_125BB = 1;
    return var_0;
}

ss_entergulag()
{
    return 0;
}

x1opsnpcs( var_0, var_1 )
{
    if ( !isdefined( var_0 ) )
    {
        if ( level.chopperoccupied._id_11F3E )
        {
            var_2 = level.chopperoccupied._id_11F3E;
            thread _id_12554( var_2 );
            wait( var_2 );
            return 1;
        }
    }

    return 0;
}

_id_12E9D( var_0 )
{
    return 1;
}

assignspectatortospectateplayer( var_0, var_1 )
{
    var_0 notify( "assignSpectatorToSpectatePlayerWaitForTeam" );

    if ( istrue( level.current_volume_enemy ) )
        return 0;

    if ( !isdefined( var_1 ) || !isplayer( var_1 ) || !isalive( var_1 ) && !isdefined( var_1._id_12A76 ) )
        return 0;

    if ( var_0.team == var_1.team )
        return 0;

    if ( !scripts\mp\utility\teams::getteamdata( var_0.team, "aliveCount" ) )
        return 0;

    thread branalytics_seteventdelayedstate( var_0 );
    return 1;
}

branalytics_seteventdelayedstate( var_0 )
{
    level endon( "brSpawnPlayersEnding" );
    var_0 endon( "assignSpectatorToSpectatePlayerWaitForTeam" );
    var_0 endon( "death_or_disconnect" );
    var_0 scripts\mp\gametypes\br_spectate.gsc::_id_119D9();
    var_0 setclientomnvar( "ui_show_spectateHud", var_0 getentitynumber() );
    wait 1.0;
    var_1 = scripts\mp\gametypes\br_spectate.gsc::movequestobjicon( var_0 );
    thread scripts\mp\gametypes\br_spectate.gsc::assignspectatortospectateplayer( var_0, var_1 );
}
