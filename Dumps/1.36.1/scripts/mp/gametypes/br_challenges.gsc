// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    if ( scripts\mp\utility\game::getgametype() != "br" )
        return;

    usingtacmap( "br_mastery_fiveContracts", ::hudcoststring );
    usingtacmap( "br_mastery_pointBlank_airstrike", ::trackriotshield_watchcancelswitchaway );
    usingtacmap( "br_mastery_pointBlank_tomahawk", ::trackriotshield_watchswitchawayfromshield );
    usingtacmap( "br_mastery_c4VehicleMultKill", ::compass );
    usingtacmap( "br_mastery_ghostRideWhip", ::level_spawnplayer );
    usingtacmap( "br_mastery_roundKillExecute", ::waittill_wave_spawned_or_timeout );
    usingtacmap( "br_mastery_travelogue", ::_id_1236E );
}

usingtacmap( var_0, var_1 )
{
    if ( !isdefined( level.canplaygasmaskgesturebr ) )
        level.canplaygasmaskgesturebr = [];

    level.canplaygasmaskgesturebr[var_0] = var_1;
}

cypher_signal_strength_nag( var_0, var_1 )
{
    if ( !isdefined( level.canplaygasmaskgesturebr ) )
        return;

    if ( istrue( level.cypher_vo_hack_progress ) )
        return;

    var_2 = level.canplaygasmaskgesturebr[var_0];

    if ( isdefined( var_2 ) )
        self thread [[ var_2 ]]( var_0, var_1 );
}

botdisablekillstreaks( var_0 )
{
    if ( getdvarint( "scr_br_challenge_debug", 0 ) )
        iprintlnbold( "Mastery Challenge: " + var_0 + " completed!" );

    scripts\cp\agents\gametype_cp_specops::setuptrain( var_0 );
}

remove_last_used_node( var_0 )
{
    if ( !isdefined( level.teamdata[var_0] ) )
        return;

    foreach ( var_2 in level.teamdata[var_0]["players"] )
        _id_1236D( var_2 );

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) )
    {
        _id_12579( var_0 );
        serverroomtvs( var_0 );
    }
}

_id_12579( var_0 )
{
    if ( !isdefined( var_0 ) )
        return;

    var_1 = 0;
    var_2 = 0;
    var_3 = max( 1, level.maxteamsize );

    foreach ( var_5 in level.teamdata[var_0]["players"] )
    {
        var_1 = var_1 + var_5.deaths;

        if ( isdefined( var_5.resttimems ) )
            var_1 = var_1 + var_5.resttimems;

        if ( scripts\mp\utility\player::isreallyalive( var_5 ) )
            var_2++;
    }

    if ( var_1 == 0 )
    {
        foreach ( var_5 in level.teamdata[var_0]["players"] )
            var_5 botdisablekillstreaks( "br_mastery_untouchable" );
    }
}

serverroomrewardspawn()
{
    if ( !isdefined( self ) || !isdefined( self.team ) )
        return;

    var_0 = self.deaths;
    var_1 = self.team;

    foreach ( var_3 in level.teamdata[var_1]["players"] )
    {
        if ( scripts\mp\utility\player::isreallyalive( var_3 ) )
        {
            if ( !isdefined( var_3.resttimems ) )
                var_3.resttimems = 0;

            var_3.resttimems = var_3.resttimems + var_0;
            break;
        }
    }
}

serverroomtvs( var_0 )
{
    var_1 = 7;

    if ( !isdefined( var_0 ) )
        return;

    var_2 = 0;

    foreach ( var_4 in level.teamdata[var_0]["players"] )
    {
        var_2 = var_2 + var_4.deaths;

        if ( isdefined( var_4.resttimems ) )
            var_2 = var_2 + var_4.resttimems;
    }

    if ( var_2 >= var_1 )
    {
        foreach ( var_4 in level.teamdata[var_0]["players"] )
            var_4 botdisablekillstreaks( "br_mastery_neverSayDie" );
    }
}

hudcoststring( var_0, var_1 )
{
    if ( !isdefined( self.team ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::get_int_or_0( self.chopper_carepackage_pilot_selected ) < 5 )
        return;

    var_2 = isdefined( level.gulag ) && !istrue( level.gulag.shutdown );

    foreach ( var_4 in level.teamdata[self.team]["players"] )
    {
        if ( !scripts\mp\utility\player::isreallyalive( var_4 ) )
            return;

        if ( var_2 && var_4 scripts\mp\gametypes\br_public.gsc::player_gun_game_next_weapon_think() )
            return;
    }

    botdisablekillstreaks( var_0 );
}

trackriotshield_watchcancelswitchaway( var_0, var_1 )
{
    var_2 = var_1.spawnselectionmarker;
    var_3 = var_1._id_12183;

    if ( !isdefined( var_2 ) || !isdefined( var_2.assaultkill ) || !isdefined( var_2.streakinfo ) || !isdefined( var_3 ) )
        return;

    var_4 = var_2.streakinfo;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_airstrike_killtracker" );
    self endon( "pointBlank_airstrike_killtracker" );
    scripts\engine\utility::_id_12913( 10.0, "airstrike_finished_" + var_2.assaultkill );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_4.kills >= 3 )
        botdisablekillstreaks( "br_mastery_pointBlankStreakKill" );
}

trackriotshield_watchswitchawayfromshield( var_0, var_1 )
{
    var_2 = var_1.streakinfo;
    var_3 = var_1._id_12183;

    if ( !isdefined( var_2 ) )
        return;

    if ( !isdefined( var_3 ) )
        return;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_tomahawk_killtracker" );
    self endon( "pointBlank_tomahawk_killtracker" );
    scripts\engine\utility::_id_12914( 20, "cluster_strike_finished" );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_2.kills >= 3 )
        botdisablekillstreaks( "br_mastery_pointBlankStreakKill" );

    return;
}

compass( var_0, var_1 )
{
    var_2 = var_1.meansofdeath;
    var_3 = var_1.inflictor;

    if ( !isdefined( var_2 ) || var_2 != "MOD_EXPLOSIVE" )
        return;

    if ( !playergetbestdropbagorigin( var_3 ) )
        return;

    self endon( "disconnect" );
    level endon( "game_ended" );
    self notify( "updateC4VehicleMultKill" );
    self endon( "updateC4VehicleMultKill" );

    if ( !isdefined( self.updatelocationbesttime ) )
        self.updatelocationbesttime = 0;

    self.updatelocationbesttime++;
    wait 4.0;

    if ( isdefined( self.updatelocationbesttime ) && self.updatelocationbesttime >= 3 )
    {
        botdisablekillstreaks( var_0 );
        var_4 = compareteamscores( var_3 );
        var_5 = isdefined( var_4 ) && isdefined( var_4.team ) && isdefined( self.team ) && var_4.team == self.team;

        if ( var_5 )
            var_4 botdisablekillstreaks( var_0 );
    }

    self.updatelocationbesttime = undefined;
}

playergetbestdropbagorigin( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.vehiclename ) && !var_0 scripts\common\vehicle_code::vehicle_is_stopped() )
    {
        var_1 = var_0 getlinkedchildren();

        foreach ( var_3 in var_1 )
        {
            if ( isdefined( var_3.weapon_name ) && var_3.weapon_name == "c4_mp_p" )
                return 1;
        }
    }
    else if ( isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_5 = var_0 getlinkedparent();

        if ( isdefined( var_5 ) && isdefined( var_5.vehiclename ) && !var_5 scripts\common\vehicle_code::vehicle_is_stopped() )
            return 1;
    }

    return 0;
}

compareteamscores( var_0 )
{
    if ( isdefined( var_0 ) && isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_1 = var_0 getlinkedparent();

        if ( isdefined( var_1 ) )
            return var_1.owner;
    }

    return undefined;
}

waittill_wave_spawned_or_timeout( var_0, var_1 )
{
    var_2 = var_1.player;

    if ( !isdefined( var_2 ) )
        return;

    if ( isdefined( var_2.modifiers["execution"] ) && var_2.modifiers["execution"] == 1 )
        var_2 botdisablekillstreaks( "br_mastery_roundKillExecute" );
}

level_spawnplayer( var_0, var_1 )
{
    self endon( "disconnect" );
    level endon( "game_ended" );

    if ( !isdefined( self ) )
        return;

    var_2 = var_1.headlessinfils;
    var_3 = var_1.regroup_all_alive_players_to_plane;

    if ( istrue( var_2 ) )
    {
        if ( istrue( self.map_dev_name_to_actual_station_name ) && self.updatelocationbesttimehud != 0 )
            return;

        self.map_dev_name_to_actual_station_name = 1;
    }

    if ( !istrue( self.map_dev_name_to_actual_station_name ) )
        return;

    self notify( "ghostRideWhip" );
    self endon( "ghostRideWhip" );

    if ( !isdefined( self.updatelocationbesttimehud ) )
        self.updatelocationbesttimehud = 0;

    if ( istrue( var_3 ) )
        self.updatelocationbesttimehud++;

    wait 4.0;

    if ( !isdefined( self ) )
        return;

    if ( isdefined( self.updatelocationbesttimehud ) && self.updatelocationbesttimehud >= 3 )
        botdisablekillstreaks( var_0 );

    self.updatelocationbesttimehud = undefined;
    self.map_dev_name_to_actual_station_name = undefined;
}

_id_1236E( var_0, var_1 )
{
    var_2 = -1;

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) == 0 )
        return;

    var_3 = var_1.inflictor;

    if ( !isdefined( var_3 ) )
        return;

    if ( !isplayer( var_3 ) && isdefined( var_3.owner ) && isplayer( var_3.owner ) )
        var_3 = var_3.owner;
    else
        return;

    var_4 = var_1.victim;

    if ( !isdefined( var_4 ) || !isplayer( var_4 ) )
        return;

    var_5 = 0;
    var_6 = var_3 scripts\mp\gametypes\br_callouts.gsc::kiosksearchradiusmax( var_3 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    var_6 = var_4 scripts\mp\gametypes\br_callouts.gsc::kiosksearchradiusmax( var_4 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    if ( var_5 == 0 )
        return;

    foreach ( var_8 in level.teamdata[var_3.team]["players"] )
    {
        if ( !isdefined( var_8._id_12BF5 ) )
            var_8._id_12BF5 = 0;

        var_8._id_12BF5 = var_8._id_12BF5 | var_5;
    }
}

_id_1236D( var_0 )
{
    var_1 = 12;

    if ( !isdefined( var_0._id_12BF5 ) || var_0._id_12BF5 == 0 )
        return;

    var_2 = 0;

    for ( var_3 = 0; var_3 < level.calloutglobals.seq3_reset_switch.size; var_3++ )
    {
        var_4 = var_0._id_12BF5 & 1 << var_3;

        if ( var_4 )
            var_2++;
    }

    if ( var_2 >= var_1 )
        var_0 botdisablekillstreaks( "br_mastery_travelogue" );

    return;
}
