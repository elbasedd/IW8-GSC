// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "circle" );
    scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "gulag" );
    scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "dropbag" );

    if ( getdvarint( "scr_bmo_use_spawn_fix", 1 ) == 0 )
        scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "teamSpectate" );

    scripts\mp\gametypes\br_gametypes.gsc::give_weapon_alt_clip_ammo_hack( "allowLateJoiners" );
    scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "oneLife" );

    if ( getdvarint( "scr_bmo_useKiosks", 1 ) == 0 )
        scripts\mp\gametypes\br_gametypes.gsc::get_end_ang( "kiosk" );

    if ( getdvarint( "scr_bmo_enableTabletReplace", 1 ) == 1 )
        scripts\mp\gametypes\br_gametypes.gsc::give_weapon_alt_clip_ammo_hack( "tabletReplace" );

    scripts\mp\gametypes\br_gametypes.gsc::using_self_revive( "playerDropPlunderOnDeath", ::playerdropplunderondeath );
    scripts\mp\gametypes\br_gametypes.gsc::using_self_revive( "playerShouldRespawn", ::targeted_entities );
    scripts\mp\gametypes\br_gametypes.gsc::using_self_revive( "playerWelcomeSplashes", ::temp_debug_wait_and_stop_music_loop );
    scripts\mp\gametypes\br_gametypes.gsc::using_self_revive( "spawnHandled", ::_id_11E1D );
    level._id_121AA = getdvarint( "scr_dmz_teamPlunder", 0 );

    if ( level._id_121AA )
    {

    }

    level.checkbluemassacre.removeplatepouch = getdvarint( "scr_br_extract_max_extractions", 5 );
    level.checkbluemassacre.setgun = 0;
    level.checkbluemassacre.hudattractionobj = "tie";
    level.br_prematchffa = 0;
    setdvar( "scr_br_allowLoadout", 1 );
    level._id_12290 = [];
    level.registercarryobjectpickupcheck = [];
    level.healthpack_health = [];
    level.convert_remaining_to_ai = 0;
    level.convoy4_actively_hacking = 0;
    level.convoy4_failed_extract = 0;
    var_0 = getdvarint( "scr_dmz_win_cost", 1000 );

    if ( var_0 > 0 )
    {
        var_0 = var_0 * 10;
        var_0 = var_0 * 100;
        setdvar( "scr_br_scorelimit", var_0 );
        scripts\mp\utility\game::registerscorelimitdvar( scripts\mp\utility\game::getgametype(), var_0 );
    }

    setdvar( "scr_br_magcount", 3 );
    setdvar( "scr_br_loadout_option", "nothing" );
    setdvarifuninitialized( "scr_br_respawnMaxTeammateOffset", 10000 );
    setomnvar( "ui_br_circle_state", 4 );
    setomnvar( "ui_gulag_state", 1 );
    setomnvar( "ui_hide_redeploy_timer", 1 );
    setdvarifuninitialized( "scr_br_bank_alarm", 1 );
    level.linktooriginandangles = getdvarint( "scr_dmz_giveLoadoutEveryTime", 1 );
    level.parachutecancutparachute = getdvarint( "scr_bmo_instantBleedOutSquadWipe", 1 );
    level.can_be_interrupted = getdvarint( "scr_bmo_bottomPercentageToAdjustEconomy", 50 );
    level._id_1228E = getdvarint( "scr_bmo_topPercentageToAdjustEconomy", 5 );
    level._id_12657 = getdvarint( "scr_bmo_useMilestonePhases", 1 );
    level.retry_no_votes = getdvarint( "scr_bmo_milestonePhase_VIPs", 10 );
    level.retrieve_data_objective = getdvarint( "scr_bmo_milestonePhase_LZs", 30 );
    level.retreat_vehicle_collision_clear = getdvarint( "scr_bmo_milestonePhase_Drops", 50 );
    level.retreatanddie = getdvarint( "scr_bmo_milestonePhase_Helis", 75 );
    level.array_convert_keys_to_ints = getdvarint( "scr_dmz_airdrop_base_cash_amount", 750 );
    level.modespawnendofgame = getdvarint( "scr_bmo_hidePlacementUntilPercent", 10 );
    level.moderestrictsarenakillstreaks = getdvarint( "scr_bmo_hideLeaderHashUntilPercent", 0 );
    level._id_11C16 = getdvarint( "scr_bmo_progressSplashesAndMusic", 1 );
    level.xanimlength = getdvarint( "scr_bmo_secondsBeforePlacementUpdates", 60 );
    level.get_extra_focus_fire_multipler = getdvarint( "scr_bmo_disable_perc_announcements", 0 );
    level.register_vehicle_as_ambient = getdvarint( "scr_dmz_loot_leader_mark_count", 3 );
    level.removeplayeraslootleader = getdvarint( "scr_dmz_loot_leader_mark_count", 3 );
    level.register_vehicle_spawners = getdvarint( "scr_bmo_loot_leader_mark_size", 5000 );
    level.registerbrsquadleaderjumpcommands = getdvarint( "scr_dmz_loot_leader_one_per_team", 1 ) == 1;
    level.register_vfx = getdvarint( "scr_bmo_loot_leader_mark_size_dynamic", 1 );
    level.registeraccesscardlocs = getdvarint( "scr_bmo_loot_leader_mark_info_strong_size", 2000 );
    level.registerarmsracevfx = getdvarint( "scr_bmo_loot_leader_mark_info_strong_value", 5000 );
    level.registerbrgametypedata = getdvarint( "scr_bmo_loot_leader_mark_info_weak_size", 5000 );
    level.registerbrgametypefunc = getdvarint( "scr_bmo_loot_leader_mark_info_weak_value", 1000 );
    level.health_bar_player_connect_monitor = getdvarint( "scr_bmo_loot_leader_expired_enabled", 0 ) == 1;
    level.register_wave_spawner = getdvarint( "scr_bmo_loot_leader_mark_top_teams", 2 );
    level.track_get_teleport_velocity = getdvarfloat( "scr_bmo_music_first", 0.3 );
    level.track_last_good_position = getdvarfloat( "scr_bmo_music_second", 0.5 );
    level.track_settings = getdvarfloat( "scr_bmo_music_third", 0.75 );
    level.track_is_operational = getdvarfloat( "scr_bmo_music_fourth", 0.9 );
    level._id_12638 = getdvarint( "scr_dmz_useAutoRespawn", 1 );
    level.bot_pickup_origin = getdvarint( "scr_dmz_autoRespawnWaitTime", 20 );
    level._id_12281 = getdvarint( "scr_dmz_tokenRespawnWaitTime", level.bot_pickup_origin );
    level.never_unloaded_from_vehicle = getdvarint( "scr_dmz_respawn_penalty", 0 );
    level.neverspectate = getdvarfloat( "scr_dmz_respawn_penalty_max", 15 );
    level.visibilityisscriptcontrolled = getdvarint( "scr_bmo_respawnHeightOverride", 5000 );
    level.vo_exfil_helo_arrived_nag = getdvarint( "scr_dmz_respawn_time_disable", 0 );
    level.soldier_agent_lwfn9 = getdvarfloat( "scr_bmo_parachuteDeployDelay", 0.5 );
    level.bush_trig_debug = getdvarint( "scr_dmz_bonusDeathPlunder", 200 );
    level.bush_zones = getdvarint( "scr_dmz_bonusDeathPlunder_ot", 250 );
    level.removestuckenemyondeathordisconnect = getdvarint( "scr_bmo_maxPlunderDropOnDeath", 10000 );
    level.rocket_fuel_x2 = getdvarint( "scr_bmo_minPlunderDropOnDeath", 200 );
    level.spawn_module_p3_form_c = getdvarint( "scr_bmo_percentagePlunderDrop", 50 );
    level.track_get_reward_time = getdvarint( "scr_bmo_plunderFXOnDropThreashold", 750 );
    level.removestructfromlevelarray = getdvarint( "scr_bmo_maxPlunderDropInOvertime", 2000 );
    level.has_headicon = getdvarfloat( "scr_bmo_executionCashMultiplier", 1 );
    level.bot_get_distance_to_goal = getdvarint( "scr_dmz_autoAssignFirstQuest", 0 );
    level.turret_guncourse_explode_on_end = getdvarint( "scr_bmo_questDomDistMin", 5000 );
    level.turret_fob_watcher = getdvarint( "scr_bmo_questDomDistMax", 10000 );
    level.turret_fob_self_destruct = getdvarint( "scr_bmo_questAssDistMin", 2500 );
    level.turret_enemy_watcher_internal = getdvarint( "scr_bmo_questAssDistMax", 7500 );
    level.turret_has_target = getdvarint( "scr_bmo_questScavDistMin", 5000 );
    level.turret_guncourse_think = getdvarint( "scr_bmo_questScavDistMax", 8000 );
    level.turret_objective_think = getdvarint( "scr_bmo_questScavDistMin", 5000 );
    level.turret_headicon = getdvarint( "scr_bmo_questScavDistMax", 8000 );
    level._id_120F5 = getdvarfloat( "scr_bmo_questTabletReplaceEveryN", 1.5 );
    level._id_11C7D = getdvarint( "scr_bmo_skipWeaponDropOnDeath", 0 );
    level._id_11C6C = getdvarint( "scr_bmo_skipEquipmentDropOnDeath", 1 );
    level.init_ai_weapons = getdvarint( "scr_bmo_forceArmorDropOnDeath", 1 );
    level.audio_heli_fade_up = getdvarint( "scr_bmo_allowFultonDropOnDeath", 1 );
    level._id_121A6 = getdvarint( "scr_bmo_score_exfil", 0 ) == 1;
    level._id_121A7 = getdvarint( "scr_bmo_exfil_showvipteamonly", 0 ) == 1;
    level._id_121A9 = getdvarint( "scr_bmo_vipteam_uav", 0 ) == 1;
    level._id_121A8 = getdvarint( "scr_bmo_exfil_timer", 180 );
    level._id_11C18 = getdvarint( "scr_bmo_plunderextract_objicon_inworld", 1 ) == 1;
    level._id_11C13 = getdvarint( "scr_bmo_extract_objicon_nonscriptable", 0 );
    level._id_12243 = getdvarint( "scr_bmo_timeout_plunderextract", 0 );
    level.sec_sys_struct_1 = getdvarint( "scr_bmo_move_plunderextract_onuse", 0 ) == 1;
    level.helicrash = int( getdvarint( "scr_bmo_extract_heli_health", 999 ) * 100 );
    level.helicratedelete = getdvarint( "scr_bmo_extract_heli_invulnerable", 1 );
    level.br_delaynojip = getdvarint( "scr_bmo_extract_plunder_instant", 1 );
    level.track_get_launch_target = getdvarint( "scr_bmo_plunder_extract_alert", 1 );
    level.removeaccesscard = getdvarint( "scr_bmo_matchstart_extractsitedelay", 120 );
    level.mode_can_play_ending = getdvarint( "scr_bmo_plunderextract_hide_unused", 1 );
    level._id_11B34 = scripts\engine\utility::ter_op( getdvar( "mapname" ) == "mp_br_mechanics", 0, getdvarint( "scr_bmo_plunderextract_distribution", 1 ) );
    level.weapondrop_createdropondeath = getdvarint( "scr_bmo_score_requires_banking", 0 );
    level.get_grenadedanger_alias = getdvarint( "scr_bmo_disable_win_on_score", 0 );
    level.get_showing_bomb_wire_pair_to_player = getdvarint( "scr_bmo_eom_ot_timer", 30 );
    level._id_11A62 = level.get_showing_bomb_wire_pair_to_player > 0;
    level.smoke_screen = getdvarint( "scr_bmo_ot_as_match_time", 1 );
    level.br_danger_circle_closing_music = getdvarint( "scr_bmo_eom_bank_to_end", 0 );
    level.relic_steelballs_dash = getdvarint( "scr_dmz_mapEdgeExtractionLocs", 0 );
    level.rpggetclosetoapc = ::tarmac_techo_start_first;
    level.grenade_chances = getdvarint( "scr_bmo_endMatchCameraTransitions", 1 );
    level.smoke_wheelson_chosen_spawn = getdvarfloat( "scr_bmo_overtimeCashMultiplier", 2 );
    setomnvar( "ui_br_overtime_cash_multiplier", level.smoke_wheelson_chosen_spawn );
    level.get_explosion_alias = getdvarint( "scr_bmo_disable_one_mil_announce", 0 );
    level.get_player_defusing_zone = getdvarint( "scr_dmz_loot_leader_update_on_pickup", 0 ) == 1;
    level.get_player_who_most_likely_broke_stealth = getdvarint( "scr_dmz_win_cost", 1000 ) * 1000;
    level.get_player_closest_to_any_goal = getdvarint( "scr_br_extract_cost", 300 );
    level.get_player_drone = getdvarint( "scr_br_extract_cost_min", 40 );
    level.get_player_cumulative_damage = getdvarint( "scr_br_extract_cost_decrease", 20 );
    level.rocket_internal = getdvarint( "br_min_plunder_extractions", 7 );
    level.repair_grill_fixing_long_sfx = getdvarint( "br_max_plunder_extractions", 7 );
    level.check_carrier_status = 1;
    thread scripts\mp\gametypes\br_lootchopper.gsc::init();
    thread outofboundstriggersspawned();
    thread onriskplayerdisconnect();
}

outofboundstriggersspawned()
{
    waittillframeend;
    level.uavsettings["uav"].timeout = 60;
    scripts\mp\flags::gameflaginit( "collect_done", 0 );
    scripts\mp\flags::gameflaginit( "helipad_wait_done", 0 );
    scripts\mp\flags::gameflaginit( "placement_updates_allowed", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_lzs", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_drops", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_helis", 0 );
    level.router_placed = ::chopper_boss_destroyed_func;
    level.chopper_gunner_assignedtargetmarkers_onnewai = ::rooftop_objective;
    level.ononeleftevent = ::ononeleftevent;
    level.onplayerkilled = ::onplayerkilled;
    level.shouldmodelognotify = ::shouldmodelognotify;
    level.ontimelimit = ::ontimelimit;
    scripts\mp\utility\spawn_event_aggregator::registeronplayerspawncallback( ::table_getaddblueprintattachments );
    scripts\mp\utility\disconnect_event_aggregator::registerondisconnecteventcallback( ::onplayerdisconnect );
    thread _id_11B4B();
    thread scripts\mp\gametypes\br_lootchopper.gsc::register_jugg_maze_interactions();
    thread scripts\mp\gametypes\br_c130airdrop.gsc::colorise_toggle_onto();
    level.register_vehicle_spawn_override = [];
    level.health_remaining_max = [];

    for ( var_0 = 0; var_0 < level.register_vehicle_as_ambient; var_0++ )
    {
        if ( level.register_wave_spawner > 0 && var_0 == 0 )
        {
            var_1 = 1;
            var_2 = 5;
        }
        else
        {
            var_1 = 1;
            var_2 = 4;
        }

        var_3 = spawnstruct();
        var_3.modifier = "";
        var_3 scripts\mp\gametypes\br_quest_util.gsc::ending_fade_in( var_1, var_2 );
        var_3 scripts\mp\gametypes\br_quest_util.gsc::_id_11A9B( level.register_vehicle_spawners );
        level.register_vehicle_spawn_override[var_0] = var_3;
        var_3 = spawnstruct();
        var_3.modifier = "";
        var_3 scripts\mp\gametypes\br_quest_util.gsc::ending_fade_in( var_1, var_2, 2 );
        var_3 scripts\mp\gametypes\br_quest_util.gsc::_id_11A9B( level.register_vehicle_spawners );
        level.health_remaining_max[var_0] = var_3;
    }

    if ( !istrue( level._id_12657 ) )
        level thread spawnprojectile();

    thread _id_128C5();
    level thread scripts\mp\gametypes\br_analytics.gsc::onplayerdamaged_func();
    cleanupents();

    if ( level._id_121AA )
    {
        if ( getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 ) > 0.0 )
            level thread _id_125DB();
    }

    if ( level._id_11C18 && level._id_11C13 )
        level thread end_silo_elevator();

    if ( level._id_12243 > 0 )
    {
        scripts\mp\flags::gameflagwait( "prematch_done" );

        if ( level.removeaccesscard > 0 )
            wait( level.removeaccesscard );

        level thread _id_11FCC();
    }

    if ( istrue( level.relic_steelballs_dash ) )
        level thread onteamleadgained();

    level._id_1268E = [];
    level._id_1268E[0] = "assassination";
    level._id_1268E[1] = "domination";
    level._id_1268E[2] = "scavenger";
    level thread handledevcommand_publicevents();

    if ( istrue( level.grenade_chances ) )
        level thread onstompeenemyprogressupdate();

    var_4 = getdvarint( "scr_bmo_c130OverrideSpeed", -1 );

    if ( var_4 > 0 )
        level.br_level.c130_speedoverride = var_4;

    level thread damage_players_when_enter_trigger();

    if ( level.get_showing_bomb_wire_pair_to_player && level.smoke_screen )
        level thread damageclonewatch();
}

damage_players_when_enter_trigger()
{
    level endon( "game_ended" );

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() )
        return;

    level endon( "end_circlestate_timer" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = getdvarint( "scr_br_timelimit" );
    setomnvar( "ui_br_circle_state", 5 );
    setomnvar( "ui_hardpoint_timer", gettime() + int( var_0 * 1000 ) );
    wait( int( max( var_0 - 300, 0 ) ) );
    setomnvar( "ui_br_circle_state", 6 );
}

damageclonewatch()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = getdvarint( "scr_br_timelimit" );
    var_1 = int( max( var_0 - level.get_showing_bomb_wire_pair_to_player, 0 ) );

    if ( var_1 <= 0 )
        return;

    wait( var_1 );
    level notify( "end_circlestate_timer" );
    level thread _id_1241E( undefined, undefined, 1 );
}

_id_128C4()
{
    level endon( "game_ended" );
    level.airlock_show_front_doors = [];
    level.airlock_show_front_doors["uktl"] = "dx_bra_uktl_respawning_enemy_in_area";
    level.airlock_show_front_doors["rutl"] = "dx_bra_rutl_respawning_enemy_in_area";
    level.solution_exists_already = getdvarint( "scr_parachute_overhead_warning_timeout_ms", 45000 );
    level.sololink = getdvarint( "scr_parachute_overhead_warning_prematch_timeout_ms", 20000 );
    level.solospawn = getdvarint( "scr_parachute_overhead_warning_radius", 2000 );
    level.soldier_encounter_test = getdvarint( "scr_parachute_overhead_warning_height", 3000 );
    level thread _id_12A10( level.sololink );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    level notify( "cancel_watch_parachuters_overhead" );
    waitframe();
    level thread _id_12A10( level.solution_exists_already );
}

_id_128C5()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    thread trackcarpunches();
}

cleanupents()
{
    scripts\cp_mp\utility\game_utility::vehicle_occupancy_mp_updateriotshield( "delete_on_load", "targetname" );
    scripts\cp_mp\utility\game_utility::vehicle_occupancy_setfriendlystatusdirty( "door_prison_cell_metal_mp", 1 );
    scripts\cp_mp\utility\game_utility::vehicle_occupancy_setfriendlystatusdirty( "door_wooden_panel_mp_01", 1 );
    scripts\cp_mp\utility\game_utility::vehicle_occupancy_setfriendlystatusdirty( "me_electrical_box_street_01", 1 );
}

onplayerdisconnect( var_0 )
{
    if ( isdefined( var_0 ) && scripts\mp\flags::gameflag( "prematch_done" ) )
    {
        var_0 scripts\mp\gametypes\br_pickups.gsc::droponplayerdeath();

        if ( isdefined( level.get_player_array ) )
        {
            if ( level.teamdata[var_0.team]["players"].size == 0 )
                level.get_player_array--;
        }
        else
            computerrebootsequence_init();
    }
}

ononeleftevent( var_0 )
{

}

temp_debug_wait_and_stop_music_loop( var_0 )
{
    self endon( "disconnect" );
    self waittill( "spawned_player" );
    wait 1;
    scripts\mp\hud_message::showsplash( "br_prematch_welcome_dmz" );

    if ( !istrue( level.br_infils_disabled ) )
        self waittill( "joining_Infil" );
    else
        scripts\mp\flags::gameflagwait( "prematch_done" );

    wait 1;
    level thread computerrebootsequence_init();

    if ( soundexists( "dx_bra_uktl_bm_tut_earn_cash" ) )
        level scripts\mp\gametypes\br_public.gsc::chooseanim_vehicleturretdeath( "bm_tut_earn_cash", self );

    scripts\mp\hud_message::showsplash( "br_gametype_dmz_welcome" );

    if ( istrue( self.play_ending ) )
        level scripts\mp\gametypes\br_public.gsc::chooseanim_vehicleturretdeath( "deploy_squad_leader", self, 1, 0, 4.5 );

    if ( istrue( level.bot_get_distance_to_goal ) && istrue( level.br_prematchstarted ) )
        bot_get_human_picked_class( self );
}

_id_11E1D( var_0 )
{
    return istrue( var_0.br_infilstarted ) && scripts\mp\flags::gameflag( "prematch_done" );
}

keepstreamposfresh()
{

}

_id_122E7()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = 0;
    var_1 = 0;
    var_2 = 0;

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() )
        return;

    for (;;)
    {
        wait 1;
        var_3 = scripts\mp\gamelogic::gettimeremaining() / 1000;

        if ( istrue( level.brplacementchallengeallowed ) )
            break;

        if ( var_3 <= 600 && !var_0 )
        {
            var_0 = 1;
            setomnvar( "ui_br_circle_state", 1 );

            foreach ( var_5 in level.players )
                var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 69, undefined, 10 );

            continue;
        }

        if ( var_3 <= 300 && !var_1 )
        {
            var_1 = 1;
            setomnvar( "ui_br_circle_state", 2 );

            foreach ( var_5 in level.players )
                var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 70, undefined, 10 );

            continue;
        }

        if ( var_3 <= 0 && !var_2 )
        {
            var_2 = 1;

            if ( level._id_121AA )
                return;

            setomnvar( "ui_nuke_countdown_active", 0 );
            level.respawntagvisibility = 1;
            var_9 = hostage_confirm_good_angles();

            if ( isplayer( var_9 ) )
            {
                var_9 scripts\mp\trials\mp_trl_boneyard_gw_race::tryusenuke();
                wait 10;
                var_10 = hostskipburndownhigh();
                level thread scripts\mp\gamelogic::endgame( var_10, game["end_reason"]["mercy_win"], game["end_reason"]["mercy_loss"], 0, 1 );
            }
            else
            {
                var_10 = hostskipburndownhigh();
                level thread scripts\mp\gamelogic::endgame( var_10, game["end_reason"]["mercy_win"], game["end_reason"]["mercy_loss"], 0, 1 );
            }
        }
    }
}

computerrebootsequence_init()
{
    level.get_player_array = 0;

    foreach ( var_1 in level.teamdata )
    {
        if ( var_1["players"].size > 0 )
            level.get_player_array++;
    }
}

hostage_confirm_good_angles()
{
    var_0 = getarraykeys( level.teamdata );

    foreach ( var_2 in var_0 )
    {
        if ( level.teamdata[var_2]["alivePlayers"].size > 0 )
            return level.teamdata[var_2]["alivePlayers"][0];
    }

    return undefined;
}

hostskipburndownhigh()
{
    var_0 = getarraykeys( level.teamdata );

    foreach ( var_2 in var_0 )
    {
        if ( level.teamdata[var_2]["players"].size == 0 )
            return var_2;
    }

    return "tie";
}

onplayerkilled( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    scripts\mp\gametypes\br.gsc::onplayerkilled( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 );

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() )
        return;

    var_10 = getdvarint( "scr_bmo_mode_splash_window", 7500 );

    if ( isdefined( level._id_11E89 ) && level._id_11E89 + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "bm_vips_marked" );

    if ( isdefined( level._id_11E88 ) && level._id_11E88 + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "bm_extract_heli_start" );

    if ( isdefined( level._id_11E86 ) && level._id_11E86 + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "br_c130airdrop_incoming" );

    if ( isdefined( level._id_11E87 ) && level._id_11E87 + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "br_lootchopper_incoming" );
}

chopper_boss_destroyed_func( var_0 )
{
    return 1;
}

playerrespawn()
{
    level endon( "game_ended" );
    self endon( "disconnect" );

    if ( istrue( level.gameended ) )
        return;

    if ( getdvarint( "scr_bmo_use_spawn_fix", 1 ) == 1 )
        self endon( "brWaitAndSpawnClientComplete" );

    var_0 = scripts\mp\utility\teams::getteamdata( self.team, "teamCount" );

    if ( !istrue( self._id_11EC0 ) || var_0 == 1 )
    {
        var_1 = 1.0;
        wait( var_1 );
    }

    if ( istrue( self.hasrespawntoken ) && isdefined( level._id_12281 ) )
        var_2 = level._id_12281;
    else if ( level.never_unloaded_from_vehicle > 0.0 )
        var_2 = clamp( self.pers["deaths"] * level.never_unloaded_from_vehicle, 0.0, level.neverspectate );
    else if ( isdefined( level.bot_pickup_origin ) )
        var_2 = level.bot_pickup_origin;
    else
        var_2 = getdvarint( "scr_br_extract_spawn_wait", 20 );

    var_3 = getdvarfloat( "scr_bmo_respawn_predict_hint_time", 10.0 );

    if ( var_2 < var_3 )
        var_2 = var_3;

    if ( level.vo_exfil_helo_arrived_nag != 0 )
        var_2 = 0;

    var_4 = getdvarfloat( "scr_bmo_squad_wiped_stream_time", 5.0 );
    scripts\engine\utility::ent_flag_init( "playerRespawn_intermission_spawned" );
    self.patchoutofboundstrigger = undefined;
    self.path_data = undefined;

    if ( istrue( self._id_11EC0 ) || var_0 == 1 )
    {
        var_5 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
        var_6 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_5 );
        thread helitrig( 0.0, var_0 > 1 );
        wait( var_4 );
    }
    else if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
    {
        scripts\mp\utility\lower_message::setlowermessageomnvar( 9, int( gettime() + var_2 * 1000 ) );
        var_7 = var_2 - var_3;
        var_8 = var_2 - var_7;
        var_9 = var_2 - getdvarfloat( "scr_bmo_respawn_intermission_time", 6.0 );
        thread helitrig( var_9 );
        var_10 = scripts\engine\utility::waittill_notify_or_timeout_return( "squad_wipe_death", var_7 );

        if ( var_10 == "squad_wipe_death" )
        {
            var_5 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
            var_6 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_5 );
            thread helitrig( 0.0, 1 );
            wait( var_4 );
        }
        else
        {
            thread _id_125ED();
            var_10 = scripts\engine\utility::waittill_notify_or_timeout_return( "squad_wipe_death", var_8 );

            if ( var_10 == "squad_wipe_death" )
            {
                var_5 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
                var_6 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_5 );
                thread helitrig( 0.0, 1 );
                wait( var_4 );
            }
        }
    }

    self notify( "stop_updatePrestreamRespawn" );
    var_5 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
    var_6 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_5 );

    if ( istrue( self._id_11EC0 ) )
    {
        self._id_11EC0 = 0;

        if ( var_0 > 1 && !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
            scripts\mp\hud_message::showsplash( "bm_your_squad_wiped" );
    }

    if ( playerdatafield( self.team ) )
        return;

    if ( istrue( self.hasrespawntoken ) )
    {
        thread scripts\mp\gametypes\br_gulag.gsc::_id_12421( 4 );
        self._id_12280 = 1;
        scripts\mp\gametypes\br_pickups.gsc::removerespawntoken();
    }

    if ( playerdatafield( self.team ) )
        return;

    if ( getdvarint( "scr_skip_respawn_gate", 1 ) == 0 )
        scripts\mp\gametypes\br_public.gsc::teleport_trigger_to_airplane();

    scripts\engine\utility::ent_flag_clear( "playerRespawn_intermission_spawned" );
    self.patchoutofboundstrigger = undefined;
    self.path_data = undefined;
    scripts\mp\utility\lower_message::setlowermessageomnvar( 0 );
    scripts\mp\playerlogic::spawnplayer( undefined, 0 );
    scripts\cp_mp\execution::_clearexecution();
    scripts\mp\gametypes\br_pickups.gsc::initplayer();

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() && isdefined( level.stop_passedby_once ) )
        var_5 = scripts\engine\utility::getstruct( level.stop_passedby_once, "targetname" );

    scripts\mp\gametypes\br_gulag.gsc::gulagwinnerrespawn( 1, undefined, var_5, 1, var_6, 1 );
    scripts\mp\gametypes\br.gsc::_id_12531( self );
    level thread scripts\mp\battlechatter_mp::trysaylocalsound( self, "player_respawn" );
    scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_transition_type", 0 );
    scripts\mp\damage::resetplayervariables();
}

targeted_entities( var_0 )
{
    return 1;
}

_id_125ED()
{
    self endon( "disconnect" );
    self endon( "spawned_player" );
    self endon( "stop_updatePrestreamRespawn" );

    for (;;)
    {
        if ( scripts\engine\utility::ent_flag( "playerRespawn_intermission_spawned" ) )
        {
            var_0 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
            var_1 = gettime();

            if ( var_1 - self.path_data >= getdvarfloat( "scr_bmo_spawn_fallback_hint_delay", 2.0 ) * 1000 )
            {
                var_0 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset( 1 );
                var_2 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_0 );
            }
        }
        else
        {
            var_0 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
            var_2 = scripts\mp\gametypes\br_gulag.gsc::takelaststandtransitionweapon( var_0 );
        }

        wait 1.0;
    }
}

helitrig( var_0, var_1 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "spawned_player" );
    self notify( "fadeToGearingUp" );
    self endon( "fadeToGearingUp" );

    if ( isdefined( var_0 ) && var_0 > 0 )
        wait( var_0 );

    var_2 = 1.0;
    thread fadeoutin();
    wait( var_2 - 0.25 );
    scripts\mp\gametypes\br.gsc::clean_up_eliminate_drone();

    if ( istrue( var_1 ) )
        scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_transition_type", 6 );
    else
        scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_transition_type", 2 );

    wait 0.25;

    if ( getdvarint( "scr_bmo_use_spawn_intermission_fix", 1 ) == 1 )
    {
        scripts\mp\gametypes\br_public.gsc::stream_hit_location_for_players_inside();
        var_3 = scripts\mp\gametypes\br_gulag.gsc::superselectonunset();
        scripts\mp\gametypes\br_spectate.gsc::streakmatchlifeid();
        scripts\mp\gametypes\br.gsc::spawnintermission( var_3.origin, var_3.angles );
        scripts\mp\spectating::setdisabled();
        self.patchoutofboundstrigger = var_3.origin;
        self.path_data = gettime();
        scripts\engine\utility::ent_flag_set( "playerRespawn_intermission_spawned" );
    }
}

fadeoutin()
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    scripts\mp\gametypes\br_gulag.gsc::gulagfadetoblack();
    self waittill( "spawned_player" );
    scripts\mp\gametypes\br_gulag.gsc::gulagfadefromblack();
}

_id_11B4B()
{
    level.checkbluemassacre.helidelete = keycardlocs();
    thread relic_disable_health_regen();
}

keycardlocs()
{
    var_0 = 2500;
    var_1 = level.br_level.br_mapcenter;
    var_2 = ( 0, randomfloatrange( 0, 360 ), 0 );
    var_3 = anglestoforward( var_2 );
    var_4 = level.br_level.br_circleradii[0] * 2;
    var_5 = var_1 + var_3 * var_4;
    var_5 = scripts\mp\gametypes\br_c130.gsc::_id_11CA8( var_1, var_5 );
    var_6 = getent( "airstrikeheight", "targetname" );
    var_7 = ( var_5[0], var_5[1], var_6.origin[2] );
    var_8 = tracegroundpoint( var_7 );
    var_5 = var_8 + ( 0, 0, var_0 );
    return var_5;
}

relic_disable_health_regen()
{
    var_0 = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );
    level.checkbluemassacre.objectiveiconid = var_0;

    if ( var_0 != -1 )
    {
        scripts\mp\objidpoolmanager::objective_add_objective( var_0, "current", level.checkbluemassacre.helidelete, "icon_waypoint_koth" );
        scripts\mp\objidpoolmanager::update_objective_setbackground( var_0, 0 );
        scripts\mp\objidpoolmanager::objective_playermask_hidefromall( var_0 );
    }
}

_id_11B4C()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = juggmazedogtagrevive();
    var_1 = scripts\mp\gametypes\br_quest_util.gsc::getquesttableindex( "gt_extract_1" );

    foreach ( var_3 in level.players )
    {
        var_3 scripts\mp\gametypes\br_quest_util.gsc::_id_11AC1( var_1 );
        var_3 scripts\mp\gametypes\br_quest_util.gsc::uiobjectivesetparameter( var_0 );
    }
}

juggmazedogtagrevive()
{
    var_0 = getdvarint( "scr_br_extract_xp", 5000 );
    var_1 = getdvarint( "scr_br_extract_xp_min", 2000 );
    var_2 = getdvarint( "scr_br_extract_xp_decrease", 200 );

    if ( var_1 > 0 )
        var_0 = int( max( var_1, var_0 - level.checkbluemassacre.setgun * var_2 ) );

    return var_0;
}

_id_11C09( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        scripts\mp\objidpoolmanager::objective_playermask_addshowplayer( level.checkbluemassacre.objectiveiconid, var_3 );
}

model_ent( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        scripts\mp\objidpoolmanager::objective_playermask_hidefrom( level.checkbluemassacre.objectiveiconid, var_3 );
}

getcargotruckspawns()
{
    level.checkbluemassacre.setgun++;
    level.checkbluemassacre.mortar_start setvalue( level.checkbluemassacre.setgun );
    level.checkbluemassacre.mortar_start thread mortar_initmortars();
    level.checkbluemassacre.mortar_smoke_occlusions thread mortar_initmortars();
    var_0 = juggernauts_spawned();
    level.checkbluemassacre.mortar_add_fov_user_scale setvalue( var_0 * 100 );

    foreach ( var_3, var_2 in level.teamdata )
    {
        if ( level scripts\mp\utility\game::playerhumanconcusspushcleanup( var_3 ) )
            continue;

        if ( isdefined( var_2["teamCount"] ) && var_2["teamCount"] > 0 )
            _id_12600( var_3 );
    }

    var_4 = juggmazedogtagrevive();

    foreach ( var_6 in level.players )
    {
        if ( !playerdatafield( var_6.team ) )
        {
            var_6 thread scripts\mp\hud_message::showsplash( "br_gametype_extract_extracted" );
            var_6 scripts\mp\gametypes\br_quest_util.gsc::uiobjectivesetparameter( var_4 );
        }
    }
}

_id_11AD7( var_0 )
{
    level.teamdata[var_0]["extracted"] = 1;
}

playerdatafield( var_0 )
{
    return istrue( level.teamdata[var_0]["extracted"] );
}

brendgame()
{
    wait 1.5;
    handleendgamesplash();
    scripts\mp\gamelogic::endgame_regularmp( level.checkbluemassacre.hudattractionobj, game["end_reason"]["objective_completed"], game["end_reason"]["br_eliminated"] );
}

handleendgamesplash()
{
    foreach ( var_1 in level.players )
    {
        if ( !playerdatafield( var_1.team ) )
            var_1 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "post_game_state", 2 );
    }
}

_id_11B5E()
{
    level endon( "game_ended" );
    var_0 = 120;
    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( !istrue( level.br_infils_disabled ) )
        level waittill( "br_ready_to_jump" );

    waitframe();
    var_1 = end_silo_thrust( &"MP_BR_INGAME/EXTRACT_COLLECT_PLUNDER", undefined, "CENTER", "CENTER", 0, -170 );
    var_1.alpha = 1;
    var_2 = scripts\mp\hud_util::createservertimer( "default", 1.5 );
    var_2 scripts\mp\hud_util::setpoint( "CENTER", "CENTER", 0, -150 );
    var_3 = getdvarint( "scr_br_extract_timecollect", 180 );

    if ( var_3 > 0 )
    {
        setomnvar( "ui_hardpoint_timer", gettime() + int( var_3 * 1000 ) );
        var_2 settimer( var_3 );
        wait( var_3 );
    }

    scripts\mp\flags::gameflagset( "collect_done" );
    var_4 = getdvarint( "scr_br_extract_timewaitactive", 180 );

    if ( var_4 > 0 )
    {
        var_1.label = &"MP_BR_INGAME/EXTRACT_HELIPADS_ACTIVE";
        var_1 thread mortar_initmortars();
        setomnvar( "ui_hardpoint_timer", gettime() + int( var_4 * 1000 ) );
        var_2 settimer( var_4 );
        wait( var_4 );
    }

    scripts\mp\flags::gameflagset( "helipad_wait_done" );
    var_5 = getdvarint( "scr_br_extract_timeextract", 840 );
    var_1.label = &"MP_BR_INGAME/EXTRACT_HELIPAD";
    var_1 thread mortar_initmortars();
    setomnvar( "ui_hardpoint_timer", gettime() + int( var_5 * 1000 ) );
    var_2 settimer( var_5 );
    var_6 = max( var_5 - var_0, 0 );
    wait( var_6 );
    var_7 = max( var_5 - var_6, 0 );
    var_2.color = ( 1, 0, 0 );
    var_2 thread mortar_initmortars();
    thread disable_map_ammo_munitions( var_7 );
    wait( var_7 );
    var_2 destroy();
    thread brendgame();
}

disable_map_ammo_munitions( var_0 )
{
    level endon( "game_ended" );

    while ( var_0 > 0 )
    {
        var_1 = 0;
        var_2 = scripts\mp\gamelogic::juggernaut_kill_assists_included( var_0 );

        if ( var_0 > 60 && var_0 % 10 == 0 || var_0 <= 60 && var_0 > 30 && var_0 % 2 == 0 || var_0 <= 30 )
            var_1 = 1;

        if ( var_1 )
        {
            foreach ( var_4 in level.players )
                var_4 playlocalsound( var_2 );
        }

        var_0 = var_0 - 1;
        wait 1;
    }
}

_id_11B5C()
{
    var_0 = 155;
    var_1 = 15;
    var_2 = -3;
    var_3 = 3;

    if ( level._id_121AA )
    {
        var_4 = level_light();
        var_5 = &"MP_BR_INGAME/WIN_COST_TEXT";
        var_6 = var_4;
    }
    else
    {
        var_4 = juggernauts_spawned();
        var_5 = &"MP_BR_INGAME/EXTRACT_COST_TEXT";
        var_6 = var_4 * 100;
    }

    level.checkbluemassacre.mortar_cooldown = end_silo_thrust( var_5, undefined, "LEFT", "CENTER", var_3, var_0, undefined, undefined, 1 );
    level.checkbluemassacre.mortar_add_fov_user_scale = end_silo_thrust( &"MP_BR_INGAME/EXTRACT_COST_MILLION", undefined, "LEFT", "CENTER", 65 + var_3, var_0, undefined, undefined, 1 );
    level.checkbluemassacre.mousetraps = end_silo_thrust( &"MP_BR_INGAME/YOUR_TEAM_PLUNDER_TEXT", undefined, "RIGHT", "CENTER", 5 + var_2, var_0, undefined, undefined, 1 );
    var_7 = 0;
    level.checkbluemassacre.propane_detonate_fiery_drips = end_silo_thrust( &"MP_BR_INGAME/LEADER_PLUNDER_TEXT", var_7, "CENTER", "CENTER", 0, var_0 + var_1, undefined, undefined, 1 );

    foreach ( var_9 in level.teamnamelist )
    {
        var_10 = end_silo_thrust( &"MP_BR_INGAME/EXTRACT_PLUNDER", 0, "RIGHT", "CENTER", 70 + var_2, var_0, undefined, var_9, 1 );
        var_11 = end_silo_thrust( &"MP_BR_INGAME/ST_PLACE", undefined, "RIGHT", "CENTER", -65 + var_2, var_0, undefined, var_9, 1 );
        var_11 setvalue( 1 );
        var_10.placement = var_11;
        _id_11ADA( var_9, var_10 );
    }

    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( !istrue( level.br_infils_disabled ) )
        level waittill( "br_ready_to_jump" );

    level.checkbluemassacre.mortar_cooldown.alpha = 1;
    level.checkbluemassacre.mortar_add_fov_user_scale.alpha = 1;
    level.checkbluemassacre.mousetraps.alpha = 1;
    level.checkbluemassacre.propane_detonate_fiery_drips.alpha = 1;

    foreach ( var_9 in level.teamnamelist )
    {
        var_10 = lbravo_spawner_jammer3( var_9 );
        var_10.alpha = 1;
        var_10.placement.alpha = 1;
    }
}

_id_11ADA( var_0, var_1 )
{
    level.teamdata[var_0]["hudPlunder"] = var_1;
    var_1.plundercount = 0;
    var_1.tracegroundheightexfil = 0;
}

lbravo_spawner_jammer3( var_0 )
{
    return level.teamdata[var_0]["hudPlunder"];
}

end_silo_thrust( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    if ( !isdefined( var_8 ) )
        var_8 = 1.5;

    if ( isdefined( var_7 ) )
        var_10 = newteamhudelem( var_7 );
    else if ( isdefined( var_9 ) )
        var_10 = newclienthudelem( var_9 );
    else
        var_10 = newhudelem();

    var_10.elemtype = "font";
    var_10.font = "default";
    var_10.fontscale = var_8;
    var_10.basefontscale = var_8;
    var_10.x = 0;
    var_10.y = 0;
    var_10.width = 0;
    var_10.height = int( level.fontheight * var_8 );
    var_10.xoffset = 0;
    var_10.yoffset = 0;
    var_10.children = [];
    var_10 scripts\mp\hud_util::setparent( level.uiparent );
    var_10.hidden = 0;
    var_10.alpha = 0;
    var_10 scripts\mp\hud_util::setpoint( var_2, var_3, var_4, var_5 );

    if ( isdefined( var_0 ) )
        var_10.label = var_0;

    if ( isdefined( var_1 ) )
        var_10 setvalue( var_1 );

    if ( isdefined( var_6 ) )
        var_10.color = var_6;

    return var_10;
}

lbravo_spawner_jammer2b( var_0 )
{
    var_1 = 0;

    if ( !level.weapondrop_createdropondeath )
    {
        var_1 = var_1 + level.teamdata[var_0]["plunderTeamTotal"];
        var_1 = var_1 + level.teamdata[var_0]["plunderInDeposit"];
    }

    var_1 = var_1 + level.teamdata[var_0]["plunderBanked"];
    return var_1;
}

lbravo_hover_rider_death_monitor( var_0 )
{
    return level.teamdata[var_0]["plunderTeamTotal"];
}

jugg_canuseweaponpickups( var_0 )
{
    return level.teamdata[var_0]["plunderBanked"];
}

_id_11CE3( var_0, var_1 )
{
    return var_0 > var_1;
}

lootchopper_getzonebyindex( var_0, var_1 )
{
    var_2 = int( min( var_1, 131072 ) );
    scripts\mp\trials\mp_trl_floorislava::_id_11A6F( var_0, var_2 );
}

lootchopper_isnearbyoccupiedspawns( var_0, var_1 )
{
    var_2 = int( min( var_1, 131072 ) );
    scripts\mp\trials\mp_trl_floorislava::_id_11A70( var_0, var_2 );
}

trackcarpunches()
{
    level endon( "game_ended" );
    var_0 = 0;
    var_1 = [];
    var_2 = 0;
    var_3 = 0;
    var_4 = 0;
    var_5 = 0;
    var_6 = 0;
    var_7 = 0;
    var_8 = level.moderestrictsarenakillstreaks != 0;
    level._id_11E89 = undefined;
    level._id_11E88 = undefined;
    level._id_11E86 = undefined;
    level._id_11E87 = undefined;
    var_9 = gettime();
    var_10 = getdvarfloat( "scr_dmz_print_error_cutoff", 30.0 );

    for (;;)
    {
        waittillframeend;
        var_11 = ( gettime() - var_9 ) / 1000;
        var_12 = level.convert_remaining_to_ai && level.convoy4_actively_hacking && level.convoy4_failed_extract;
        var_13 = level_light();
        var_0 = var_0 + level.framedurationseconds;

        if ( istrue( level._id_12657 ) )
            var_14 = scripts\mp\flags::gameflag( "placement_updates_allowed" );
        else
            var_14 = var_0 > level.xanimlength;

        var_15 = level._id_11C16 && var_14;
        var_16 = scripts\mp\gamescore::lbravo_spawner_jammer3b();
        var_17 = level._id_121AA;
        var_18 = undefined;
        var_19 = undefined;
        var_20 = undefined;
        var_21 = [];
        var_22 = [];
        var_23 = [];
        var_24 = "none";
        var_25 = "none";
        var_26 = -1;
        var_27 = -1;

        foreach ( var_29 in level.teamnamelist )
        {
            if ( level scripts\mp\utility\game::playerhumanconcusspushcleanup( var_29 ) )
                continue;

            var_30 = lbravo_spawner_jammer2b( var_29 );
            var_31 = jugg_canuseweaponpickups( var_29 );
            var_32 = _id_12601( var_30, var_29, level.checkbluemassacre.hudattractionobj );
            var_33 = lastbored( var_29 );
            var_34 = var_16[var_29];

            if ( var_30 > var_26 )
            {
                if ( var_26 > var_27 )
                {
                    var_27 = var_26;
                    var_25 = var_24;
                }

                var_26 = var_30;
                var_24 = var_29;
            }
            else if ( var_24 != "none" )
            {
                if ( var_30 > var_27 )
                {
                    var_27 = var_30;
                    var_25 = var_29;
                }
            }

            var_35 = ( var_30 - var_31 ) * 100;
            var_36 = var_30 * 100;
            var_37 = var_36 - var_35;

            if ( var_35 < 0 )
                var_35 = 0;

            if ( var_36 >= var_13 * 0.9 )
                var_18 = var_29;
            else if ( var_36 >= var_13 * 0.75 )
                var_19 = var_29;
            else if ( var_36 >= var_13 * 0.5 )
                var_20 = var_29;

            if ( var_36 >= var_13 * level.track_is_operational )
                thread scripts\mp\music_and_dialog::total_successful_vehicle_spawns();
            else if ( var_36 >= var_13 * level.track_settings )
                thread scripts\mp\music_and_dialog::tr_kiosksearchparams();
            else if ( var_36 >= var_13 * level.track_last_good_position )
                thread scripts\mp\music_and_dialog::toppercentagetoadjusteconomy();
            else if ( var_36 >= var_13 * level.track_get_teleport_velocity )
                thread scripts\mp\music_and_dialog::tr_playerdisconnect();

            var_38 = scripts\mp\utility\teams::getfriendlyplayers( var_29, 0 );

            foreach ( var_40 in var_38 )
            {
                var_34 = var_16[var_40.team];

                if ( !var_6 )
                    var_34 = 155;

                var_40 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_team_placement", var_34 );
                var_40 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_player_position", var_34 );
                var_40 lootchopper_getzonebyindex( "ui_br_team_cash_banked", int( var_37 * 0.01 ) );
                var_40 lootchopper_getzonebyindex( "ui_br_team_cash_pockets", int( var_35 * 0.01 ) );
                var_22[var_40.guid] = var_40.plundercount;
                var_23[var_40.guid] = var_40;
            }

            if ( var_15 )
            {
                if ( var_34 == 1 )
                    var_21[var_21.size] = var_29;
                else if ( var_34 <= 5 )
                {
                    if ( var_33 > 5 )
                        showsplashtoteam( var_29, "bm_top_5" );
                }
                else if ( var_34 <= 10 )
                {
                    if ( var_33 > 10 )
                        showsplashtoteam( var_29, "bm_top_10" );
                }
            }

            if ( var_36 >= var_13 )
            {
                bot_get_low_on_all_ammo( var_29 );

                if ( var_17 && level.get_showing_bomb_wire_pair_to_player > 0 && !level.get_grenadedanger_alias )
                {
                    level thread _id_1241E( var_29, level.teamdata[var_29]["plunderBanked"] * 1000 >= var_13 );
                    continue;
                }

                if ( var_17 && level.get_showing_bomb_wire_pair_to_player > 0 && level.get_grenadedanger_alias )
                {
                    level.openaltbunker = var_29;
                    continue;
                }

                if ( level._id_121A6 && !istrue( level.exfilactive ) )
                {
                    level.exfilactive = 1;
                    level thread _id_11FA9( var_29 );
                    continue;
                }

                if ( !level._id_121A6 && var_17 && !level.get_grenadedanger_alias )
                    level thread little_bird_mg_creategunnerturret( var_29 );
            }
        }

        var_43 = [];
        var_44 = [];

        if ( level.register_wave_spawner == 1 )
            var_43 = tablesort( game["teamPlacements"], "up" );
        else if ( level.register_wave_spawner == 2 )
        {
            var_43[0] = var_24;
            var_44 = tablesort( var_22, "down" );
        }
        else
            var_44 = tablesort( var_22, "down" );

        level.checkbluemassacre.hudattractionobj = var_24;

        if ( var_24 == "none" )
        {
            var_1 = [];
            waitframe();
            continue;
        }

        if ( var_25 != "none" )
            lootchopper_isnearbyoccupiedspawns( "ui_br_cash_second", int( var_27 ) );

        lootchopper_isnearbyoccupiedspawns( "ui_br_cash_leader", int( var_26 ) );

        if ( !var_8 && var_26 * 100 >= var_13 * level.moderestrictsarenakillstreaks * 0.01 )
        {
            setomnvar( "ui_br_leader_hash_percentage_hit", 1 );
            var_8 = 1;
        }

        if ( !istrue( var_7 ) && istrue( level._id_12657 ) )
        {
            if ( !var_2 && var_26 * 100 >= var_13 * level.retry_no_votes * 0.01 )
            {
                var_2 = 1;
                level._id_11E89 = gettime();
                scripts\mp\flags::gameflagset( "placement_updates_allowed" );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                    _id_11C20( "bm_vips_marked" );
            }

            if ( !var_3 && ( level.weapondrop_createdropondeath || var_26 * 100 >= var_13 * level.retrieve_data_objective * 0.01 ) )
            {
                var_3 = 1;
                level._id_11E88 = gettime();
                scripts\mp\flags::gameflagset( "activate_cash_lzs" );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                    scripts\mp\gametypes\br_public.gsc::brleaderdialog( "extract_enabled", 0, undefined, undefined, undefined, "bm" );
            }

            if ( !var_4 && var_26 * 100 >= var_13 * level.retreat_vehicle_collision_clear * 0.01 )
            {
                var_4 = 1;
                level._id_11E86 = gettime();
                level.br_level.c130_speedoverride = 3044;
                scripts\mp\flags::gameflagset( "activate_cash_drops" );
            }

            if ( !var_5 && ( istrue( level.brplacementchallengeallowed ) || var_26 * 100 >= var_13 * level.retreatanddie * 0.01 ) )
            {
                var_5 = 1;
                level._id_11E87 = gettime();
                scripts\mp\flags::gameflagset( "activate_cash_helis" );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                    scripts\mp\gametypes\br_public.gsc::brleaderdialog( "event_chopper", 0, undefined, undefined, undefined, "bm" );
            }

            if ( !var_6 && var_26 * 100 >= var_13 * level.modespawnendofgame * 0.01 )
                var_6 = 1;

            if ( var_2 && var_3 && var_4 && var_5 )
                var_7 = 1;
        }

        if ( !var_15 || var_26 == 0 )
        {
            waitframe();
            continue;
        }

        if ( level.register_wave_spawner > 0 )
        {
            if ( level.get_player_defusing_zone || istrue( level.trackriotshield_grenadepullbackforc4 ) )
            {
                _id_125DA( var_22, var_44, var_23, var_43 );
                level.trackriotshield_grenadepullbackforc4 = 0;
            }
        }
        else if ( level.get_player_defusing_zone || istrue( level.trackriotshield_grenadepullbackforc4 ) )
        {
            _id_125DA( var_22, var_44, var_23 );
            level.trackriotshield_grenadepullbackforc4 = 0;
        }

        foreach ( var_29 in var_21 )
        {
            if ( !scripts\engine\utility::array_contains( var_1, var_29 ) )
            {
                if ( istrue( level._id_12290[var_29] ) )
                {
                    if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                        showsplashtoteam( var_29, "bm_top_team_regained" );

                    continue;
                }

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                    showsplashtoteam( var_29, "bm_top_team" );

                level._id_12290[var_29] = 1;
            }
        }

        if ( var_21.size > 0 )
        {
            foreach ( var_29 in var_1 )
            {
                if ( !scripts\engine\utility::array_contains( var_21, var_29 ) )
                    showsplashtoteam( var_29, "bm_top_team_lost" );
            }
        }

        var_1 = var_21;
        level._id_1228F = var_24;
        level.trial_dlog_pitcher = var_16;

        if ( !var_12 )
        {
            if ( !level.convoy4_failed_extract && isdefined( var_18 ) )
            {
                level.convoy4_failed_extract = 1;

                if ( var_11 < var_10 )
                    level trial_fetch_mission_table( "90 Percent", var_11 );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                {
                    if ( !level.get_extra_focus_fire_multipler )
                    {
                        _id_11C21( var_18, "bm_first_to_90_them" );
                        showsplashtoteam( var_18, "bm_first_to_90_us" );
                        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_90_perc_first", var_18, 1, undefined, "bm" );

                        foreach ( var_50 in level.teamnamelist )
                        {
                            if ( var_50 != var_18 )
                                level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_90_perc_enemy", var_50, 1, undefined, "bm" );
                        }
                    }
                }
            }
            else if ( !level.convoy4_actively_hacking && isdefined( var_19 ) )
            {
                level.convoy4_actively_hacking = 1;

                if ( var_11 < var_10 )
                    level trial_fetch_mission_table( "75 Percent", var_11 );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                {
                    if ( !level.get_extra_focus_fire_multipler )
                    {
                        _id_11C21( var_19, "bm_first_to_75_them" );
                        showsplashtoteam( var_19, "bm_first_to_75_us" );
                        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_75_perc_first", var_19, 1, undefined, "bm" );

                        foreach ( var_50 in level.teamnamelist )
                        {
                            if ( var_50 != var_19 )
                                level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_75_perc_enemy", var_50, 1, undefined, "bm" );
                        }
                    }
                }
            }
            else if ( !level.convert_remaining_to_ai && isdefined( var_20 ) )
            {
                level.convert_remaining_to_ai = 1;

                if ( var_11 < var_10 )
                    level trial_fetch_mission_table( "50 Percent", var_11 );

                if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                {
                    if ( !level.get_extra_focus_fire_multipler )
                    {
                        _id_11C21( var_20, "bm_first_to_50_them" );
                        showsplashtoteam( var_20, "bm_first_to_50_us" );
                        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_50_perc_first", var_20, 1, undefined, "bm" );

                        foreach ( var_50 in level.teamnamelist )
                        {
                            if ( var_50 != var_20 )
                                level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "gamestate_50_perc_enemy", var_50, 1, undefined, "bm" );
                        }
                    }
                }
            }
        }

        waitframe();
    }
}

bot_get_low_on_all_ammo( var_0 )
{
    if ( !isdefined( level.bot_get_landing_spot ) )
        level.bot_get_landing_spot = [];

    if ( scripts\engine\utility::array_contains( level.bot_get_landing_spot, var_0 ) )
        return;

    var_1 = scripts\mp\utility\teams::getteamdata( var_0, "players" );
    var_2 = scripts\mp\gametypes\br_plunder.gsc::endgame_luidecisionreceived();
    var_2.start_player_links = 0;

    foreach ( var_4 in var_1 )
    {
        if ( isdefined( var_4.plundercount ) && var_4.plundercount > 0 )
            var_4 scripts\mp\gametypes\br_plunder.gsc::tac_rover_initcollision( var_4.plundercount, undefined, var_2 );
    }

    level.bot_get_landing_spot[level.bot_get_landing_spot.size] = var_0;
}

smoke_wheelson_chosen_spawn()
{
    level thread scripts\mp\gametypes\br_plunder.gsc::trial_patch_finished( level.smoke_wheelson_chosen_spawn );
}

spawnprojectile()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( isdefined( level.xanimlength ) )
        wait( level.xanimlength );

    scripts\mp\flags::gameflagset( "placement_updates_allowed" );
}

lastbored( var_0 )
{
    if ( isdefined( level.trial_dlog_pitcher ) )
        return level.trial_dlog_pitcher[var_0];

    return -1;
}

_id_11C20( var_0 )
{
    foreach ( var_2 in level.players )
        var_2 scripts\mp\hud_message::showsplash( var_0 );
}

showsplashtoteam( var_0, var_1 )
{
    foreach ( var_3 in level.teamdata[var_0]["players"] )
        var_3 scripts\mp\hud_message::showsplash( var_1 );
}

_id_11C21( var_0, var_1 )
{
    foreach ( var_3 in level.players )
    {
        if ( isdefined( var_3 ) && var_3.team != var_0 )
            var_3 scripts\mp\hud_message::showsplash( var_1 );
    }
}

_id_1241D( var_0 )
{
    scripts\mp\gamelogic::resumetimer();
    level.starttime = gettime();
    level.discardtime = 0;
    level.timerpausetime = 0;
    var_1 = getdvarfloat( "scr_bmo_900k_timer", 10 );
    var_2 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
    level.watchdvars[var_2].value = var_1;
    level.overridewatchdvars[var_2] = var_1;
}

_id_12600( var_0 )
{
    var_1 = lbravo_spawner_jammer3( var_0 );
    var_2 = lbravo_spawner_jammer2b( var_0 );
    var_3 = juggernauts_spawned();

    if ( var_2 >= var_3 && var_1.color != ( 0, 1, 0 ) )
    {
        var_1.color = ( 0, 1, 0 );
        var_4 = level.teamdata[var_0]["players"];

        foreach ( var_6 in var_4 )
            var_6 playlocalsound( "br_plunder_atm_deposit_gtr" );
    }
}

get_target_group( var_0, var_1 )
{
    var_2 = level.teamdata[var_1]["players"];

    foreach ( var_4 in var_2 )
    {
        if ( var_0 > 0 )
        {
            if ( isdefined( var_4.mousetraplocs ) )
            {
                var_5 = gettime() - var_4.mousetraplocs;

                if ( var_5 <= 6000 )
                    break;
            }

            var_4.mousetraplocs = gettime();

            if ( isalive( var_4 ) )
                var_4 playlocalsound( "br_plunder_atm_use" );

            continue;
        }

        var_4.mousetraplocs = undefined;
        var_4 stoplocalsound( "br_plunder_atm_use" );
    }
}

little_bird_mg_creategunnerturret( var_0 )
{
    if ( istrue( level._id_12418 ) )
        return;

    if ( !istrue( level._id_12418 ) )
        level thread trial_flare_destruct_missile();

    level._id_12418 = 1;
    level thread scripts\mp\gamelogic::endgame( var_0, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
}

playerdropplunderondeath( var_0, var_1 )
{
    if ( scripts\mp\utility\game::player_grenade_fire_monitor( self ) )
        return 1;

    if ( istrue( level.gameended ) )
        return 1;

    if ( istrue( self.play_operator_reply_vo ) )
    {
        var_2 = self.plundercount;
        var_3 = self.plundercount;
        var_4 = 0;
    }
    else
    {
        if ( isdefined( level.rocket_fuel_x2 ) && self.plundercount < level.rocket_fuel_x2 )
        {
            var_4 = 0;
            var_2 = self.plundercount;
            var_3 = self.plundercount;
        }
        else
        {
            var_4 = int( self.plundercount * ( ( 100 - level.spawn_module_p3_form_c ) * 0.01 ) );
            var_2 = self.plundercount - var_4;
            var_3 = var_2;
        }

        if ( istrue( level.brplacementchallengeallowed ) )
        {
            if ( var_2 <= level.bush_zones && ( !isdefined( var_1 ) || var_1 != self ) )
                var_2 = level.bush_zones;
        }
        else if ( var_2 <= level.bush_trig_debug && ( !isdefined( var_1 ) || var_1 != self ) )
            var_2 = level.bush_trig_debug;

        if ( isdefined( level.removestuckenemyondeathordisconnect ) )
        {
            if ( var_2 >= level.removestuckenemyondeathordisconnect )
            {
                var_2 = level.removestuckenemyondeathordisconnect;
                var_3 = var_2;
                var_4 = self.plundercount - level.removestuckenemyondeathordisconnect;
            }
        }
    }

    self.plundercountondeath = var_4;

    if ( var_3 > 0 )
        scripts\mp\gametypes\br_plunder.gsc::tacinsert_brrespawnsplash( var_3 );

    if ( var_2 >= level.track_get_reward_time )
        playfx( scripts\engine\utility::getfx( "money" ), self.origin + ( 0, 0, 32 ) );

    var_5 = var_2;

    if ( istrue( self._id_12983 ) )
        var_5 = int( var_2 * level.has_headicon );

    var_6 = var_5;

    if ( istrue( level.brplacementchallengeallowed ) && ( !isdefined( var_1 ) || self != var_1 ) )
    {
        var_6 = int( var_5 * level.smoke_wheelson_chosen_spawn );

        if ( isdefined( level.removestructfromlevelarray ) && var_6 > level.removestructfromlevelarray )
            var_6 = level.removestructfromlevelarray;
    }

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() && istrue( self.currentvalue ) )
        var_6 = int( var_6 + self.currentvalue );

    var_7 = dropplunderbyrarity( var_6, var_0 );

    foreach ( var_9 in var_7 )
        var_9.registercontrolledcallback = "combat";

    if ( scripts\mp\gametypes\br_public.gsc::play_player_approach() )
        level notify( "victim_death_drop", self, var_1, var_7 );

    if ( isdefined( var_1 ) && self == var_1 || !level.killcam )
        scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_plunder_dropped", var_3 );
    else
        self.train_minimap_icon_attach = var_3;

    scripts\mp\gametypes\br_analytics.gsc::_id_122CD( self, "combat", var_3 * -1 );

    if ( isdefined( level.registercarryobjectpickupcheck ) && scripts\engine\utility::array_contains( level.registercarryobjectpickupcheck, self ) )
        playfx( scripts\engine\utility::getfx( "money" ), self.origin + ( 0, 0, 64 ) );

    return 1;
}

shouldmodelognotify()
{
    if ( isdefined( self.train_minimap_icon_attach ) )
    {
        scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_plunder_dropped", int( self.train_minimap_icon_attach ) );
        self.train_minimap_icon_attach = undefined;
    }
}

dropplunderbyrarity( var_0, var_1 )
{
    if ( !istrue( level.br_plunder_enabled ) )
        return;

    var_2 = [];
    var_3 = [];
    var_4 = 0;

    for ( var_5 = level.br_plunder.turn_on_have_target_hud.size - 1; var_5 >= 0; var_5-- )
    {
        var_3[var_5] = int( var_0 / level.br_plunder.turn_on_have_target_hud[var_5] );
        var_4 = var_4 + var_3[var_5];

        if ( var_0 <= 0 )
            break;
        else
            var_0 = var_0 - var_3[var_5] * level.br_plunder.turn_on_have_target_hud[var_5];
    }

    for ( var_6 = level.br_plunder.turn_on_have_target_hud.size - 1; var_6 >= 0; var_6-- )
    {
        if ( !isdefined( var_3[var_6] ) )
            continue;

        for ( var_7 = 0; var_7 < var_3[var_6]; var_7++ )
        {
            var_8 = scripts\mp\gametypes\br_pickups.gsc::getitemdroporiginandangles( var_1, self.origin, self.angles, self );
            var_9 = scripts\mp\gametypes\br_pickups.gsc::spawnpickup( level.br_plunder.names[var_6], var_8, level.br_plunder.turn_on_have_target_hud[var_6], 1 );
            scripts\mp\gametypes\br_plunder.gsc::run_func_on_each_player( level.br_plunder.names[var_6], 1 );

            if ( isdefined( var_9 ) )
            {
                var_2[var_2.size] = var_9;

                if ( scripts\mp\gametypes\br_plunder.gsc::inplunderlivelobby() )
                    level.br_plunder_ents[level.br_plunder_ents.size] = var_9;
            }
        }
    }

    level.br_plunder.total_puddle_count = level.br_plunder.total_puddle_count + var_4;
    level.br_plunder.tr_respawn = level.br_plunder.tr_respawn + var_0;
    level thread scripts\mp\gametypes\br_plunder.gsc::dropplundersounds( self.origin, var_2.size );
    return var_2;
}

juggernauts_spawned()
{
    if ( level.get_player_drone > 0 )
        var_0 = int( max( level.get_player_drone, level.get_player_closest_to_any_goal - level.checkbluemassacre.setgun * level.get_player_cumulative_damage ) );

    return level.get_player_closest_to_any_goal;
}

level_light()
{
    return level.get_player_who_most_likely_broke_stealth;
}

teamplunderexfil( var_0 )
{
    if ( self.plundercount < var_0 )
        var_0 = self.plundercount;

    if ( !isdefined( self.track_get_launch_velocity ) )
        self.track_get_launch_velocity = 0;

    self.track_get_launch_velocity = self.track_get_launch_velocity + var_0;
    scripts\mp\gametypes\br_plunder.gsc::playersetplundercount( self.plundercount - var_0 );
    return var_0;
}

_id_12196( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        var_3.track_get_launch_velocity = 0;
}

_id_121AB( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
    {
        if ( isdefined( var_3.track_get_launch_velocity ) )
        {
            var_3 scripts\mp\gametypes\br_plunder.gsc::tacinsert_pickuplistener( var_3.track_get_launch_velocity );
            var_3.track_get_launch_velocity = 0;
            var_3 iprintlnbold( "Extraction refunded, chopper shot down." );
        }
    }
}

mortar_initmortars()
{
    self endon( "death" );

    if ( istrue( self.tryspawnscriptable ) )
        return;

    var_0 = 0.5;
    var_1 = 4;
    self.tryspawnscriptable = 1;
    var_2 = self.fontscale;
    self changefontscaleovertime( var_0 );
    self.fontscale = var_1;
    wait( var_0 );
    self changefontscaleovertime( var_0 );
    self.fontscale = var_2;
    self.tryspawnscriptable = undefined;
}

drophelicrate( var_0 )
{
    var_1 = spawn( "script_model", var_0.origin );
    var_1 setmodel( "military_skyhook_backpack" );
    var_2 = var_0.origin;
    var_3 = ( var_2[0], var_2[1], -12000 );
    var_4 = scripts\engine\trace::create_contents( 0, 1, 1, 1, 1, 1, 0 );
    var_5 = scripts\engine\trace::ray_trace( var_2, var_3, var_0, var_4 );
    var_6 = var_5["position"];
    var_7 = var_2[2] - var_6[2];

    if ( var_7 > 0 )
    {
        var_8 = sqrt( 2 * var_7 / 800 );
        var_1 moveto( var_6, var_8, var_8, 0 );
        wait( var_8 );
    }

    var_1.origin = var_6;
    playfx( scripts\engine\utility::getfx( "airdrop_crate_impact" ), var_6 );
    var_1 cratedropplunder();
    var_1 delete();
}

cratedropplunder()
{
    var_0 = juggernauts_spawned();
    var_1 = scripts\mp\gametypes\br_pickups.gsc::onsquadeliminatedplacement();
    scripts\mp\gametypes\br_plunder.gsc::dropplunderbyrarity( var_0, var_1 );
}

computer_prompt_pressed( var_0, var_1, var_2 )
{
    var_3 = var_0 * 0.0000157828;
    var_4 = 0.5 * var_2;
    var_5 = var_1;
    var_6 = -1 * var_3;
    var_7 = ( -1 * var_5 + sqrt( var_5 * var_5 - 4 * var_4 * var_6 ) ) / ( 2 * var_4 );
    var_7 = var_7 * 3600;
    var_7 = var_7 + 1.5;
    return var_7;
}

computer_player_listener( var_0 )
{
    var_1 = computer_prompt_pressed( 30000, 100, 125 );
    var_2 = computer_prompt_pressed( var_0, 25.0, 31.25 );
    var_3 = var_1 + var_2;
    return var_3;
}

minigunbackup( var_0, var_1 )
{
    var_2 = gettime() + int( var_1 * 1000 );
    var_3 = level.teamdata[self.team]["alivePlayers"];

    foreach ( var_5 in var_3 )
    {
        var_5 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_plunder_extract_state", var_0 );
        var_5 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_plunder_extract_end_time", var_2 );
    }
}

mine_caves_chopper()
{
    if ( !isdefined( self.plunder ) )
        return;

    level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "plunder_extract_success", self.team, 1 );
    var_0 = 0;
    var_1 = 0;

    foreach ( var_3 in self.plunder )
    {
        var_0 = var_0 + var_3.plundercount;

        if ( var_3.player.team != self.team )
            var_1 = 1;
    }

    scripts\mp\gametypes\br_analytics.gsc::cave_initial_enemy_goto_struct_on_spawn_and_wait_till_seen( self.plunder.size, var_0, "little_bird", var_1, self.endpoint );
    level.br_plunder.heli_outro = level.br_plunder.heli_outro + var_0;
    level.br_plunder.heli_orbit_logic++;
    scripts\mp\gametypes\br_plunder.gsc::handle_settings_for_techo_group();
}

heliusecleanup()
{
    if ( isdefined( self.usable ) )
    {
        level.br_depots = scripts\engine\utility::array_remove( level.br_depots, self.usable );
        self.usable = undefined;
    }
}

helicleanupdepotonleaving( var_0 )
{
    self.usable endon( "death" );
    scripts\engine\utility::waittill_either( "leaving", "death" );
    heliusecleanup();
}

helicreateextractvfx( var_0 )
{
    self.vfxent = spawn( "script_model", var_0 );
    self.vfxent setmodel( "scr_smoke_grenade" );
    self.vfxent.angles = ( 0, 90, 90 );
    self.vfxent playloopsound( "smoke_carepackage_smoke_lp" );
    self.vfxent setscriptablepartstate( "smoke", "on" );
}

helicleanupextract( var_0 )
{
    if ( isdefined( self.vfxent ) )
    {
        self.vfxent stoploopsound();
        self.vfxent delete();
    }

    if ( istrue( var_0 ) && isdefined( self.site ) )
        self.site setscriptablepartstate( self.site.type, self.site.airlock_show_room_doors );
}

mine_caves_turret_op()
{
    self endon( "death" );

    if ( !isdefined( self.vfxent ) )
        return;

    wait 5;
    self.vfxent endon( "death" );
    self.vfxent setscriptablepartstate( "smoke", "dissipate" );
    self.vfxent playsound( "smoke_canister_tail_dissipate" );
    wait 1;
    self.vfxent stoploopsound();
    wait 4.5;
    self.vfxent delete();
}

spawnheli( var_0, var_1, var_2, var_3 )
{
    var_4 = vectortoangles( var_2 - var_1 );
    var_5 = 99;
    var_6 = scripts\cp_mp\vehicles\vehicle_tracking::_spawnhelicopter( var_0, var_1, var_4, "veh_apache_plunder_mp", "veh8_mil_air_mindia8_plunder_x" );

    if ( !isdefined( var_6 ) )
        return;

    var_7 = var_2 * ( 1, 1, 0 );
    var_6.damagecallback = ::callback_vehicledamage;
    var_6.speed = 50;
    var_6.accel = 99999;
    var_6.health = 1000;
    var_6.maxhealth = var_6.health;
    var_6.team = var_0.team;
    var_6.owner = var_0;
    var_6.defendloc = var_2;
    var_6.lifeid = 0;
    var_6.flaresreservecount = var_5;
    var_6.pathgoal = var_2;
    var_6.spawn_bomb_hostage = var_3;
    var_6.endpoint = var_7;
    var_6.little_bird_mg_reenter = var_4[1];
    var_6.vehiclename = "magma_plunder_chopper";
    var_6 setcandamage( 1 );
    var_6 setmaxpitchroll( 10, 25 );
    var_6 vehicle_setspeed( var_6.speed, var_6.accel );
    var_6 sethoverparams( 50, 100, 50 );
    var_6 setturningability( 0.05 );
    var_6 setyawspeed( 45, 25, 25, 0.5 );
    var_6 setotherent( var_0 );
    _id_11E4A( var_6 );
    var_6 thread scripts\mp\killstreaks\flares::flares_handleincomingstinger( undefined, undefined );
    var_6 thread max_dist_sq_from_node();
    var_6 thread handledestroydamage();
    var_6 thread mine_caves_turret_2_support_internal();
    return var_6;
}

_id_11E4A( var_0 )
{
    var_1 = spawn( "script_model", ( 0, 0, 0 ) );
    var_1 setmodel( "br_plunder_extraction_delivery_rope" );
    var_1 linkto( var_0, "side_door_l_jnt", ( 11, 20, 42 ), ( 0, 180, 0 ) );
    var_2 = spawn( "script_model", ( 0, 0, 0 ) );
    var_2 setmodel( "br_plunder_extraction_delivery_bag" );
    var_2 linkto( var_1, "dyn_rope_end", ( 0, 0, 0 ), ( 0, 0, 0 ) );
    var_0.rope = var_1;
    var_0.crate = var_2;
}

mine_caves_turret_2_support_internal()
{
    self endon( "heli_gone" );
    self endon( "swapped" );
    var_0 = self.owner;
    var_1 = self.team;
    self waittill( "death", var_2, var_3, var_4, var_5 );
    _id_121AB( var_1 );
    mine_caves_end_support_internal();

    if ( !isdefined( self ) )
        return;

    if ( !isdefined( self.largeprojectiledamage ) && !istrue( self.isdepot ) )
    {
        self vehicle_setspeed( 25, 5 );
        thread mine_caves_support_combat( 75 );
        scripts\mp\hostmigration::waitlongdurationwithhostmigrationpause( 2.7 );
    }

    mine_light_vfx( var_2 );
}

mine_caves_end_support_internal()
{
    if ( isdefined( self.rope ) )
        self.rope delete();

    if ( isdefined( self.crate ) )
        self.crate delete();
}

mine_light_vfx( var_0 )
{
    var_1 = self gettagorigin( "tag_origin" ) + ( 0, 0, 40 );
    self radiusdamage( var_1, 256, 140, 70, var_0, "MOD_EXPLOSIVE" );
    playfx( scripts\engine\utility::getfx( "little_bird_explode" ), var_1, anglestoforward( self.angles ), anglestoup( self.angles ) );
    playsoundatpos( var_1, "veh_chopper_support_crash" );
    earthquake( 0.4, 800, var_1, 0.7 );
    playrumbleonposition( "grenade_rumble", var_1 );
    physicsexplosionsphere( var_1, 500, 200, 1 );
    self notify( "explode" );
    wait 0.35;
    level thread scripts\mp\gametypes\br.gsc::_id_121B2( "br_gametype_extract_heli_shot_down", self.owner, self.owner.team );
    helicleanupextract( 1 );
    mine_caves_turret_2_support();
}

mine_caves_turret_2_support()
{
    scripts\cp_mp\vehicles\vehicle_tracking::_deletevehicle( self );
}

mine_caves_support_combat( var_0 )
{
    self endon( "explode" );
    self notify( "heli_crashing" );
    self setvehgoalpos( self.origin + ( 0, 0, 100 ), 1 );
    scripts\mp\hostmigration::waitlongdurationwithhostmigrationpause( 1.5 );
    self setyawspeed( var_0, var_0, var_0 );
    self settargetyaw( self.angles[1] + var_0 * 2.5 );
}

handledestroydamage()
{
    self endon( "death" );
    self endon( "leaving" );
    self endon( "swapped" );

    for (;;)
    {
        self waittill( "damage", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12, var_13 );
        var_9 = scripts\mp\utility\weapon::mapweapon( var_9, var_13 );

        if ( ( var_9.basename == "aamissile_projectile_mp" || var_9.basename == "nuke_mp" ) && var_4 == "MOD_EXPLOSIVE" && var_0 >= self.health )
        {
            callback_vehicledamage( var_1, var_1, 9001, 0, var_4, var_9, var_3, var_2, var_3, 0, 0, var_7 );
            helicleanupextract( 1 );
        }
    }
}

callback_vehicledamage( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12 )
{
    if ( isdefined( var_1 ) )
    {
        if ( isdefined( var_1.owner ) )
            var_1 = var_1.owner;
    }

    if ( ( var_1 == self || isdefined( var_1.pers ) && var_1.pers["team"] == self.team && !level.friendlyfire && level.teambased ) && var_1 != self.owner )
        return;

    if ( self.health <= 0 )
        return;

    var_2 = scripts\mp\utility\killstreak::getmodifiedantikillstreakdamage( var_1, var_5, var_4, var_2, self.maxhealth, 3, 4, 5 );
    scripts\mp\killstreaks\killstreaks::killstreakhit( var_1, var_5, self, var_4, var_2 );
    var_1 scripts\mp\damagefeedback::updatedamagefeedback( "" );

    if ( self.health - var_2 <= 900 && ( !isdefined( self.smoking ) || !self.smoking ) )
        self.smoking = 1;

    self vehicle_finishdamage( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11 );
}

minigun_is_firing()
{
    self endon( "death" );
    self notify( "leaving" );
    self.leaving = 1;
    self setvehgoalpos( self.pathgoal, 1 );
    self settargetyaw( self.little_bird_mg_reenter );
    minigunbackup( 3, self.implement_cointoss );
    self waittill( "goal" );
    self vehicle_setspeed( self.speed, self.accel );
    self setvehgoalpos( self.spawn_bomb_hostage, 1 );
    self settargetyaw( self.little_bird_mg_reenter );
    self waittill( "goal" );
    self stoploopsound();
    mine_caves_chopper();
    minigunbackup( 0, 0 );
    self notify( "heli_gone" );
    mine_caves_turret_2_support();
}

helidescend( var_0, var_1 )
{
    self endon( "death" );
    var_2 = var_0[0];
    var_3 = var_0[1];
    var_4 = ( var_2, var_3, var_1 );
    self setvehgoalpos( var_4, 1 );
    self settargetyaw( self.little_bird_mg_reenter );
    self vehicle_setspeed( 25.0, 31.25 );
    thread mine_caves_turret_clip_remove_on_death();
    thread mine_caves_turret_config();
    self waittill( "goal" );
    self sethoverparams( 1, 1 );
    wait 1;
    self sethoverparams( 25, 20, 10 );
}

handle_just_keep_moving()
{
    return isalive( self ) && ( scripts\common\vehicle::isvehicle() || isdefined( self.classname ) && self.classname == "script_vehicle" );
}

mine_caves_turret_clip_remove_on_death()
{
    self endon( "leaving" );
    self endon( "death" );

    for (;;)
    {
        self waittill( "touch", var_0 );

        if ( isdefined( var_0 ) && var_0 handle_just_keep_moving() )
            var_0 dodamage( var_0.health, self.origin, var_0, var_0, "MOD_CRUSH" );
    }
}

mine_caves_turret_config()
{
    self endon( "leaving" );
    self endon( "death" );
    var_0 = 70;
    var_1 = -80;
    var_2 = 150;
    var_3 = 25;
    var_4 = -100;

    for (;;)
    {
        var_5 = getentarrayinradius( "script_vehicle", "classname", self.origin, getdvarfloat( "test_radius", 400 ) );

        if ( var_5.size <= 1 )
        {
            wait 0.5;
            continue;
        }

        var_6 = scripts\engine\trace::create_vehicle_contents();
        var_7 = anglestoforward( self.angles );
        var_8 = self.origin + var_7 * getdvarfloat( "test_f", var_2 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 handle_just_keep_moving() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        var_8 = self.origin + var_7 * getdvarfloat( "test_m", var_3 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 handle_just_keep_moving() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        var_8 = self.origin + var_7 * getdvarfloat( "test_b", var_4 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 handle_just_keep_moving() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        waitframe();
    }
}

tracegroundheight( var_0 )
{
    var_1 = 125;
    var_2 = tracegroundpoint( var_0, 100, [ self ] );
    var_3 = var_2[2];
    var_4 = var_3 + var_1;
    return var_4;
}

tracegroundpoint( var_0, var_1, var_2 )
{
    var_3 = -99999;
    var_4 = ( var_0[0], var_0[1], var_3 );
    var_5 = scripts\engine\trace::create_world_contents();
    var_6 = undefined;

    if ( isdefined( var_1 ) )
        var_6 = scripts\engine\trace::sphere_trace( var_0, var_4, var_1, var_2, var_5 );
    else
        var_6 = scripts\engine\trace::ray_trace( var_0, var_4, var_2, var_5 );

    return var_6["position"];
}

heliwatchgameendleave()
{
    self endon( "death" );
    self endon( "leaving" );
    level waittill( "game_ended" );
    thread minigun_is_firing();
}

onteamleadgained()
{
    var_0 = [];

    if ( scripts\cp_mp\utility\game_utility::play_players_arrive_at_exfil() )
    {
        var_0[var_0.size] = ( -33750, -36000, 155 );
        var_0[var_0.size] = ( -41550, -7950, 515 );
        var_0[var_0.size] = ( -37500, 15350, 1130 );
        var_0[var_0.size] = ( -31950, 52015, 2265 );
        var_0[var_0.size] = ( -18510, 64815, 1940 );
        var_0[var_0.size] = ( 15565, 60050, 2680 );
        var_0[var_0.size] = ( 44400, 39255, -50 );
        var_0[var_0.size] = ( 59780, 13800, 555 );
        var_0[var_0.size] = ( 61200, -8445, 30 );
        var_0[var_0.size] = ( 59325, -38390, -210 );
        var_0[var_0.size] = ( 8660, -36630, -640 );
    }
    else
    {
        switch ( level.mapname )
        {
            case "mp_br_mechanics":
                var_0[var_0.size] = ( 1500, 1500, 0 );
                var_0[var_0.size] = ( 2500, 1500, 0 );
                var_0[var_0.size] = ( 3500, 1500, 0 );
                var_0[var_0.size] = ( 4500, 1500, 0 );
                break;
            case "mp_mb_tut":
                break;
        }
    }

    level.heli_screenshake = var_0;
    level.heli_landing_volumes = [];
    level thread sat_wait_for_signal_transfer();
}

end_origin( var_0 )
{

}

sat_wait_for_signal_transfer()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    foreach ( var_1 in level.heli_screenshake )
    {
        var_2 = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );

        if ( var_2 != -1 )
        {
            scripts\mp\objidpoolmanager::objective_add_objective( var_2, "active", var_1, "icon_waypoint_flag" );
            scripts\mp\objidpoolmanager::update_objective_setbackground( var_2, 0 );
            scripts\mp\objidpoolmanager::objective_playermask_hidefromall( var_2 );
            scripts\mp\objidpoolmanager::objective_playermask_showtoall( var_2 );
        }

        level thread _id_11E16( var_1 );
    }

    for (;;)
    {
        foreach ( var_1 in level.heli_screenshake )
        {
            var_5 = scripts\mp\utility\player::getplayersinradius( var_1, 300 );

            foreach ( var_7 in var_5 )
            {
                if ( !scripts\engine\utility::array_contains( level.heli_landing_volumes, var_7 ) && !istrue( var_7.heli_rpg_enemy_run_away ) )
                    var_7 heli_audio( var_1 );
            }
        }

        foreach ( var_7 in level.heli_landing_volumes )
        {
            if ( distancesquared( var_7.origin, var_7.heli_rpg_enemy_think ) > 90000 )
            {
                var_7 heli_assault3_fob_death_watcher();
                continue;
            }

            var_7.heli_starts_addstart = var_7.heli_starts_addstart - level.framedurationseconds;

            if ( var_7.heli_starts_addstart <= 0.0 )
                var_7 heli_assault3_hangar_death_watcher();
        }

        waitframe();
    }
}

_id_11E16( var_0 )
{
    var_1 = scripts\engine\utility::drop_to_ground( var_0, 50, -200, ( 0, 0, 1 ) );
    var_2 = spawn( "script_model", var_1 + ( 0, 0, 3 ) );
    var_2 setmodel( "scr_smoke_grenade" );
    wait 1.0;
    playfxontag( scripts\engine\utility::getfx( "vfx_smk_signal_red" ), var_2, "tag_fx" );
    var_2 playloopsound( "mp_flare_burn_lp" );
}

heli_audio( var_0 )
{
    self iprintlnbold( "Extraction Start!" );
    level.heli_landing_volumes = scripts\engine\utility::array_add( level.heli_landing_volumes, self );
    self.heli_rpg_enemy_think = var_0;
    self.heli_starts_addstart = 10;
    thread _id_11C0A();
}

_id_11C0A()
{
    self endon( "death_or_disconnect" );
    self endon( "extactionCancel" );

    while ( self.heli_starts_addstart > 0 )
    {
        self iprintlnbold( "Extracting in " + scripts\engine\math::round_float( self.heli_starts_addstart, 1 ) );
        waitframe();
    }
}

heli_assault3_fob_death_watcher()
{
    self notify( "extactionCancel" );
    self iprintlnbold( "Extraction Canceled!" );
    level.heli_landing_volumes = scripts\engine\utility::array_remove( level.heli_landing_volumes, self );
    self.heli_rpg_enemy_think = undefined;
    self.heli_starts_addstart = undefined;
}

heli_assault3_hangar_death_watcher()
{
    self iprintlnbold( "Extraction Complete!" );
    level.heli_landing_volumes = scripts\engine\utility::array_remove( level.heli_landing_volumes, self );
    self.heli_rpg_enemy_run_away = 1;
    kick( self getentitynumber(), "EXE/PLAYERKICKED_EXTRACTED" );
}

bot_get_human_picked_class( var_0 )
{
    if ( !isdefined( level.questinfo.teamsonquests ) || scripts\engine\utility::array_contains( level.questinfo.teamsonquests, var_0.team ) )
        return;

    var_1 = [];

    foreach ( var_3 in level._id_1268E )
        var_1 = scripts\engine\utility::array_combine( var_1, getlootscriptablearray( "brloot_" + var_3 + "_tablet" ) );

    var_1 = scripts\engine\utility::array_randomize( var_1 );
    var_5 = undefined;
    var_6 = undefined;

    foreach ( var_10, var_8 in var_1 )
    {
        var_9 = scripts\engine\utility::distance_2d_squared( var_0.origin, var_8.origin );

        if ( var_9 <= 16777216 && var_9 >= 16384 )
        {
            var_0 scripts\mp\gametypes\br_quest_util.gsc::_id_1213C( var_8 );
            return;
        }

        if ( !isdefined( var_5 ) || var_9 < var_6 )
        {
            var_5 = var_8;
            var_6 = var_9;
        }
    }

    if ( isdefined( var_5 ) )
        var_0 scripts\mp\gametypes\br_quest_util.gsc::_id_1213C( var_5 );
}

rooftop_objective( var_0 )
{
    var_1 = [];

    foreach ( var_4, var_3 in var_0 )
    {
        if ( var_4 == "circle_peek" )
            continue;

        var_1[var_4] = var_3;
    }

    return var_1;
}

make_focus_fire_headicon( var_0 )
{
    var_1 = [];

    if ( isdefined( var_0 ) && var_0 != "tie" )
        var_1 = scripts\mp\utility\teams::getteamdata( var_0, "players" );

    var_2 = scripts\mp\gamelogic::jugg_modifyherodroptoplayerdamage();

    foreach ( var_4 in level.players )
    {
        if ( var_1.size > 0 && scripts\engine\utility::array_contains( var_1, var_4 ) )
        {
            var_4 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "post_game_state", var_2 );
            var_4 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_end_game_splash_type", 11 );
            continue;
        }

        var_4 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "post_game_state", var_2 );
        var_4 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "ui_br_end_game_splash_type", 12 );
    }
}

_id_12601( var_0, var_1, var_2 )
{
    var_3 = scripts\mp\gamescore::_getteamscore( var_1 );
    var_4 = var_0 - var_3;

    if ( var_4 != 0 )
    {
        var_5 = scripts\engine\utility::ter_op( scripts\mp\gametypes\br_public.gsc::play_player_approach(), 1, undefined );
        level thread scripts\mp\gamescore::giveteamscoreforobjective( var_1, var_4, 0, undefined, var_5, var_2 );
    }

    return var_4;
}

_id_125DB()
{
    level notify( "restartLootLeaders" );
    level endon( "restartLootLeaders" );
    level endon( "game_ended" );
    var_0 = getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 );
    var_1 = getdvarfloat( "scr_dmz_loot_leader_update_interval_blink", 5.0 );
    var_2 = getdvarint( "scr_bmo_circle_pulse_start", 800 );
    var_3 = getdvarint( "scr_bmo_circle_pulse_end", 200 );
    var_4 = var_0 - var_1;
    scripts\mp\flags::gameflagwait( "placement_updates_allowed" );

    for (;;)
    {
        level.trackriotshield_grenadepullbackforc4 = 1;
        scripts\engine\utility::waittill_notify_or_timeout( "bmo_overtime_start", var_4 );

        if ( var_1 > 0 )
        {
            foreach ( var_6 in level.register_vehicle_spawn_override )
                var_6.mapcircle setmapcirclestyleindex( 2 );

            scripts\engine\utility::waittill_notify_or_timeout( "bmo_overtime_start", var_1 );

            foreach ( var_6 in level.register_vehicle_spawn_override )
                var_6.mapcircle setmapcirclestyleindex( 0 );
        }
    }
}

_id_125C3( var_0, var_1, var_2 )
{
    var_3 = gettime();
    var_2 = var_2 * 1000;
    var_4 = int( var_3 + var_2 );
    var_5 = abs( var_0 - var_1 );

    for (;;)
    {
        var_3 = gettime();
        var_6 = clamp( 1.0 - ( var_4 - var_3 ) / var_2, 0.0, 1.0 );
        var_7 = scripts\engine\utility::ter_op( var_0 < var_1, var_5 * var_6 + var_0, var_0 - var_5 * var_6 );
        setdvar( "PPRTMPMQM", var_7 );

        if ( var_6 == 1.0 )
            break;

        waitframe();
    }
}

_id_125DA( var_0, var_1, var_2, var_3 )
{
    level.registerchallenge = level.registercarryobjectpickupcheck;
    level.registercarryobjectpickupcheck = [];

    if ( level.registerbrsquadleaderjumpcommands )
    {
        if ( istrue( level.brplacementchallengeallowed ) )
        {
            level.register_vehicle_as_ambient = ammo_crates();

            foreach ( var_5 in level.register_vehicle_spawn_override )
                var_5.mapcircle hide();
        }
    }

    if ( level.register_wave_spawner == 1 )
    {
        for ( var_7 = 0; var_7 < level.register_vehicle_as_ambient; var_7++ )
        {
            var_8 = knock_player_forward( var_7, var_0, var_3 );

            if ( isdefined( var_8 ) )
            {
                level.registercarryobjectpickupcheck[level.registercarryobjectpickupcheck.size] = var_2[var_8];

                if ( istrue( level.health_bar_player_connect_monitor ) )
                {
                    if ( isdefined( var_2[var_8].health_reduction ) )
                        vehicle_playerenteredtrackedlittlebird( var_2[var_8].health_reduction );
                }
            }
        }
    }
    else if ( level.register_wave_spawner == 2 )
    {
        var_8 = knock_player_forward( 0, var_0, var_3 );

        if ( isdefined( var_8 ) )
        {
            level.registercarryobjectpickupcheck[0] = var_2[var_8];

            if ( istrue( level.health_bar_player_connect_monitor ) )
            {
                if ( isdefined( var_2[var_8].health_reduction ) )
                    vehicle_playerenteredtrackedlittlebird( var_2[var_8].health_reduction );
            }
        }

        foreach ( var_10 in var_1 )
        {
            var_11 = var_2[var_10];
            var_12 = var_0[var_10];

            if ( var_12 == 0 )
                break;

            if ( scripts\engine\utility::array_contains( level.registercarryobjectpickupcheck, var_11 ) )
                continue;

            level.registercarryobjectpickupcheck[level.registercarryobjectpickupcheck.size] = var_11;

            if ( istrue( level.health_bar_player_connect_monitor ) )
            {
                if ( isdefined( var_11.health_reduction ) )
                    vehicle_playerenteredtrackedlittlebird( var_11.health_reduction );
            }

            if ( level.registercarryobjectpickupcheck.size == level.register_vehicle_as_ambient )
                break;
        }
    }
    else
    {
        foreach ( var_11 in var_1 )
        {
            var_12 = var_0[var_11];

            if ( var_12 == 0 )
                break;

            level.registercarryobjectpickupcheck[level.registercarryobjectpickupcheck.size] = var_2[var_11];

            if ( istrue( level.health_bar_player_connect_monitor ) )
            {
                if ( isdefined( var_2[var_11].health_reduction ) )
                    vehicle_playerenteredtrackedlittlebird( var_2[var_11].health_reduction );
            }

            if ( level.registercarryobjectpickupcheck.size == level.register_vehicle_as_ambient )
                break;
        }
    }

    foreach ( var_11 in level.registerchallenge )
    {
        if ( isdefined( var_11 ) && !scripts\engine\utility::array_contains( level.registercarryobjectpickupcheck, var_11 ) )
            vehicle_playerkilledfx( var_11 );
    }

    for ( var_7 = 0; var_7 < level.register_vehicle_as_ambient; var_7++ )
    {
        var_18 = level.register_vehicle_spawn_override[var_7];

        if ( isdefined( level.registercarryobjectpickupcheck[var_7] ) )
        {
            var_18.targetplayer = level.registercarryobjectpickupcheck[var_7];
            var_18.targetplayer.register_valid_objectives_for_create_script = var_18;
            var_19 = ( var_18.targetplayer.origin[0], var_18.targetplayer.origin[1], level.register_vehicle_spawners );

            if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
                var_19 = var_19 + scripts\engine\math::random_vector_2d() * randomfloatrange( 100, 1000 );

            var_18 scripts\mp\gametypes\br_quest_util.gsc::sec_sys_struct_2( var_19 );

            if ( istrue( level.register_vfx ) )
                _id_125D9( var_18 );

            var_20 = scripts\engine\utility::array_contains( level.registercarryobjectpickupcheck, var_18.targetplayer ) && !scripts\engine\utility::array_contains( level.registerchallenge, var_18.targetplayer );
            var_21 = scripts\mp\utility\teams::getenemyplayers( var_18.targetplayer.team, 0 );

            foreach ( var_11 in var_21 )
                var_18 scripts\mp\gametypes\br_quest_util.gsc::_id_11C1A( var_11 );

            var_24 = scripts\mp\utility\teams::getfriendlyplayers( var_18.targetplayer.team, 0 );

            foreach ( var_11 in var_24 )
                var_18 scripts\mp\gametypes\br_quest_util.gsc::modetype( var_11 );

            if ( var_20 )
                allowmeleevehicledamage( var_18 );

            continue;
        }

        var_18.targetplayer = undefined;

        foreach ( var_11 in level.players )
            var_18 scripts\mp\gametypes\br_quest_util.gsc::modetype( var_11 );
    }
}

knock_player_forward( var_0, var_1, var_2 )
{
    var_3 = var_2[var_0];
    var_4 = 0;
    var_5 = 0;

    if ( level.teamdata[var_3]["players"].size == 0 )
        return undefined;

    var_6 = level.teamdata[var_3]["players"][0].guid;

    foreach ( var_8 in level.teamdata[var_3]["players"] )
    {
        var_4 = var_1[var_8.guid];

        if ( var_4 > var_5 )
        {
            var_5 = var_4;
            var_6 = var_8.guid;
        }
    }

    return var_6;
}

allowmeleevehicledamage( var_0 )
{
    var_1 = var_0.targetplayer;

    if ( scripts\mp\utility\player::isreallyalive( var_1 ) )
    {
        if ( !scripts\mp\gametypes\br_public.gsc::play_player_approach() )
            var_1 scripts\mp\hud_message::showsplash( "bm_player_marked" );

        var_1 currentsolsign();

        if ( istrue( level.health_bar_player_connect_monitor ) )
            var_1 thread watchforplayerdeath( var_0 );
    }
}

vehicle_playerkilledfx( var_0 )
{
    if ( isdefined( var_0.bonusdeathplunderot ) )
        var_0 currenttime();

    if ( isdefined( var_0.register_valid_objectives_for_create_script ) )
    {
        foreach ( var_2 in level.players )
            var_0.register_valid_objectives_for_create_script scripts\mp\gametypes\br_quest_util.gsc::modetype( var_2 );

        var_4 = var_0.register_valid_objectives_for_create_script.delete_race;

        if ( istrue( level.health_bar_player_connect_monitor ) )
            allowleaderboardstatsupdates( var_0, var_4 );

        var_0.register_valid_objectives_for_create_script.targetplayer = undefined;
        var_0.register_valid_objectives_for_create_script = undefined;
    }

    level.registercarryobjectpickupcheck = scripts\engine\utility::array_remove( level.registercarryobjectpickupcheck, var_0 );
}

watchforplayerdeath( var_0 )
{
    self endon( "disconnect" );
    self waittill( "death" );
    var_1 = self.register_valid_objectives_for_create_script.delete_race;
    vehicle_playerkilledfx( self );
    allowleaderboardstatsupdates( self, var_1 );
}

allowleaderboardstatsupdates( var_0, var_1 )
{
    if ( scripts\engine\utility::array_contains( level.healthpack_health, var_0 ) )
        return;

    level.healthpack_health = scripts\engine\utility::array_add( level.healthpack_health, var_0 );
    var_2 = undefined;

    foreach ( var_4 in level.health_remaining_max )
    {
        if ( !isdefined( var_4.targetplayer ) )
        {
            var_2 = var_4;
            break;
        }
    }

    if ( !isdefined( var_2 ) )
    {
        var_6 = undefined;

        foreach ( var_4 in level.health_remaining_max )
        {
            if ( !isdefined( var_6 ) || var_4.lastusedtime < var_6 )
            {
                var_6 = var_4.lastusedtime;
                var_2 = var_4;
            }
        }

        var_2.targetplayer notify( "stop_update" );
    }

    var_2.targetplayer = var_0;
    var_2.lastusedtime = gettime();
    var_0.health_reduction = var_2;

    if ( isdefined( var_1 ) )
        var_2 scripts\mp\gametypes\br_quest_util.gsc::sec_sys_struct_2( var_1 );
    else
    {
        var_9 = ( var_2.targetplayer.origin[0], var_2.targetplayer.origin[1], level.register_vehicle_spawners );
        var_9 = var_9 + scripts\engine\math::random_vector_2d() * randomfloatrange( 100, 1000 );
        var_2 scripts\mp\gametypes\br_quest_util.gsc::sec_sys_struct_2( var_9 );
    }

    if ( istrue( level.register_vfx ) )
        _id_125D9( var_2 );

    var_10 = scripts\mp\utility\teams::getenemyplayers( var_2.targetplayer.team, 0 );

    foreach ( var_4 in var_10 )
        var_2 scripts\mp\gametypes\br_quest_util.gsc::_id_11C1A( var_4 );

    var_13 = scripts\mp\utility\teams::getfriendlyplayers( var_2.targetplayer.team, 0 );

    foreach ( var_4 in var_13 )
        var_2 scripts\mp\gametypes\br_quest_util.gsc::modetype( var_4 );

    thread _id_125CA( var_2 );
}

_id_125CA( var_0 )
{
    var_0.targetplayer endon( "stop_update" );
    wait 5.0;
    vehicle_playerenteredtrackedlittlebird( var_0 );
}

vehicle_playerenteredtrackedlittlebird( var_0 )
{
    var_1 = var_0.targetplayer;
    level.healthpack_health = scripts\engine\utility::array_remove( level.healthpack_health, var_0.targetplayer );
    var_0.targetplayer.health_reduction = undefined;
    var_0.targetplayer = undefined;

    foreach ( var_3 in level.players )
        var_0 scripts\mp\gametypes\br_quest_util.gsc::modetype( var_3 );

    var_1 notify( "stop_update" );
}

currentsolsign()
{
    self attach( "accessory_money_bag_large_closed_player", "tag_stowed_back3", 1, 1 );
    self.bonusdeathplunderot = "accessory_money_bag_large_closed_player";

    if ( self tagexists( "j_bag_left" ) )
        playfxontag( level._effect["vfx_br_cashLeaderBag"], self, "j_bag_left" );

    thread currentsol();
}

currenttime()
{
    if ( isdefined( self ) && isdefined( self.bonusdeathplunderot ) )
    {
        if ( self tagexists( "j_bag_left" ) )
            stopfxontag( level._effect["vfx_br_cashLeaderBag"], self, "j_bag_left" );

        self detach( self.bonusdeathplunderot, "tag_stowed_back3" );
        self.bonusdeathplunderot = undefined;
    }

    self notify( "killthread_bagModelSwap" );
}

currentsol()
{
    self notify( "cashleader_trackDeath" );
    self endon( "cashleader_trackDeath" );
    self endon( "killthread_bagModelSwap" );
    self waittill( "death" );
    currenttime();
}

so_forceendgame( var_0 )
{
    if ( scripts\mp\utility\game::lb_mg_impulse_dmg_threshold_top() == "kingslayer" )
        return;

    var_1 = 0;
    var_2 = 0;

    if ( isdefined( self.plundercount ) )
        var_2 = var_2 + self.plundercount;

    if ( isdefined( self.plunderbanked ) )
        var_2 = var_2 + self.plunderbanked;

    var_2 = int( var_2 / 10 );

    if ( var_2 > 4095 )
        var_2 = 4095;

    var_1 = var_2;
    var_3 = 0;

    if ( isdefined( self.rooftop_crate_usefunc ) )
        var_3 = var_3 + self.rooftop_crate_usefunc;

    if ( var_3 > 15 )
        var_3 = 15;

    var_1 = var_1 + ( var_3 << 12 );
    scripts\mp\utility\stats::setextrascore0( var_1 );
}

so_displaygameend()
{
    var_0 = 0;
    var_1 = 0;

    if ( isdefined( self.plundercount ) )
        var_1 = var_1 + self.plundercount;

    if ( isdefined( self.plunderbanked ) )
        var_1 = var_1 + self.plunderbanked;

    var_1 = int( var_1 / 10 );

    if ( var_1 > 4095 )
        var_1 = 4095;

    var_0 = var_1;
    var_2 = 0;

    if ( isdefined( self.rooftop_crate_usefunc ) )
        var_2 = var_2 + self.rooftop_crate_usefunc;

    if ( var_2 > 15 )
        var_2 = 15;

    var_0 = var_0 + ( var_2 << 12 );
    return var_0;
}

table_getaddblueprintattachments()
{
    self endon( "disconnect" );

    if ( isdefined( level.registercarryobjectpickupcheck ) && scripts\engine\utility::array_contains( level.registercarryobjectpickupcheck, self ) )
    {
        scripts\mp\hud_message::showsplash( "bm_player_marked" );
        currentsolsign();
    }
}

_id_125D9( var_0 )
{
    if ( isdefined( var_0.targetplayer.plundercount ) )
        var_1 = var_0.targetplayer.plundercount;
    else
        var_1 = 0;

    var_1 = clamp( var_1, level.registerbrgametypefunc, level.registerarmsracevfx );
    var_2 = level.registerarmsracevfx - level.registerbrgametypefunc;
    var_3 = ( var_1 - level.registerbrgametypefunc ) / var_2;
    var_4 = level.registerbrgametypedata - level.registeraccesscardlocs;
    var_5 = level.registerbrgametypedata - var_3 * var_4;
    var_0 scripts\mp\gametypes\br_quest_util.gsc::_id_11A9B( var_5 );
}

ammo_crates( var_0 )
{
    var_1 = 0;
    var_0 = scripts\mp\gamescore::lbravo_spawner_jammer3b();
    var_2 = level_light();

    for ( var_3 = 1; var_3 < level.removeplayeraslootleader + 1; var_3++ )
    {
        foreach ( var_5 in level.teamnamelist )
        {
            if ( level scripts\mp\utility\game::playerhumanconcusspushcleanup( var_5 ) )
                continue;

            if ( var_0[var_5] != var_3 )
                continue;

            var_6 = lbravo_spawner_jammer2b( var_5 ) * 100;

            if ( var_6 >= var_2 || var_0[var_5] == 1 )
                var_1++;
        }
    }

    if ( var_1 > level.removeplayeraslootleader )
        var_1 = level.removeplayeraslootleader;

    return var_1;
}

get_most_recent_danger_ping( var_0, var_1 )
{
    var_2 = 0;
    var_3 = 0;
    var_4 = 20;
    var_1 = var_1 * 100;
    var_5 = var_0.plundercount * 100;
    var_6 = var_5 + var_1;
    var_7 = var_6;
    var_8 = var_5;
    var_9 = var_8 - var_6;
    var_10 = scripts\engine\utility::sign( var_9 );
    var_11 = var_9 / 2.0;
    var_12 = int( var_11 * 2 * level.framedurationseconds );

    if ( var_12 == 0 )
        return;

    var_13 = end_silo_thrust( &"MP_BR_INGAME/PLUNDER_DEATH_LOSS", var_1, "RIGHT", "CENTER", var_2 + 46, var_3, undefined, undefined, 1.25, var_0 );
    var_13.alpha = 1;
    var_14 = end_silo_thrust( &"MP_BR_INGAME/YOUR_PLUNDER_TEXT", undefined, "RIGHT", "CENTER", var_2, var_3 + var_4, undefined, undefined, 1.25, var_0 );
    var_14.alpha = 1;
    var_15 = end_silo_thrust( &"MP_BR_INGAME/EXTRACT_PLUNDER", var_6, "LEFT", "CENTER", var_2 + 45, var_3 + var_4, undefined, undefined, 1.25, var_0 );
    var_15.alpha = 1;
    wait 1.0;

    while ( var_7 != var_8 )
    {
        var_7 = var_7 + var_12;

        if ( var_10 > 0 && var_7 > var_8 || var_10 < 0 && var_7 < var_8 )
            var_7 = var_8;

        var_15 setvalue( var_7 );
        wait( level.framedurationseconds );
    }

    wait 3.0;
    var_13 destroy();
    var_14 destroy();
    var_15 destroy();
}

activate_track_timers( var_0, var_1, var_2 )
{
    if ( var_0.size == 0 || var_1 == 0 )
        return;

    var_3 = [];
    var_4 = [];

    if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );
        var_4 = int( min( var_1, var_0.size ) );
    }

    if ( level._id_11B34 )
    {
        var_5 = 0;

        if ( level.ammo_crate_use > 0 && istrue( var_2 ) )
        {
            var_6 = 0;

            for ( var_7 = 0; var_7 < var_4; var_7++ )
            {
                if ( var_6 > level.so_postmatchfx.size - 1 )
                    var_6 = 0;

                var_8 = level.so_postmatchfx[var_6];

                foreach ( var_10 in var_0 )
                {
                    if ( tracks( var_10.origin, var_8, level.trackserverend ) )
                    {
                        var_3[var_3.size] = var_10;
                        var_0 = scripts\engine\utility::array_remove( var_0, var_10 );
                        break;
                    }
                }

                var_6++;
            }
        }
        else
        {
            foreach ( var_10 in var_0 )
            {
                var_5 = 0;

                foreach ( var_14 in level.turn_off_silo_lights )
                {
                    var_15 = scripts\engine\utility::distance_2d_squared( var_14, var_10.origin );

                    if ( var_5 == 0 || var_15 < var_5 )
                    {
                        var_5 = var_15;
                        var_10.disableonemilannounce = var_14;
                    }
                }
            }

            var_6 = 0;

            for ( var_7 = 0; var_7 < var_4; var_7++ )
            {
                if ( var_6 > level.turn_off_silo_lights.size - 1 )
                    var_6 = 0;

                var_8 = level.turn_off_silo_lights[var_6];

                foreach ( var_10 in var_0 )
                {
                    if ( isdefined( var_10.disableonemilannounce ) && var_10.disableonemilannounce == var_8 )
                    {
                        var_3[var_3.size] = var_10;
                        var_0 = scripts\engine\utility::array_remove( var_0, var_10 );
                        break;
                    }
                }

                var_6++;
            }
        }
    }
    else if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );
        var_4 = int( min( var_1, var_0.size ) );

        for ( var_7 = 0; var_7 < var_4; var_7++ )
            var_3[var_3.size] = var_0[var_7];
    }

    if ( istrue( level.mode_can_play_ending ) )
    {
        foreach ( var_21 in var_0 )
        {
            if ( !scripts\engine\utility::array_contains( var_3, var_21 ) )
                var_21 setscriptablepartstate( var_21.type, "hidden" );
        }
    }

    return var_3;
}

tracks( var_0, var_1, var_2 )
{
    var_3 = var_1[0] - var_2;
    var_4 = var_1[0] + var_2;
    var_5 = var_1[1] - var_2;
    var_6 = var_1[1] + var_2;
    return var_0[0] >= var_3 && var_0[0] <= var_4 && var_0[1] >= var_5 && var_0[1] <= var_6;
}

activate_upload_station( var_0, var_1 )
{
    if ( var_0.size == 0 || var_1 == 0 )
        return;

    var_2 = undefined;

    if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );

        for ( var_3 = 0; var_3 < var_0.size; var_3++ )
        {
            if ( !scripts\engine\utility::array_contains( level.br_plunder_sites, var_0[var_3] ) )
            {
                var_2 = var_0[var_3];
                break;
            }
        }
    }

    return var_2;
}

hostdamagepercentlow( var_0 )
{
    var_1 = activate_upload_station( scripts\mp\gametypes\br_plunder.gsc::iw8_ship_hack_add_flag_node(), level.repair_grill_fixing_long_sfx );
    var_1.disabled = undefined;
    var_1.mine_caves_turret_damage_feedback = undefined;
    var_2 = scripts\engine\utility::ter_op( level._id_11C18 && !level._id_11C13, var_1.airlock_front_blocker, var_1.airlock_show_room_doors );
    var_1 setscriptablepartstate( var_1.type, var_2 );

    if ( level._id_11C18 && level._id_11C13 )
    {
        scripts\mp\objidpoolmanager::returnobjectiveid( var_0.locale.objectiveiconid );
        var_0.locale.objectiveiconid = -1;
        var_3 = spawnstruct();
        var_3 ending_fade_out( "ui_mp_br_mapmenu_icon_atm", "current", var_1.origin + ( 0, 0, 200 ) );
        var_1.locale = var_3;
    }

    level.br_plunder_sites = scripts\engine\utility::array_remove( level.br_plunder_sites, var_0 );
    level.br_plunder_sites = scripts\engine\utility::array_add( level.br_plunder_sites, var_1 );
}

_id_11FCC()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );
    waitframe();

    foreach ( var_1 in level.br_plunder_sites )
    {
        var_1.disabled = undefined;
        var_2 = scripts\engine\utility::ter_op( level._id_11C18 && !level._id_11C13, var_1.airlock_front_blocker, var_1.airlock_show_room_doors );
        var_1 setscriptablepartstate( var_1.type, var_2 );
        var_1 thread watchbombuse();

        if ( level._id_11C18 && level._id_11C13 )
            var_1.locale thread _id_12244();
    }

    level thread watch_for_players_joining();
}

watchbombuse()
{
    wait( level._id_12243 );
    self.disabled = 1;
    self.mine_caves_turret_damage_feedback = 1;
    self setscriptablepartstate( self.type, self.get_dropoff_point_spawner );
}

watch_for_players_joining()
{
    wait( level._id_12243 + 1 );
    level.br_plunder_sites = activate_track_timers( scripts\mp\gametypes\br_plunder.gsc::iw8_ship_hack_add_flag_node(), level.repair_grill_fixing_long_sfx );

    if ( level._id_11C18 && level._id_11C13 )
        level thread end_silo_elevator();

    level thread _id_11FCC();
}

end_silo_elevator()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    foreach ( var_1 in level.br_plunder_sites )
    {
        var_2 = spawnstruct();
        var_2 ending_fade_out( "ui_mp_br_mapmenu_icon_atm", "current", var_1.origin + ( 0, 0, 200 ) );
        var_1.locale = var_2;
    }
}

_id_12244()
{
    var_0 = level._id_12243;
    var_1 = level.framedurationseconds;
    var_2 = var_1 * 1000;
    var_3 = var_0 * 1000;
    var_4 = var_3 - var_2;
    var_5 = gettime() + var_3;

    while ( gettime() < var_5 )
    {
        var_6 = var_4 / var_3;
        scripts\mp\objidpoolmanager::objective_show_progress( self.objectiveiconid, 1 );
        scripts\mp\objidpoolmanager::objective_set_progress( self.objectiveiconid, var_6 );
        var_4 = max( var_4 - var_2, 1 );
        waitframe();
    }

    scripts\mp\objidpoolmanager::returnobjectiveid( self.objectiveiconid );
    self.objectiveiconid = -1;
}

ending_fade_out( var_0, var_1, var_2 )
{
    self.objectiveiconid = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );

    if ( self.objectiveiconid != -1 )
    {
        scripts\mp\objidpoolmanager::objective_add_objective( self.objectiveiconid, var_1, ( 0, 0, 0 ), var_0 );
        scripts\mp\objidpoolmanager::update_objective_setbackground( self.objectiveiconid, 1 );
        objective_showtoplayersinmask( self.objectiveiconid );
        scripts\mp\objidpoolmanager::objective_set_play_intro( self.objectiveiconid, 1 );

        if ( isdefined( var_2 ) )
            secondaryweaponbackup( var_2 );
    }
    else
    {

    }
}

secondaryweaponbackup( var_0 )
{
    scripts\mp\objidpoolmanager::update_objective_position( self.objectiveiconid, var_0 );
}

_id_11C1C( var_0 )
{
    objective_addclienttomask( self.objectiveiconid, var_0 );
}

_id_11C1B( var_0 )
{
    objective_addalltomask( var_0 );
}

modeusesgroundwarteamoobtriggers( var_0 )
{
    objective_removeclientfrommask( self.objectiveiconid, var_0 );
}

fulton_handlefataldamage()
{
    scripts\mp\objidpoolmanager::objective_playermask_hidefromall( self.objectiveiconid );
    scripts\mp\objidpoolmanager::returnobjectiveid( self.objectiveiconid );
}

_id_11FA9( var_0 )
{
    level _id_11A81();
    level thread _id_11C08( var_0 );

    if ( level._id_121A9 )
        level thread _id_11FDB( var_0 );

    if ( level._id_121A7 )
        level thread vehicle_occupancy_takeriotshield();

    level thread _id_11FB2();
    level thread set_up_coop_push( var_0 );
}

_id_11A81()
{
    scripts\mp\gamelogic::resumetimer();
    level.starttime = gettime();
    level.discardtime = 0;
    level.timerpausetime = 0;
    var_0 = getdvarfloat( "scr_bmo_exfil_timer", 180 );
    var_1 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
    level.watchdvars[var_1].value = var_0;
    level.overridewatchdvars[var_1] = var_0;
}

_id_11C08( var_0 )
{
    foreach ( var_2 in level.teamnamelist )
        scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "plunder_extract_requested", var_2 );

    foreach ( var_5 in level.players )
    {
        var_5 thread scripts\mp\hud_message::showsplash( "callout_bmo_exfil_winners" );
        var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 71, undefined, 20 );
    }
}

_id_11FDB( var_0 )
{
    level.radarmode[var_0] = "normal_radar";
    level.activeuavs[var_0] = 1;
    level.activeadvanceduavs[var_0] = 0;
    scripts\cp_mp\killstreaks\uav::_setteamradarstrength( var_0, 4 );
}

vehicle_occupancy_takeriotshield()
{
    level.register_vehicle_as_ambient = 1;

    if ( getdvarint( "scr_dmz_loot_leader_update_on_pickup", 0 ) == 1 )
    {
        if ( getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 ) > 0.0 )
            level thread _id_125DB();
    }
}

_id_11FB2()
{
    foreach ( var_1 in level.br_plunder_sites )
    {
        var_1 setscriptablepartstate( var_1.type, "inuse" );
        var_2 = getgroundposition( var_1.origin, 1 ) + ( 0, 0, 2 );
        var_3 = level.players[0];

        for ( var_4 = 0; var_4 < 200; var_4++ )
        {
            var_3 = hostage_confirm_good_angles();

            if ( isplayer( var_3 ) )
                break;
        }

        var_2 = getgroundposition( var_1.origin, 1 ) + ( 0, 0, 2 );
        var_5 = var_3 team_revive_kbm_override( var_2, var_1 );

        if ( isdefined( var_5 ) )
        {
            var_5.site = var_1;
            var_1.heli = var_5;
            var_5 helicreateextractvfx( var_2 );
            var_5 thread helicopter_firendly_dmg_text_display();
        }
    }
}

team_revive_kbm_override( var_0, var_1 )
{
    var_2 = var_0;
    var_3 = var_2 + ( 0, 0, 2500 );
    var_4 = hostage_vo( var_3, var_1 );
    var_5 = ( 0, var_4, 0 );

    if ( getdvarint( "scr_br_plunder_heli_adjust_bag", 1 ) == 1 )
    {
        var_6 = -100;
        var_7 = 60;
        var_8 = anglestoforward( var_5 );
        var_9 = anglestoright( var_5 );
        var_2 = var_2 + var_8 * var_6 + var_9 * var_7;
    }

    var_10 = var_3 + -1 * anglestoforward( var_5 ) * 30000;
    var_11 = var_3 + anglestoforward( var_5 ) * 30000;
    var_12 = spawnheli( self, var_10, var_3, var_11 );
    return var_12;
}

helicopter_firendly_dmg_text_display()
{
    self endon( "death" );
    self endon( "leaving" );
    self setvehgoalpos( self.pathgoal, 1 );
    self settargetyaw( self.little_bird_mg_reenter );
    var_0 = _id_122BC( self.pathgoal );
    var_1 = self.pathgoal[2] - var_0;
    self.implement_cointoss = computer_player_listener( var_1 );
    minigunbackup( 1, self.implement_cointoss );
    self waittill( "goal" );

    foreach ( var_3 in level.players )
        var_3 scripts\mp\utility\lower_message::setlowermessageomnvar( 72, undefined, 20 );

    thread heliwatchgameendleave();
    thread mine_caves_turret_op();
    helidescend( self.endpoint, var_0 );

    foreach ( var_6 in level.teamnamelist )
        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "plunder_extract_chopper_arrive", var_6, 1 );

    minigun_model();
    helicleanupextract();

    foreach ( var_6 in level.teamnamelist )
        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "plunder_extract_chopper_leave", var_6, 1 );

    thread minigun_is_firing();
}

minigun_model()
{
    self.isdepot = 1;
    self.usable = self.crate;
    var_0 = self.usable;
    var_0 makeusable();
    var_0 setcursorhint( "HINT_NOICON" );
    var_0 setuseholdduration( "duration_medium" );
    var_0 sethintrequiresholding( 1 );
    var_0 setuserange( 230 );
    var_0 sethintstring( &"MP/BR_USE_EXFIL_CHOPPER" );
    var_1 = level.br_depots.size;
    level.br_depots[var_1] = var_0;

    foreach ( var_3 in level.players )
    {
        if ( !isdefined( var_3 ) )
            continue;
    }

    thread helicleanupdepotonleaving();
    thread mine_launch_vfx( var_0 );
    minigunbackup( 2, 300 );
    wait 300;
    self.isdepot = 0;
    heliusecleanup();
}

mine_launch_vfx( var_0 )
{
    self endon( "death" );
    var_0 endon( "death" );

    for (;;)
    {
        var_0 waittill( "trigger", var_1 );
        var_1 scripts\mp\hud_message::showsplash( "callout_exfil_success" );
        var_1 playerhide();
        var_1 sethidenameplate( 0 );
        var_1 allowmovement( 0 );
        var_1 allowfire( 0 );
        var_1 disableoffhandprimaryweapons( 0 );
        var_1 disableoffhandsecondaryweapons( 0 );
        var_1 disableweapons( 0 );
        var_1 disableweaponswitch( 0 );
        var_1 setcamerathirdperson( 1 );
        var_1 allowcrouch( 0 );
        var_1 allowmelee( 0 );
        var_1 allowjump( 0 );
        var_1 allowprone( 0 );
        var_1 scripts\common\utility::allow_killstreaks( 0 );
        var_1 scripts\common\utility::allow_supers( 0 );
        var_1.watchforentervehicle = 1;
        var_0 disableplayeruse( var_1 );
    }
}

max_dist_sq_from_node()
{
    self endon( "death" );
    self endon( "leaving" );
    self endon( "swapped" );

    for (;;)
    {
        self waittill( "damage", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12, var_13 );
        self.health = 99999;
    }
}

_id_122BC( var_0 )
{
    var_1 = 256;
    var_2 = tracegroundpoint( var_0, 100, [ self ] );
    var_3 = var_2[2];
    var_4 = var_3 + var_1;
    return var_4;
}

hostage_vo( var_0, var_1 )
{
    if ( isdefined( var_1 ) && isdefined( var_1.ignore_player_silencer ) )
        return var_1.ignore_player_silencer;

    var_2 = 10;
    var_3 = scripts\engine\trace::create_world_contents();

    for ( var_4 = 0; var_4 < 360; var_4 = var_4 + var_2 )
    {
        var_5 = ( 0, var_4, 0 );
        var_6 = var_0 + -1 * anglestoforward( var_5 ) * 30000;
        var_7 = var_0 + anglestoforward( var_5 ) * 30000;
        var_8 = scripts\engine\trace::sphere_trace( var_0, var_7, 100, undefined, var_3, 1 );

        if ( var_8["fraction"] == 1 )
        {
            if ( isdefined( var_1 ) )
                var_1.ignore_player_silencer = var_4;

            return var_4;
        }

        if ( var_4 % 3 == 0 )
            waitframe();
    }

    var_4 = randomfloat( 360 );

    if ( isdefined( var_1 ) )
        var_1.ignore_player_silencer = var_4;

    return var_4;
}

set_up_coop_push( var_0 )
{
    level notify( "mercy_ending_timer_started" );
    level endon( "mercy_ending_triggered" );
    setomnvar( "ui_nuke_timer_type", 1 );
    setomnvar( "ui_nuke_timer_time", level._id_121A8 );
    setomnvar( "ui_nuke_countdown_active", 1 );
    var_1 = gettime();
    var_2 = level._id_121A8 * 1000 + var_1;
    setomnvar( "ui_nuke_end_milliseconds", level._id_121A8 * 1000 + var_1 );

    for (;;)
    {
        waitframe();

        if ( gettime() > var_2 )
            break;
    }

    level thread scripts\mp\gamelogic::endgame( var_0, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
    setomnvar( "ui_nuke_countdown_active", 0 );
}

_id_1241E( var_0, var_1, var_2 )
{
    if ( istrue( level.brplacementchallengeallowed ) )
    {
        if ( istrue( level.br_danger_circle_closing_music ) && istrue( var_1 ) )
            level thread brpickupscriptableid();

        return;
    }

    level.brplacementchallengeallowed = 1;
    level notify( "cancel_announcer_dialog" );
    thread scripts\mp\music_and_dialog::totalapacheresponses();
    scripts\mp\gametypes\br_publicevents.gsc::create_oscilloscope();
    level thread smoke_wheelson_chosen_spawn();
    var_3 = scripts\engine\utility::ter_op( level.smoke_wheelson_chosen_spawn >= 1, "bm_overtime_double_cash_num", "bm_overtime_double_cash_perc" );

    if ( isdefined( var_0 ) && ( level.get_grenadedanger_alias || !level.get_explosion_alias ) )
    {
        level.openaltbunker = var_0;
        _id_11C21( var_0, "bm_overtime_start_them" );
        showsplashtoteam( var_0, "bm_overtime_start_us" );
        level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "bm_gamestate_overtime", var_0 );

        foreach ( var_5 in level.teamnamelist )
        {
            if ( var_5 != var_0 )
                level thread scripts\mp\gametypes\br_public.gsc::choosebestpropforkillcam( "bm_gamestate_overtime_enemy", var_5 );
        }
    }
    else
        level.openaltbunker = killentireenemyteam();

    if ( !isdefined( var_2 ) )
    {
        scripts\mp\gamelogic::resumetimer();
        level.starttime = gettime();
        level.discardtime = 0;
        level.timerpausetime = 0;
        var_7 = scripts\engine\utility::ter_op( level.get_explosion_alias || istrue( var_2 ), 7, 12 );
        var_8 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
        level.watchdvars[var_8].value = var_7;
        level.overridewatchdvars[var_8] = var_7;

        if ( !level.get_explosion_alias )
            wait 5;

        _id_11C20( var_3 );
        wait 5;
        level.ontimelimitgraceperiod = level.get_showing_bomb_wire_pair_to_player;
        level.currenttimelimitdelay = 0.0;
        level.canprocessot = 1;
        level notify( "bmo_overtime_start" );
    }
    else
    {
        level.ontimelimitgraceperiod = level.get_showing_bomb_wire_pair_to_player;
        level.currenttimelimitdelay = 0.0;
        level.canprocessot = 1;
        level notify( "bmo_overtime_start" );
        level.init_chopper_boss = 1;
        level.init_civs = 1;
        _id_11C20( var_3 );
        wait 5;
    }

    level.init_chopper_boss = undefined;
    scripts\mp\flags::gameflagwait( "overtime_started" );
    setomnvar( "ui_br_circle_state", 7 );

    if ( !isdefined( var_2 ) )
    {
        var_9 = gettime() + int( level.get_showing_bomb_wire_pair_to_player * 1000 );
        setomnvar( "ui_hardpoint_timer", var_9 );
    }

    wait( int( max( level.get_showing_bomb_wire_pair_to_player - 60, 0 ) ) );
    setomnvar( "ui_br_circle_state", 8 );
}

killentireenemyteam()
{
    var_0 = "";

    foreach ( var_2 in level.teamnamelist )
    {
        var_3 = game["teamPlacements"][var_2];

        if ( var_3 == 1 )
        {
            var_0 = var_2;
            break;
        }
    }

    return var_0;
}

ontimelimit()
{
    if ( level._id_121AA && level.get_showing_bomb_wire_pair_to_player > 0 && !istrue( level.brplacementchallengeallowed ) )
    {
        if ( !isdefined( level.openaltbunker ) )
            level.openaltbunker = killentireenemyteam();

        level thread _id_1241E( level.openaltbunker, level.teamdata[level.openaltbunker]["plunderBanked"] * 1000 >= level_light() );
        level waittill( "bmo_overtime_start" );

        while ( level.currenttimelimitdelay < level.ontimelimitgraceperiod )
            wait( level.framedurationseconds );
    }

    level thread brpickupscriptableid();
}

brpickupscriptableid()
{
    if ( istrue( level.gameended ) )
        return;

    level thread lootstruct_offsets();
    level thread scripts\mp\gamelogic::endgame( level.checkbluemassacre.hudattractionobj, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
}

lootstruct_offsets()
{
    var_0 = scripts\mp\gamelogic::jugg_modifyherodroptoplayerdamage();

    foreach ( var_2 in level.players )
        var_2 scripts\mp\trials\mp_trl_floorislava::_id_11A6F( "post_game_state", var_0 );
}

tarmac_techo_start_first()
{
    if ( !istrue( self.controlsfrozen ) )
        scripts\mp\utility\player::_freezecontrols( 1, undefined, "spawnEndOfGame" );

    var_0 = spawnstruct();
    var_1 = self getspectatingplayer();

    if ( !isdefined( var_1 ) )
        var_1 = self;

    var_0.origin = var_1.origin;
    var_0.angles = var_1.angles;

    if ( !var_1 isonground() )
    {
        var_2 = scripts\engine\trace::create_default_contents( 1 );
        var_0.origin = scripts\engine\utility::drop_to_ground( var_0.origin, 0, -20000, undefined, var_2 );
    }

    var_0.origin = var_0.origin + ( 0, 0, 100 );

    if ( !isdefined( level.greenlight ) )
    {
        level.greenlight = 1;
        level thread _id_12412();
    }

    return 1;
}

handledevcommand_publicevents()
{
    level waittill( "give_match_bonus" );
    waitframe();
    var_0 = getdvarfloat( "scr_bmo_eom_held_cash_scalar", 1.0 );
    var_1 = getdvarfloat( "scr_bmo_eom_banked_cash_scalar", 1.0 );
    var_2 = getdvarint( "scr_bmo_eom_initial_winner_bonus", 10000 );
    var_3 = getdvarint( "scr_bmo_eom_over_wincost_bonus", 7500 );
    var_4 = getdvarint( "scr_bmo_eom_top10_bonus", 5000 );

    foreach ( var_6 in level.teamnamelist )
    {
        if ( level scripts\mp\utility\game::playerhumanconcusspushcleanup( var_6 ) )
            continue;

        var_7 = lbravo_spawner_jammer2b( var_6 ) * 100;
        var_8 = var_7 >= level_light();
        var_9 = game["teamPlacements"][var_6];
        var_10 = 0;

        if ( var_8 )
            var_10 = var_3;
        else if ( var_9 <= 10 )
            var_10 = var_4;

        var_11 = scripts\mp\utility\teams::getteamdata( var_6, "players" );

        foreach ( var_13 in var_11 )
        {
            var_13 scripts\mp\gametypes\br.gsc::_id_12023();
            var_13 scripts\cp_mp\utility\game_utility::_id_11A96( var_9 );

            if ( !var_13 scripts\mp\utility\game::rankingenabled() || !var_13 hasplayerdata() )
                continue;

            var_13 scripts\mp\utility\stats::incpersstat( "cash", int( var_7 / 10000 ) );
            var_14 = var_13.pers["combatXP"];

            if ( !isdefined( var_14 ) )
                var_14 = 0;

            var_13 setplayerdata( "mp", "aarValue", 0, var_14 );
            var_15 = var_13.pers["missionXP"];

            if ( !isdefined( var_15 ) )
                var_15 = 0;

            var_13 setplayerdata( "mp", "aarValue", 1, var_15 );
            var_16 = var_13.pers["lootingXP"];

            if ( !isdefined( var_16 ) )
                var_16 = 0;

            var_13 setplayerdata( "mp", "aarValue", 2, var_16 );
            var_17 = 0;

            if ( isdefined( var_13.plundercount ) )
                var_17 = int( var_13.plundercount * var_0 );

            var_18 = 0;

            if ( isdefined( var_13.plunderbanked ) )
                var_18 = int( var_13.plunderbanked * var_1 );

            var_19 = var_17 + var_18;

            if ( var_19 > 0 )
                var_13 scripts\mp\rank::giverankxp( "cash_conversion_bonus", var_19, undefined, 1, 1 );

            var_13 setplayerdata( "mp", "aarValue", 3, var_19 );
            var_20 = 0;

            if ( isdefined( var_13.matchbonus ) )
                var_20 = int( var_13.matchbonus );

            var_13 setplayerdata( "mp", "aarValue", 4, var_20 );

            if ( var_10 > 0 )
                var_13 scripts\mp\rank::giverankxp( "placement_bonus", var_10, undefined, 1, 1 );

            var_13 setplayerdata( "mp", "aarValue", 5, var_10 );
            var_21 = var_13 getplayerdata( "mp", "aarValue", 6 );
            var_22 = var_21 + var_13.pers["summary"]["xp"];
            var_13 setplayerdata( "mp", "aarValue", 7, var_22 );
        }
    }
}

onstompeenemyprogressupdate()
{
    var_0 = [];
    level.globalrelicsfunc = [];
    level.global_stealth_broken_func = [];

    if ( level.mapname == "mp_br_mechanics" )
    {
        level.globalrelicsfunc[0] = ( 8682, -1036, 427 );
        level.global_stealth_broken_func[0] = ( 14, 163, 0 );
        level.globalrelicsfunc[1] = ( -1139, -3425, 1116 );
        level.global_stealth_broken_func[1] = ( 33, 75, 0 );
        level.globalrelicsfunc[2] = ( -5567, -4786, 1116 );
        level.global_stealth_broken_func[2] = ( 37, 192, 0 );
    }
    else
    {
        level.globalrelicsfunc[0] = ( -36548, -31983, 2400 );
        level.global_stealth_broken_func[0] = ( 12, 72, 0 );
        level.globalrelicsfunc[1] = ( -17592, -36440, 1379 );
        level.global_stealth_broken_func[1] = ( 17, 90, 0 );
        level.globalrelicsfunc[2] = ( -3520, -34298, 1217 );
        level.global_stealth_broken_func[2] = ( 11, 110, 0 );
        level.globalrelicsfunc[3] = ( -9577, -25957, 360 );
        level.global_stealth_broken_func[3] = ( 357, 82, 0 );
        level.globalrelicsfunc[4] = ( 23022, -26926, 1359 );
        level.global_stealth_broken_func[4] = ( 16, 101, 0 );
        level.globalrelicsfunc[5] = ( 31261, -29753, 1359 );
        level.global_stealth_broken_func[5] = ( 27, 52, 0 );
        level.globalrelicsfunc[6] = ( 44843, -41261, 3220 );
        level.global_stealth_broken_func[6] = ( 16, 52, 0 );
        level.globalrelicsfunc[7] = ( 44229, -15403, 1331 );
        level.global_stealth_broken_func[7] = ( 13, 72, 0 );
        level.globalrelicsfunc[8] = ( 44491, 3484, 1638 );
        level.global_stealth_broken_func[8] = ( 23, 11, 0 );
        level.globalrelicsfunc[9] = ( 16047, -3206, 2613 );
        level.global_stealth_broken_func[9] = ( 27, 309, 0 );
        level.globalrelicsfunc[10] = ( 5668, -5905, 1614 );
        level.global_stealth_broken_func[10] = ( 23, 304, 0 );
        level.globalrelicsfunc[11] = ( -13412, -20443, 1033 );
        level.global_stealth_broken_func[11] = ( 11, 109, 0 );
        level.globalrelicsfunc[12] = ( -30369, -7811, 1680 );
        level.global_stealth_broken_func[12] = ( 31, 339, 0 );
        level.globalrelicsfunc[13] = ( -26278, 4081, 142 );
        level.global_stealth_broken_func[13] = ( 6, 110, 0 );
        level.globalrelicsfunc[14] = ( -16429, 6021, 847 );
        level.global_stealth_broken_func[14] = ( 21, 57, 0 );
        level.globalrelicsfunc[15] = ( -7525, 11672, 1082 );
        level.global_stealth_broken_func[15] = ( 14, 46, 0 );
        level.globalrelicsfunc[16] = ( 8356, 15296, 2021 );
        level.global_stealth_broken_func[16] = ( 12, 38, 0 );
        level.globalrelicsfunc[17] = ( 26010, 29975, 2716 );
        level.global_stealth_broken_func[17] = ( 13, 68, 0 );
        level.globalrelicsfunc[18] = ( 12043, 30910, 3081 );
        level.global_stealth_broken_func[18] = ( 21, 88, 0 );
        level.globalrelicsfunc[19] = ( 7127, 52592, 2100 );
        level.global_stealth_broken_func[19] = ( 28, 241, 0 );
        level.globalrelicsfunc[20] = ( -6693, 56481, 4026 );
        level.global_stealth_broken_func[20] = ( 16, 246, 0 );
        level.globalrelicsfunc[21] = ( -21394, 37175, 757 );
        level.global_stealth_broken_func[21] = ( 2, 124, 0 );
        level.globalrelicsfunc[22] = ( -26151, 25577, 271 );
        level.global_stealth_broken_func[22] = ( 357, 10, 0 );
    }

    if ( 0 )
        global_variables();
}

_id_12412()
{
    var_0 = 0;

    foreach ( var_2 in level.players )
    {
        if ( isdefined( var_2 ) )
        {
            var_2 thread tire_repair_start_air_stop_sfx( var_2, jugg_get_closest_attackable_player( var_2 ) );
            var_0++;
        }

        if ( var_0 == 5 )
        {
            waitframe();
            var_0 = 0;
        }
    }
}

global_variables()
{
    for (;;)
    {
        var_0 = getdvarint( "scr_bmo_testEndCamera", -1 );

        if ( var_0 > -1 )
        {
            tire_repair_start_air_stop_sfx( level.players[0], jugg_get_closest_attackable_player( level.players[0] ) );
            setdvar( "scr_bmo_testEndCamera", -1 );
        }

        waitframe();
    }
}

jugg_get_closest_attackable_player( var_0 )
{
    var_1 = undefined;
    var_2 = undefined;

    foreach ( var_6, var_4 in level.globalrelicsfunc )
    {
        var_5 = distance2dsquared( var_0.origin, var_4 );

        if ( var_5 <= 9000000 )
            return var_6;

        if ( !isdefined( var_2 ) || var_2 > var_5 )
        {
            var_2 = var_5;
            var_1 = var_6;
        }
    }

    return var_1;
}

tire_repair_start_air_stop_sfx( var_0, var_1 )
{
    var_2 = level.globalrelicsfunc[var_1];
    var_3 = level.global_stealth_broken_func[var_1];
    var_4 = var_2 + anglestoright( var_3 ) * 1000;
    var_5 = var_3;
    var_6 = spawn( "script_model", var_2 );
    var_6 setmodel( "tag_origin" );
    var_6.angles = var_3;
    var_0 cameralinkto( var_6, "tag_origin" );
    var_6 moveto( var_4, 60 );
    var_6 rotateto( var_5, 60 );

    if ( 0 )
    {
        wait 5;
        var_0 cameraunlink();
    }
}

trial_flare_destruct_missile()
{
    wait 1;
    var_0 = level_light();
    var_1 = tablesort( game["teamPlacements"], "up" );

    for ( var_2 = 0; var_2 < var_1.size - 1; var_2++ )
    {
        if ( isdefined( level.teamdata[var_1[var_2]]["plunderTeamTotal"] ) )
            var_3 = lbravo_spawner_jammer2b( var_1[var_2] ) * 100;
    }
}

onriskplayerdisconnect()
{
    wait 1;
    game["dialog"]["gametype"] = "gametype_bmo_plunder";
    game["dialog"]["match_start"] = "gametype_bmo_plunder";
    game["dialog"]["boost_short"] = "boost_bmo_short";
    game["dialog"]["offense_obj"] = "boost_bmo";
    game["dialog"]["defense_obj"] = "boost_bmo";
    game["dialog"]["contract_hold_area"] = "bm_contract_hold_area";
    game["dialog"]["contract_loot_chests"] = "bm_contract_loot_chests";
    game["dialog"]["contract_kill_target"] = "bm_contract_kill_target";
    game["dialog"]["event_chopper"] = "bm_event_chopper";
    game["dialog"]["event_airdrop"] = "bm_event_airdrop";
    game["dialog"]["extract_enabled"] = "bm_extract_enabled";
    game["dialog"]["gamestate_25_perc"] = "bm_gamestate_25_perc";
    game["dialog"]["gamestate_50_perc"] = "bm_gamestate_50_perc";
    game["dialog"]["gamestate_75_perc"] = "bm_gamestate_75_perc";
    game["dialog"]["gamestate_90_perc"] = "bm_gamestate_90_perc";
    game["dialog"]["gamestate_25_perc_enemy"] = "bm_gamestate_25_perc_enemy";
    game["dialog"]["gamestate_50_perc_enemy"] = "bm_gamestate_50_perc_enemy";
    game["dialog"]["gamestate_75_perc_enemy"] = "bm_gamestate_75_perc_enemy";
    game["dialog"]["gamestate_90_perc_enemy"] = "bm_gamestate_90_perc_enemy";
    game["dialog"]["gamestate_25_perc_first"] = "bm_gamestate_25_perc_first";
    game["dialog"]["gamestate_50_perc_first"] = "bm_gamestate_50_perc_first";
    game["dialog"]["gamestate_75_perc_first"] = "bm_gamestate_75_perc_first";
    game["dialog"]["gamestate_90_perc_first"] = "bm_gamestate_90_perc_first";
    game["dialog"]["lead_lost"] = "bm_gamestate_lead_lost";
    game["dialog"]["lead_taken"] = "bm_gamestate_lead_taken";
    game["dialog"]["mission_failure"] = "bm_gamestate_lost";
    game["dialog"]["mission_success"] = "bm_gamestate_win";
    game["dialog"]["gamestate_top_3"] = "bm_gamestate_top_3";
    game["dialog"]["gamestate_top_5"] = "bm_gamestate_top_5";
    game["dialog"]["gamestate_top_10"] = "bm_gamestate_top_10";
    game["dialog"]["bm_gamestate_overtime"] = "bm_gamestate_overtime_million_cash_deposited";
    game["dialog"]["bm_gamestate_overtime_enemy"] = "bm_tut_get_cash";
    game["dialog"]["bm_tut_get_cash"] = "bm_tut_get_cash";
    game["dialog"]["bm_tut_earn_cash"] = "bm_tut_earn_cash";
    game["dialog"]["bm_tut_loot_cash"] = "bm_tut_loot_cash";
    game["dialog"]["event_bank"] = "event_bank";
    game["dialog"]["exfil_arrived"] = "exfil_arrived";
    game["dialog"]["exfil_failed"] = "exfil_failed";
    game["dialog"]["exfil_inbound"] = "exfil_inbound";
    game["dialog"]["exfil_leaving"] = "exfil_leaving";
    game["dialog"]["exfil_start_generic"] = "exfil_start_generic";
    game["dialog"]["exfil_start_win"] = "exfil_start_win";
    game["dialog"]["exfil_start_win_lz"] = "exfil_start_win_lz";
    game["dialog"]["exfil_success_full"] = "exfil_success_full";
    game["dialog"]["exfil_success_partial"] = "exfil_success_partial";
}

_id_12A10( var_0 )
{
    level endon( "game_ended" );
    level endon( "cancel_watch_parachuters_overhead" );

    for (;;)
    {
        foreach ( var_15, var_2 in level.airlock_show_doors )
        {
            if ( !isdefined( var_2 ) || !scripts\mp\utility\player::isreallyalive( var_2 ) || !( var_2 isparachuting() || var_2 isinfreefall() ) )
            {
                level.airlock_show_doors[var_15] = undefined;
                continue;
            }

            var_3 = scripts\common\utility::playersincylinder( var_2.origin, level.solospawn, undefined, level.soldier_encounter_test );
            var_4 = var_2.team;

            foreach ( var_6 in var_3 )
            {
                if ( scripts\mp\utility\game::player_grenade_fire_monitor( var_6 ) )
                    continue;

                var_7 = var_4 == var_6.team;

                if ( var_7 )
                    continue;

                var_8 = !scripts\mp\utility\player::isreallyalive( var_6 ) || istrue( var_6.inlaststand );

                if ( var_8 )
                    continue;

                var_9 = var_6 isparachuting() || var_6 isinfreefall();

                if ( var_9 )
                    continue;

                var_10 = gettime();
                var_11 = isdefined( var_6.maxaccesscardspawns_red ) && var_10 - var_6.maxaccesscardspawns_red < var_0;

                if ( var_11 )
                    continue;

                var_6.maxaccesscardspawns_red = var_10;
                var_12 = scripts\mp\gametypes\br_public.gsc::checked_missiles( var_6 );
                var_13 = game["voice"][var_12];
                var_6 queuedialogforplayer( level.airlock_show_front_doors[var_13], "respawning_enemy_in_area", 2 );
            }

            waitframe();
        }

        waitframe();
    }
}

trial_fetch_mission_table( var_0, var_1 )
{
    var_2 = scripts\mp\gamescore::lbravo_spawner_jammer3b();
}
