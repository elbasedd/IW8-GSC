// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

#using_animtree("script_model");

setreduceregendelayonkills()
{
    setdvarifuninitialized( "scr_br_carriable_respawn_time", 0 );
    setdvarifuninitialized( "scr_br_carriable_inactive_delete_time", 300 );
    setdvarifuninitialized( "scr_br_carriable_spawn_system", 0 );
    setdvarifuninitialized( "scr_br_carriable_max_entity_cariables", 50 );
    setdvarifuninitialized( "scr_br_carriable_spawn_chance", 0 );
    precachestring( &"MP_BR_INGAME/PICKUP_PROPANE" );
    precachestring( &"MP_BR_INGAME/PICKUP_POISON" );
    setdvarifuninitialized( "carriableFuseTime", 5 );
    setdvarifuninitialized( "carriableThrowForce", 2250 );
    level.forest_barrels = [];
    level.forest_barrels["gasoline"] = spawnstruct();
    level.forest_barrels["gasoline"].weaponname = "gasoline_can_mp";
    level.forest_barrels["gasoline"].modelname = "offhand_wm_jerrycan_thrown";
    level.forest_barrels["gasoline"].round_smoke_logic = &"MP_BR_INGAME/PICKUP_PROPANE";
    level.forest_barrels["gasoline"].is_instant_use_munition = ::force_group_thermites;
    level.forest_barrels["gasoline"].is_kidnapping_player = "vfx_propane_exp_main";
    level.forest_barrels["gasoline"].is_killstreak_valid_for_swat = "vfx_propane_exp_air";
    level.forest_barrels["gasoline"].onlaststandrevive = "vfx_carriable_fuse";
    level.forest_barrels["gasoline"]._id_12EED = "vfx_fire_spout";
    level.forest_barrels["gasoline"].onmatchplacement = "canister_warning";
    level.forest_barrels["gasoline"]._id_13CB5 = getcompleteweaponname( level.forest_barrels["gasoline"].weaponname );
    level.forest_barrels["gasoline"].onloadout = "ges_carriable_gasoline_ignite";
    level.forest_barrels["gasoline"].onlowpopstart = "iw8_ges_plyr_carriable_gasoline_ignite";
    level.forest_barrels["gasoline"].onmaprestart = "gasolineCanisterIgnite";
    level.forest_barrels["gasoline"].onlineprimaryoverride = 1.23;
    level.forest_barrels["propane"] = spawnstruct();
    level.forest_barrels["propane"].weaponname = "gas_can_mp";
    level.forest_barrels["propane"].modelname = "offhand_wm_container_gas_tank_01";
    level.forest_barrels["propane"].round_smoke_logic = &"MP_BR_INGAME/PICKUP_PROPANE";
    level.forest_barrels["propane"].is_instant_use_munition = ::force_interrupt_current_combat_action;
    level.forest_barrels["propane"].is_module_active = "gas_tank_explode";
    level.forest_barrels["propane"].is_kidnapping_player = "vfx_propane_exp_main";
    level.forest_barrels["propane"].is_killstreak_valid_for_swat = "vfx_propane_exp_air";
    level.forest_barrels["propane"].onlaststandrevive = "vfx_carriable_fuse";
    level.forest_barrels["propane"]._id_12EED = "vfx_fire_spout";
    level.forest_barrels["propane"].onmatchplacement = "canister_warning";
    level.forest_barrels["propane"]._id_13CB5 = getcompleteweaponname( level.forest_barrels["propane"].weaponname );
    level.forest_barrels["propane"].onloadout = "ges_gas_can_ignite";
    level.forest_barrels["propane"].onlowpopstart = "iw8_ges_plyr_gas_can_ignite";
    level.forest_barrels["neurotoxin"] = spawnstruct();
    level.forest_barrels["neurotoxin"].weaponname = "gas_can_toxic_mp";
    level.forest_barrels["neurotoxin"].modelname = "offhand_wm_container_gas_tank_02";
    level.forest_barrels["neurotoxin"].round_smoke_logic = &"MP_BR_INGAME/PICKUP_POISON";
    level.forest_barrels["neurotoxin"].is_instant_use_munition = ::force_interrupt_all_current_combat_actions;
    level.forest_barrels["neurotoxin"].is_module_active = "neuro_tank_explode";
    level.forest_barrels["neurotoxin"].is_kidnapping_player = "vfx_ntoxin_exp_main";
    level.forest_barrels["neurotoxin"].is_killstreak_valid_for_swat = "vfx_ntoxin_exp_air";
    level.forest_barrels["neurotoxin"].original_meleechargedistvsplayer = "vfx_ntoxin_ground_gas";
    level.forest_barrels["neurotoxin"].original_health = "vfx_ntoxin_air_gas";
    level.forest_barrels["neurotoxin"].onlaststandrevive = "vfx_carriable_fuse";
    level.forest_barrels["neurotoxin"].onmatchplacement = "canister_warning";
    level.forest_barrels["neurotoxin"]._id_13CB5 = getcompleteweaponname( level.forest_barrels["neurotoxin"].weaponname );
    level.forest_barrels["neurotoxin"].onloadout = "ges_gas_can_ignite";
    level.forest_barrels["neurotoxin"].onlowpopstart = "iw8_ges_plyr_gas_can_ignite";
    level._effect["vfx_br3_rope_fire"] = loadfx( "vfx/iw8_br/island/custom/vfx_br3_rope_fire.vfx" );
    level._effect["flash_bang_explode"] = loadfx( "vfx/iw8_mp/equipment/flashbang/vfx_flash_bang" );
    level._effect["vfx_propane_exp_main"] = loadfx( "vfx/iw8_br/equipment/vfx_propane_exp_main" );
    level._effect["vfx_propane_exp_air"] = loadfx( "vfx/iw8_br/equipment/vfx_propane_exp_air" );
    level._effect["fire_drip_test"] = loadfx( "vfx/core/smktrail/smoke_geotrail_rpg" );
    level._effect["vfx_carriable_fuse"] = loadfx( "vfx/iw8_br/island/custom/vfx_br3_canister_fuse" );
    level._effect["vfx_fire_spout"] = loadfx( "vfx/iw8_br/equipment/vfx_fire_spout" );
    level._effect["vfx_ntoxin_ground_gas"] = loadfx( "vfx/iw8_br/equipment/vfx_ntoxin_ground_gas" );
    level._effect["vfx_ntoxin_air_gas"] = loadfx( "vfx/iw8_br/equipment/vfx_ntoxin_air_gas" );
    level._effect["vfx_ntoxin_exp_main"] = loadfx( "vfx/iw8_br/equipment/vfx_ntoxin_exp_main" );
    level._effect["vfx_ntoxin_exp_air"] = loadfx( "vfx/iw8_br/equipment/vfx_ntoxin_exp_air" );
    level.scr_animtree["player"] = #animtree;
    level.scr_anim["player"]["carriable_ascender_attach"] = %vm_eq_carriable_gasoline_ascender_attach_plr;
    level.scr_animname["player"]["carriable_ascender_attach"] = "vm_eq_carriable_gasoline_ascender_attach_plr";
    level.scr_eventanim["player"]["carriable_ascender_attach"] = "carriable_ascender_attach";
    level.scr_animtree["device"] = #animtree;
    level.scr_anim["device"]["carriable_ascender_device_attach"] = %wm_eq_carriable_gasoline_ascender_attach_ascender;
    level.scr_animname["device"]["carriable_ascender_device_attach"] = "wm_eq_carriable_gasoline_ascender_attach_ascender";
    level.scr_eventanim["device"]["carriable_ascender_device_attach"] = "carriable_ascender_device_attach";
    level._id_137AA = [];
    level.forceunsetdemeanor = [];
    scripts\engine\scriptable::_id_1279F( "br_carriable_pickup", ::_id_127A6 );
    scripts\engine\scriptable::_id_1279E( ::_id_127A4 );
    get_track_start_struct();
}

_id_12D36( var_0, var_1, var_2 )
{
    if ( !isdefined( var_0 ) )
    {

    }

    if ( !isdefined( var_1 ) )
        var_1 = ( 0, 0, 0 );

    if ( !isdefined( var_2 ) )
        var_2 = ( 0, 0, 0 );

    level._id_137AA = scripts\engine\utility::array_removeundefined( level._id_137AA );
    var_3 = getdvarint( "scr_br_carriable_max_entity_cariables", 50 );

    if ( level._id_137AA.size >= var_3 )
    {
        var_4 = 999999999;
        var_5 = undefined;

        foreach ( var_7 in level._id_137AA )
        {
            if ( istrue( var_7.onlyexecutefromthefront ) || var_7 islinked() )
                continue;

            if ( var_7.tango_infil_radio_idle < var_4 )
            {
                var_4 = var_7.tango_infil_radio_idle;
                var_5 = var_7;
            }
        }

        if ( !isdefined( var_5 ) )
        {

        }

        var_5 delete();
    }

    var_9 = spawn( "script_model", var_1 );
    var_9 setmodel( var_0.modelname );
    var_9.angles = var_2;
    var_9.forest_barrel_damage_watch = var_0;
    var_9 forceabshotfoot( var_0 );
    level._id_137AA = scripts\engine\utility::array_add( level._id_137AA, var_9 );
    return var_9;
}

get_track_start_struct()
{
    foreach ( var_1 in level._id_137AA )
    {
        if ( !isdefined( var_1 ) )
            continue;

        if ( var_1 islinked() )
            var_1 forcefail( 1 );

        var_1 delete();
    }

    level._id_137AA = [];

    switch ( getdvarint( "scr_br_carriable_spawn_system", 0 ) )
    {
        case 0:
            _id_12D12();
            break;
        case 1:
            _id_12D0F();
            break;
        case 2:
            _id_12D10();
            break;
        default:
            _id_12D12();
            break;
    }
}

_id_12D12()
{
    var_0 = getentitylessscriptablearrayinradius( "scriptable_br_carriable_gasoline", "classname" );
    var_1 = getentitylessscriptablearrayinradius( "scriptable_br_carriable_propane", "classname" );
    var_2 = getentitylessscriptablearrayinradius( "scriptable_br_carriable_neurotoxin", "classname" );
    var_3 = scripts\engine\utility::array_randomize( scripts\engine\utility::array_combine( var_0, var_1, var_2 ) );

    if ( var_3.size == 0 )
        return;

    var_4 = getdvarfloat( "scr_br_carriable_spawn_chance", 0 );
    var_5 = floor( var_4 * var_3.size );

    for ( var_6 = 0; var_6 < var_3.size; var_6++ )
    {
        if ( var_6 < var_5 )
        {
            var_3[var_6] setscriptablepartstate( "br_carriable_pickup", "visible" );
            continue;
        }

        var_3[var_6] setscriptablepartstate( "br_carriable_pickup", "hidden" );
    }
}

_id_12D10()
{
    var_0 = getdvarfloat( "scr_br_carriable_spawn_chance", 0 );
    var_1 = scripts\engine\utility::array_randomize( getentitylessscriptablearrayinradius( "scriptable_br_carriable_gasoline", "classname" ) );
    var_2 = scripts\engine\utility::array_randomize( getentitylessscriptablearrayinradius( "scriptable_br_carriable_propane", "classname" ) );
    var_3 = scripts\engine\utility::array_randomize( getentitylessscriptablearrayinradius( "scriptable_br_carriable_neurotoxin", "classname" ) );

    if ( var_1.size > 0 )
    {
        var_4 = floor( var_0 * var_1.size );

        for ( var_5 = 0; var_5 < var_1.size; var_5++ )
        {
            if ( var_5 < var_4 )
            {
                var_1[var_5] setscriptablepartstate( "br_carriable_pickup", "visible" );
                continue;
            }

            var_1[var_5] setscriptablepartstate( "br_carriable_pickup", "hidden" );
        }
    }

    if ( var_2.size > 0 )
    {
        var_4 = floor( var_0 * var_2.size );

        for ( var_5 = 0; var_5 < var_2.size; var_5++ )
        {
            if ( var_5 < var_4 )
            {
                var_2[var_5] setscriptablepartstate( "br_carriable_pickup", "visible" );
                continue;
            }

            var_2[var_5] setscriptablepartstate( "br_carriable_pickup", "hidden" );
        }
    }

    if ( var_3.size > 0 )
    {
        var_4 = floor( var_0 * var_3.size );

        for ( var_5 = 0; var_5 < var_3.size; var_5++ )
        {
            if ( var_5 < var_4 )
            {
                var_3[var_5] setscriptablepartstate( "br_carriable_pickup", "visible" );
                continue;
            }

            var_3[var_5] setscriptablepartstate( "br_carriable_pickup", "hidden" );
        }
    }
}

_id_12D0F()
{
    var_0 = scripts\engine\utility::array_randomize( getentitylessscriptablearrayinradius( "scriptable_br_carriable_gasoline", "classname" ) );

    if ( var_0.size == 0 )
        return;

    var_1 = [];
    var_1["agricultural_center"] = ( 11086, -12336, 0 );
    var_1["airfield"] = ( -21484, -22516, 0 );
    var_1["arsenal"] = ( -17334, 47252, 0 );
    var_1["beachhead"] = ( 52009, 20904, 0 );
    var_1["caldera"] = ( 12090, 13317, 0 );
    var_1["capital"] = ( 22833, -47700, 0 );
    var_1["docks"] = ( 14082, 50019, 0 );
    var_1["lagoon"] = ( -45466, -19181, 0 );
    var_1["mines"] = ( -6589, 14626, 0 );
    var_1["radio_station"] = ( -5862, -46402, 0 );
    var_1["resort"] = ( 42804, -43023, 0 );
    var_1["ruins"] = ( -44348, 19225, 0 );
    var_1["runway"] = ( 42214, 39322, 0 );
    var_1["submarine_pen"] = ( 44138, -20531, 0 );
    var_1["village"] = ( -33835, -1222, 0 );
    var_2 = [];

    foreach ( var_5, var_4 in var_1 )
        var_2[var_5] = [];

    foreach ( var_7 in var_0 )
    {
        var_8 = undefined;
        var_9 = 99999;

        foreach ( var_5, var_4 in var_1 )
        {
            var_11 = distance2d( var_7.origin, var_4 );

            if ( var_11 < var_9 )
            {
                var_9 = var_11;
                var_8 = var_5;
            }
        }

        var_7 setscriptablepartstate( "br_carriable_pickup", "hidden" );
        var_2[var_8][var_2[var_8].size] = var_7;
    }

    foreach ( var_5, var_14 in var_2 )
    {
        var_15 = getdvarfloat( "scr_br_carriable_spawn_chance_" + var_5, getdvarfloat( "scr_br_carriable_spawn_chance", 0 ) );
        var_16 = var_14;
        var_17 = 0;

        foreach ( var_7 in var_14 )
        {
            if ( isdefined( var_7.script_noteworthy ) && var_7.script_noteworthy == "forcespawn" )
            {
                var_7 linktooriginandangles();
                var_16 = scripts\engine\utility::array_remove( var_16, var_7 );
                var_17++;
            }
        }

        var_20 = floor( float( var_15 ) * ( var_14.size - var_17 ) );

        if ( var_20 <= 0 )
            continue;
        else
        {
            for ( var_21 = 0; var_21 < var_20; var_21++ )
                var_16[var_21] linktooriginandangles();
        }
    }
}

linktooriginandangles()
{
    self setscriptablepartstate( "br_carriable_pickup", "visible" );
}

_id_127A6( var_0, var_1, var_2, var_3, var_4 )
{
    if ( istrue( level.gameended ) )
        return;

    if ( !var_3 scripts\cp_mp\utility\player_utility::_isalive() || istrue( var_3.inlaststand ) )
        return;

    if ( var_3 scripts\cp_mp\utility\player_utility::isinvehicle() )
    {
        if ( isdefined( level.showuseresultsfeedback ) )
        {
            var_3 [[ level.showuseresultsfeedback ]]( 17 );
            return;
        }
    }

    if ( !var_0 force_call_lz( var_3 ) )
        return;

    if ( var_2 == "visible" )
    {
        var_5 = level.forest_barrels["gasoline"];

        if ( var_0.type == "br_carriable_propane" )
            var_5 = level.forest_barrels["propane"];

        if ( var_0.type == "br_carriable_neurotoxin" )
            var_5 = level.forest_barrels["neurotoxin"];

        var_0 setscriptablepartstate( "br_carriable_pickup", "hidden" );
        level notify( "carriable_kill_callout_" + var_0.origin );
        var_6 = _id_12D36( var_5, var_3.origin );
        var_6 thread forced_bleedout( var_3 );
        level thread _id_12524( var_0, var_1 );
    }
}

spawn_carriable_at_struct( var_0 )
{
    return var_0.type == "br_carriable_gasoline" || var_0.type == "br_carriable_neurotoxin" || var_0.type == "br_carriable_propane";
}

_id_127A4( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10 )
{
    if ( !isdefined( var_2 ) || !spawn_carriable_at_struct( var_2 ) )
        return;

    level thread _id_127A5( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10 );
}

_id_127A5( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10 )
{
    waittillframeend;
    var_11 = level.forest_barrels["gasoline"];

    if ( var_2.type == "br_carriable_propane" )
        var_11 = level.forest_barrels["propane"];

    if ( var_2.type == "br_carriable_neurotoxin" )
        var_11 = level.forest_barrels["neurotoxin"];

    level notify( "carriable_kill_callout_" + var_2.origin );
    var_12 = _id_12D36( var_11, var_2.origin, var_2.angles );
    var_12.owner = var_1;
    var_12.team = var_1.team;

    if ( isplayer( var_1 ) && istrue( var_1.remaphardpointorder ) )
        var_12.spawnglobalscriptabledelayed = 1;

    var_12 thread [[ var_11.is_instant_use_munition ]]();
    var_12 makeunusable();
    var_12 hide();
    var_12.origin = var_2.origin + ( 0, 0, 10000 );
    wait 5;

    if ( isdefined( var_12 ) )
        var_12 delete();
}

_id_12524( var_0, var_1 )
{
    var_2 = getdvarfloat( "scr_br_carriable_respawn_time", 0 );

    if ( var_2 == 0 )
        return;

    wait( var_2 );
    var_0 setscriptablepartstate( "br_carriable_pickup", "visible" );
}

_id_12D0B( var_0 )
{
    var_1 = "gasoline";

    if ( var_0.script_noteworthy == "carriable_propane" )
        var_1 = "propane";

    if ( var_0.script_noteworthy == "carriable_neurotoxin" )
        var_1 = "neurotoxin";

    var_2 = level.forest_barrels[var_1];
    var_3 = _id_12D36( var_2, var_0.origin );
}

forceabshotfoot( var_0 )
{
    self.start_origin = self.origin;
    self.forest_barrel_damage_watch = var_0;

    if ( !isdefined( self.script_health ) )
        self.script_health = 16;

    self setnonstick( 1 );
    self sethintstring( self.forest_barrel_damage_watch.round_smoke_logic );
    self setuseholdduration( "duration_short" );
    self sethintrequiresholding( 0 );
    forceexplosivedeath();
}

forceextractscriptable()
{
    self.origin = self.start_origin;
    self show();
    forceexplosivedeath();
}

forceexplosivedeath()
{
    self _meth_87CB( 0 );
    self physics_takecontrol( 1 );
    self physics_registerforcollisioncallback();
    thread forceotlogictorun();
    thread forcejumpnearobjective();
    self.tango_infil_radio_idle = gettime();
    self.team = undefined;

    if ( !isdefined( self.onlyexecutefromthefront ) )
        self.onlyexecutefromthefront = 0;

    if ( !self.onlyexecutefromthefront )
    {
        self makeusable();
        thread forced_kill_off();
        thread force_var_to_array();
    }

    if ( self.script_health > 0 )
    {
        self setcandamage( 1 );
        self.hostdefensefactormod = 0;
        self.health = 99999999;
        thread force_dismount();
    }
}

force_dismount()
{
    self notify( "carriable_damage_wait" );
    self endon( "death" );
    self endon( "explode" );
    self endon( "carriable_damage_wait" );

    for (;;)
    {
        self waittill( "damage", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 );
        self.health = 99999999;

        if ( var_0 < 2 )
        {
            thread force_players_out_of_vehicle( undefined, undefined );
            thread force_teleport_player_into_plane( var_1 );
            continue;
        }

        if ( isdefined( var_1.owner ) && isdefined( var_1.owner.vehicle ) )
        {
            var_10 = var_1.owner.vehicle vehicle_getvelocity();
            var_11 = sqrt( vectordot( var_10, var_10 ) );

            if ( var_11 > 400 )
            {
                self.owner = undefined;
                self.team = undefined;
                thread force_maze_ai_state();
                return;
            }
        }
        else
        {
            switch ( var_4 )
            {
                case "MOD_CRUSH":
                case "MOD_IMPACT":
                case "melee":
                case "MOD_MELEE":
                    break;
                default:
                    var_12 = isdefined( var_1 ) && scripts\mp\utility\damage::attackerishittingteam( self, var_1 );

                    if ( !var_12 )
                    {
                        if ( isdefined( var_1 ) )
                        {
                            self.owner = var_1;
                            self.team = var_1.team;
                        }

                        if ( isdefined( level._id_119B3 ) && isplayer( var_1 ) && istrue( var_1.remaphardpointorder ) )
                        {
                            self.spawnglobalscriptabledelayed = 1;
                            self [[ level._id_119B3 ]]( var_1 );
                        }

                        thread force_maze_ai_state();
                        return;
                    }
            }
        }
    }
}

force_players_out_of_vehicle( var_0, var_1 )
{
    if ( isdefined( var_0 ) )
    {
        var_0 endon( "death_or_disconnect" );
        var_0 endon( "drop_object" );
    }

    self endon( "death" );
    self endon( "explode" );

    if ( isdefined( var_1 ) )
        wait( var_1 );

    self.onlyexecutefromthefront = 1;
    self setscriptablepartstate( "fuse", "fuse_sfx" );
    playfxontag( level._effect[self.forest_barrel_damage_watch.onlaststandrevive], self, "tag_fx" );

    if ( isdefined( var_0 ) && isdefined( self.forest_barrel_damage_watch.onmaprestart ) )
    {
        var_0 setscriptablepartstate( "weaponVFXViewmodel", self.forest_barrel_damage_watch.onmaprestart, 0 );
        var_0 setscriptablepartstate( "weaponVFXWorldmodel", self.forest_barrel_damage_watch.onmaprestart, 0 );
    }
}

force_teleport_player_into_plane( var_0 )
{
    self endon( "death" );
    self endon( "explode" );
    self endon( "pickup" );
    self endon( "cancel_fuse" );

    if ( 0 )
        thread force_thermites( var_0 );

    var_0 setclientomnvar( "ui_br_gas_can_status", 2 );
    var_1 = getdvarfloat( "carriableFuseTime", 5 );
    wait( var_1 );
    self.owner = var_0;
    self.team = var_0.team;
    thread force_maze_ai_state();
}

force_thermites( var_0 )
{
    var_1 = var_0 scripts\mp\hud_util::createprimaryprogressbar();
    var_2 = var_0 scripts\mp\hud_util::createprimaryprogressbartext();
    var_2 settext( "FUSE LIT" );
    var_3 = getdvarfloat( "carriableFuseTime", 5 );

    if ( var_3 <= 0 )
        var_3 = 1;

    var_1 scripts\mp\hud_util::updatebar( 0, 1 / var_3 );
    var_0 scripts\engine\utility::_id_13ADA( "death", "weapon_fired", "drop_object" );
    var_1 scripts\mp\hud_util::destroyelem();
    var_2 scripts\mp\hud_util::destroyelem();
}

force_spawn_all_dead_players()
{
    self endon( "death" );
    self endon( "explode" );

    if ( istrue( self.onlyexecutefromthefront ) )
    {
        waitframe();
        force_players_out_of_vehicle( undefined, undefined );
    }
}

forced_laststand_weapon( var_0, var_1 )
{
    self endon( "death_or_disconnect" );
    var_2 = getcompleteweaponname( var_0 );
    self giveandfireoffhand( var_2 );
    scripts\common\utility::allow_fire( 0, "carriableGesture" );
    wait( var_1 );
    scripts\common\utility::allow_fire( 1, "carriableGesture" );

    if ( self hasweapon( var_2 ) )
        self takeweapon( var_2 );
}

force_remove_stim( var_0 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );
    var_1 = self getgestureanimlength( var_0.forest_barrel_damage_watch.onloadout );
    thread forced_laststand_weapon( var_0.forest_barrel_damage_watch.onlowpopstart, var_1 );

    if ( isdefined( var_0.forest_barrel_damage_watch.onlineprimaryoverride ) )
        var_2 = var_0.forest_barrel_damage_watch.onlineprimaryoverride;
    else
        var_2 = var_1;

    var_0 thread force_players_out_of_vehicle( self, var_2 );
    wait( var_1 );
}

force_teleport_downedplayer( var_0 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );

    for (;;)
    {
        self waittill( "fuse_attempt_carriable" );

        if ( self getcurrentweapon() == var_0.forest_barrel_damage_watch._id_13CB5 && !self attackbuttonpressed() )
        {
            force_remove_stim( var_0 );
            var_0 thread force_teleport_player_into_plane( self );
            return;
        }
    }
}

force_maze_ai_state( var_0 )
{
    if ( istrue( self.exploded ) )
        return;

    self.exploded = 1;
    level endon( "game_ended" );
    self endon( "death" );
    self notify( "explode" );
    level notify( "carriable_kill_callout_" + self.origin );

    if ( istrue( self.onlyexecutefromthefront ) )
        stopfxontag( level._effect[self.forest_barrel_damage_watch.onlaststandrevive], self, "tag_fx" );

    if ( isdefined( self.carrier ) )
    {
        thread forced_aitypes( ( 0, 0, -90 ), self.carrier );
        wait 0.1;
    }

    self [[ self.forest_barrel_damage_watch.is_instant_use_munition ]]( var_0 );
    self makeunusable();
    self hide();
    self.origin = self.origin + ( 0, 0, 10000 );
    wait 5;
    self delete();
}

forced_kill_off()
{
    self endon( "death" );
    self endon( "explode" );
    self endon( "pickup" );

    for (;;)
    {
        self waittill( "trigger", var_0 );

        if ( isdefined( var_0 ) && isalive( var_0 ) && force_call_lz( var_0 ) )
        {
            thread forced_bleedout( var_0 );
            return;
        }
    }
}

forced_bleedout( var_0 )
{
    self notify( "pickup" );
    level notify( "carriable_kill_callout_" + self.origin );
    var_1 = self getlinkedparent();

    if ( isdefined( var_1 ) )
        self unlink();

    self physicslaunchserver( self.origin, ( 0, 0, 0 ) );
    self physicsstopserver();
    self show();
    self hide();
    self linkto( var_0, "tag_accessory_right", ( 0, 0, 0 ), ( 0, 0, 0 ) );

    if ( isdefined( self.projectile ) )
        self.projectile delete();

    self setotherent( var_0 );
    self makeunusable();
    self.carrier = var_0;
    self.owner = var_0;
    self.team = var_0.team;
    var_0 scripts\mp\equipment::allow_equipment( 0, "carriable" );
    var_0.iscarrying = 1;
    var_0._id_12138 = var_0 getcurrentweapon();
    var_0 giveweapon( self.forest_barrel_damage_watch._id_13CB5 );
    var_0 setweaponammoclip( self.forest_barrel_damage_watch._id_13CB5, 1 );
    var_0 switchtoweapon( self.forest_barrel_damage_watch._id_13CB5 );
    var_0 thread isgroundwarcoremode();
    var_0 scripts\engine\utility::_id_13ADB( "weapon_change", "weapon_taken", "weapon_switch_invalid", "death_or_disconnect" );

    if ( !isalive( var_0 ) || var_0 getcurrentweapon() != self.forest_barrel_damage_watch._id_13CB5 )
    {
        if ( isdefined( var_0 ) )
        {
            var_0 scripts\mp\equipment::allow_equipment( 1, "carriable" );
            var_0.iscarrying = 0;
            var_0 takeweapon( self.forest_barrel_damage_watch._id_13CB5 );
            var_0._id_12138 = undefined;
            var_0 notify( "drop_object" );
        }

        self.owner = undefined;
        self.carrier = undefined;
        self.team = undefined;
        self setotherent( undefined );
        self unlink();
        self show();
        var_2 = anglestoforward( self.angles ) * 40;
        forced_aitype_armored( var_2 );
        forceexplosivedeath();
        return;
    }

    self _meth_87CB( 1 );
    var_0.footprint_mask_clipheight = self;
    var_0 thread forcegivekillstreak( self );
    var_0 thread force_teleport_downedplayer( self );
    var_0 thread forcestuckdamage( self, "weapon_switch_started" );
    var_0 thread forcestuckdamage( self, "weapon_change" );
    var_0 thread forced_revive( self );
    var_0 thread forcedisablelaststand( self );
    var_0 thread force_kill_off_other_ai();

    if ( !isai( var_0 ) )
    {
        var_0 notifyonplayercommand( "lethal_attempt_carriable", "+frag" );
        var_0 notifyonplayercommand( "lethal_attempt_carriable", "+smoke" );
        var_0 notifyonplayercommand( "fuse_attempt_carriable", "+speed_throw" );

        if ( !var_0 isconsoleplayer() )
            var_0 notifyonplayercommand( "fuse_attempt_carriable", "+toggleads_throw" );
    }

    var_0 scripts\common\utility::allow_supers( 0 );
    var_0 allowmelee( 0 );
    var_0 allowsupersprint( 0 );
    var_0 scripts\common\utility::allow_prone( 0, "carriable" );
    var_0 setclientomnvar( "ui_br_gas_can_status", 1 );
}

_id_13820( var_0 )
{
    var_1 = undefined;

    if ( issameweapon( var_0 ) )
    {
        if ( nullweapon( var_0 ) )
            return 0;

        foreach ( var_3 in level.forest_barrels )
        {
            if ( var_3._id_13CB5 == var_0 )
                return 0;
        }

        var_1 = var_0.basename;
    }

    if ( isstring( var_0 ) )
    {
        if ( var_0 == "none" )
            return 0;

        foreach ( var_3 in level.forest_barrels )
        {
            if ( var_3._id_13CB5.basename == var_0 )
                return 0;
        }

        var_1 = var_0;
    }

    if ( scripts\mp\utility\killstreak::isremotekillstreakweapon( var_1 ) )
        return 0;

    if ( scripts\mp\utility\weapon::iskillstreakweapon( var_0 ) )
        return 0;

    return 1;
}

force_call_lz( var_0 )
{
    if ( !isplayer( var_0 ) )
        return 0;

    if ( !var_0 scripts\common\utility::is_weapon_allowed() )
        return 0;

    if ( var_0 scripts\mp\supers::issuperinuse() )
        return 0;

    if ( var_0 scripts\cp_mp\utility\player_utility::isinvehicle() )
        return 0;

    if ( istrue( var_0.inlaststand ) )
        return 0;

    if ( istrue( var_0.isreviving ) )
        return 0;

    if ( var_0 isskydiving() )
        return 0;

    if ( istrue( var_0.isjuggernaut ) )
    {
        if ( isdefined( level.showuseresultsfeedback ) )
        {
            var_0 [[ level.showuseresultsfeedback ]]( 16 );
            return 0;
        }
    }

    if ( isdefined( var_0.manuallyjoiningkillstreak ) && var_0.manuallyjoiningkillstreak )
        return 0;

    if ( istrue( var_0.iscarrying ) )
    {
        if ( isdefined( level.showuseresultsfeedback ) )
        {
            var_0 [[ level.showuseresultsfeedback ]]( 3 );
            return 0;
        }
    }

    var_1 = var_0 getcurrentweapon();

    if ( isdefined( var_1 ) )
    {
        if ( !_id_13820( var_1 ) )
        {
            var_0 scripts\mp\hud_message::showerrormessage( "MP/FIELD_UPGRADE_CANNOT_USE" );
            return 0;
        }
    }

    var_2 = var_0.changingweapon;

    if ( isdefined( var_2 ) && var_0 isswitchingweapon() )
    {
        if ( !_id_13820( var_2 ) )
            return 0;
    }

    if ( var_0 scripts\cp_mp\utility\inventory_utility::isanymonitoredweaponswitchinprogress() )
    {
        var_2 = var_0 scripts\cp_mp\utility\inventory_utility::getcurrentmonitoredweaponswitchweapon();

        if ( !_id_13820( var_2 ) )
            return 0;
    }

    if ( var_0 scripts\mp\utility\player::isusingremote() )
        return 0;

    if ( istrue( self.onlyexecutefromthefront ) )
        return 0;

    if ( istrue( var_0.shouldpickup ) )
        return 0;

    return 1;
}

forcegivekillstreak( var_0 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );
    var_1 = 0;
    var_2 = getdvarfloat( "carriableThrowForce", 2250 );

    for (;;)
    {
        self waittill( "grenade_fire", var_3, var_4 );

        if ( var_4 != var_0.forest_barrel_damage_watch._id_13CB5 )
            continue;

        if ( isdefined( var_3 ) )
            var_3 initlocs_bunkertest();

        self setweaponammoclip( var_0.forest_barrel_damage_watch._id_13CB5, 0 );
        break;
    }

    if ( self issprintsliding() )
    {
        var_1 = -12;
        var_2 = var_2 + 200;
    }

    var_5 = self getplayerangles();
    var_5 = var_5 + ( var_1, 0, 0 );
    var_5 = ( clamp( var_5[0], -85, 85 ), var_5[1], var_5[2] );
    var_6 = anglestoforward( var_5 );
    var_0 thread forced_aitypes( var_6 * var_2, self );
}

initlocs_bunkertest()
{
    self endon( "death" );
    waitframe();
    self delete();
}

isgroundwarcoremode()
{
    self disableweaponswitch();
    scripts\engine\utility::_id_13ADA( "weapon_change", "death_or_disconnect", "drop_object" );
    self enableweaponswitch();
}

forcestuckdamage( var_0, var_1 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );
    thread isgroundwarcoremode();

    for (;;)
    {
        self waittill( var_1, var_2 );

        if ( istrue( self.usingascender ) )
            continue;

        if ( var_2 != var_0.forest_barrel_damage_watch._id_13CB5 )
        {
            wait 0.34;
            break;
        }
    }

    var_0 thread forcefail( 0, 0.4 );
}

force_kill_off_other_ai()
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );

    for (;;)
    {
        self waittill( "lethal_attempt_carriable" );
        scripts\mp\hud_message::showerrormessage( "MP/FIELD_UPGRADE_CANNOT_USE" );
    }
}

forcedisablelaststand( var_0 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    self endon( "drop_object" );

    for (;;)
    {
        if ( self isskydiving() )
            break;

        if ( scripts\mp\supers::issuperinuse() )
            break;

        if ( scripts\cp_mp\utility\player_utility::isinvehicle( 1 ) )
            break;

        if ( self isinexecutionattack() || self isinexecutionvictim() )
            break;

        waitframe();
    }

    var_0 thread forcefail( 0 );
}

forced_revive( var_0 )
{
    level endon( "game_ended" );
    self endon( "drop_object" );
    self waittill( "death_or_disconnect" );
    var_1 = self getplayerangles();
    var_1 = ( clamp( var_1[0], -85, 85 ), scripts\engine\utility::absangleclamp180( var_1[1] ), 0 );
    var_2 = anglestoforward( var_1 );
    var_3 = 90;
    var_0 thread forced_aitypes( var_2 * var_3, self );
}

forced_aitype_armored( var_0, var_1, var_2 )
{
    self.origin_prev = undefined;
    var_3 = self;

    if ( isdefined( var_1 ) )
    {
        self.origin = var_1 gettagorigin( "j_gun" );
        self.angles = var_1 gettagangles( "j_gun" );
    }

    self dontinterpolate();

    if ( istrue( var_2 ) )
        self physicslaunchserver( self.origin, var_0 * 1.2 );
    else
        self physicslaunchserver( self.origin + 10 * anglestoup( self.angles ), var_0 );
}

lootspawnitem()
{
    return isalive( self ) && ( scripts\common\vehicle::isvehicle() || isdefined( self.classname ) && self.classname == "script_vehicle" );
}

force_bleedout_all_downed_players( var_0, var_1 )
{
    self endon( "death" );
    self endon( "explode" );
    self endon( "pickup" );
    self endon( "collision" );

    if ( var_1 > 1 || var_0[2] < 0.5 )
        wait 2;
    else
        wait 0.5;

    if ( !self islinked() )
    {
        var_2 = scripts\engine\trace::ray_trace( self.origin, ( self.origin[0], self.origin[1], self.origin[2] - 10 ), [ self ] );
        var_3 = var_2["entity"];

        if ( isdefined( var_3 ) && var_3 != self && ( var_3 lootspawnitem() || var_3 _meth_87C5() ) )
        {
            self linkto( var_3 );
            self _meth_87C7( 1 );
            self _meth_87C6( 1 );
        }
    }
}

forceotlogictorun()
{
    self endon( "death" );
    self endon( "explode" );
    self endon( "pickup" );

    for (;;)
    {
        self waittill( "collision", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7 );

        if ( !self islinked() )
            thread force_bleedout_all_downed_players( var_5, var_6 );

        if ( !istrue( self.onlyexecutefromthefront ) )
            continue;

        if ( isdefined( var_7 ) && isdefined( var_7.vehiclename ) )
        {
            if ( !isdefined( self.owner ) )
            {
                var_8 = scripts\cp_mp\vehicles\vehicle_occupancy::vehicle_occupancy_getdriver( var_7 );

                if ( !isdefined( var_8 ) || !isplayer( var_8 ) )
                    continue;

                self.owner = var_8;
            }

            thread force_maze_ai_state();
            break;
        }
    }
}

forced_aitypes( var_0, var_1 )
{
    self.moverdoesnotkill = 1;
    forcefail( 1 );
    forced_aitype_armored( var_0, var_1, 1 );
    forceexplosivedeath();
}

forcejumpnearobjective()
{
    self endon( "death" );
    self endon( "pickup" );
    var_0 = [ [ "bounce_large_sfx", 20.0 ], [ "bounce_medium_sfx", 5.0 ], [ "bounce_small_sfx", 1.0 ] ];

    while ( isdefined( self ) )
    {
        self waittill( "collision", var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8 );

        foreach ( var_10 in var_0 )
        {
            if ( var_7 > var_10[1] )
            {
                if ( self getscriptableparthasstate( "sfx", var_10[0] ) )
                    self setscriptablepartstate( "sfx", var_10[0] );

                break;
            }
        }

        wait 0.125;
        self setscriptablepartstate( "sfx", "disabled" );
    }
}

forcefail( var_0, var_1 )
{
    var_2 = 40;
    var_3 = 11.0;
    var_4 = 7.1;

    if ( !isdefined( var_0 ) )
        var_0 = 0;

    self.droptime = gettime();
    self notify( "dropped" );
    var_5 = ( 0, 0, 0 );
    var_6 = self.carrier;

    if ( isdefined( var_6 ) && var_6.team != "spectator" )
    {
        var_7 = var_6.origin;
        var_5 = var_6.angles;
        var_6 notify( "drop_object" );
        var_6 setclientomnvar( "ui_br_gas_can_status", 0 );
    }
    else
        var_7 = self.start_origin;

    var_8 = ( 0, var_5[1], 0 );
    var_9 = anglestoforward( var_8 );
    var_7 = var_7 + ( ( 0, 0, var_3 ) + var_4 * var_9 );
    self.origin = var_7;
    self.angles = var_5;
    self show();
    var_10 = self getlinkedparent();

    if ( isdefined( var_10 ) )
        self unlink();

    forest_fire_setup( var_1 );
    self dontinterpolate();
    self.ownerteam = "any";

    if ( !var_0 )
    {
        var_11 = var_9 * var_2;
        forced_aitype_armored( var_11, var_6 );
        forceexplosivedeath();
    }

    thread force_spawn_all_dead_players();
    return 1;
}

forest_fire_setup( var_0 )
{
    if ( isdefined( self.carrier ) )
    {
        if ( istrue( self.onlyexecutefromthefront ) )
        {
            self.carrier setscriptablepartstate( "weaponVFXViewmodel", "neutral", 0 );
            self.carrier setscriptablepartstate( "weaponVFXWorldmodel", "neutral", 0 );
        }

        self.carrier.iscarrying = undefined;
        self.carrier.footprint_mask_clipheight = undefined;
        self setotherent( undefined );
        self.carrier thread frag_crate_player_at_max_ammo( var_0 );
        self.carrier scripts\mp\equipment::allow_equipment( 1, "carriable" );

        if ( !isai( self.carrier ) )
        {
            self.carrier notifyonplayercommandremove( "lethal_attempt_carriable", "+frag" );
            self.carrier notifyonplayercommandremove( "lethal_attempt_carriable", "+smoke" );
            self.carrier notifyonplayercommandremove( "fuse_attempt_carriable", "+speed_throw" );

            if ( !self.carrier isconsoleplayer() )
                self.carrier notifyonplayercommandremove( "fuse_attempt_carriable", "+toggleads_throw" );
        }

        self.carrier scripts\common\utility::allow_supers( 1 );
        self.carrier allowmelee( 1 );
        self.carrier allowsupersprint( 1 );
        self.carrier scripts\common\utility::allow_prone( 1, "carriable" );
        self.carrier = undefined;
    }
}

frag_crate_player_at_max_ammo( var_0 )
{
    self endon( "death_or_disconnect" );

    foreach ( var_2 in level.forest_barrels )
    {
        if ( self getcurrentweapon() == var_2._id_13CB5 )
        {
            if ( self getweaponammoclip( var_2._id_13CB5 ) == 0 )
            {
                wait 0.6;

                if ( isdefined( self._id_12138 ) )
                    self switchtoweaponimmediate( self._id_12138 );
            }
            else
            {
                if ( isdefined( var_0 ) )
                    wait( var_0 );

                if ( isdefined( self._id_12138 ) )
                    self switchtoweaponimmediate( self._id_12138 );
            }

            self takeweapon( var_2._id_13CB5 );
            break;
        }
        else if ( self hasweapon( var_2._id_13CB5 ) )
        {
            self takeweapon( var_2._id_13CB5 );
            break;
        }
    }
}

hvt_key_dropped( var_0, var_1 )
{
    if ( !isdefined( level._id_137AA ) )
        return;

    var_2 = var_1 * var_1;

    foreach ( var_4 in level._id_137AA )
    {
        if ( isdefined( var_4 ) && !var_4 islinked() && distance2dsquared( var_4.origin, var_0 ) > var_2 )
            var_4 delete();
    }
}

force_var_to_array()
{
    self endon( "death" );
    self endon( "explode" );
    self endon( "pickup" );
    var_0 = getdvarfloat( "scr_br_carriable_inactive_delete_time", 300 );

    if ( var_0 == 0 )
        return;

    wait( var_0 );
    self delete();
}

force_group_thermites( var_0 )
{
    var_1 = self.origin + ( 0, 0, -96 );
    var_2 = physicstrace( self.origin, var_1 );
    var_3 = var_2 == var_1;
    var_4 = "detonateGround";

    if ( var_3 )
        var_4 = "detonateAir";

    var_5 = easepower( "br_carriable_explosion_gasoline", self.origin, self.angles );
    var_5 setscriptablepartstate( "carrible_explode_base", var_4 );
    var_5 thread getbeingrevivedinternal( 5 );
    self radiusdamage( self.origin, 300, 200, 200, self, "MOD_EXPLOSIVE", "gasoline_can_mp" );

    if ( istrue( self.spawnglobalscriptabledelayed ) )
        var_5 _findgivearmoramountanddropleftovers::nvgvisionsetoverride( self );
}

force_interrupt_current_combat_action()
{
    var_0 = 2;
    var_1 = self.origin + ( 0, 0, -96 );
    var_2 = physicstrace( self.origin, var_1 );
    var_3 = var_2 == var_1;
    var_4 = "detonateGround";

    if ( var_3 )
        var_4 = "detonateAir";

    var_5 = easepower( "br_carriable_explosion_propane", self.origin, self.angles );
    var_5 setscriptablepartstate( "carrible_explode_base", var_4 );
    var_5 thread getbeingrevivedinternal( 5 );
    var_6 = spawn( "script_origin", self.origin );
    var_6.angles = self.angles;
    var_6.owner = self.owner;
    var_6.team = self.team;
    var_6.script_noteworthy = "fake_molotov";
    var_6.weapon_name = "gas_can_mp";

    if ( isplayer( self.owner ) )
        self radiusdamage( var_6.origin, 250, 400, 1, self.owner, "MOD_EXPLOSIVE", "c4_mp_p" );
    else
        self radiusdamage( var_6.origin, 250, 400, 1, undefined, "MOD_EXPLOSIVE", "c4_mp_p" );

    var_7 = "gas_can_fire_spout";
    var_8 = 3;
    level.forceunsetdemeanor = scripts\engine\utility::array_removedead( level.forceunsetdemeanor );
    var_9 = 0;

    for ( var_10 = playerplunderpickupcallback( var_8 ); var_9 < var_8; var_9++ )
    {
        if ( level.forceunsetdemeanor.size >= 12 )
            break;

        var_11 = magicgrenademanual( var_7, self.origin + var_10[var_9] * 0.02, var_10[var_9], 5 );
        level.forceunsetdemeanor = scripts\engine\utility::array_add( level.forceunsetdemeanor, var_11 );
        var_11 thread mp_rust_patch( var_6 );
    }

    if ( var_2 != var_1 )
        var_6 scripts\mp\equipment\molotov::molotov_simulate_impact( var_6, var_2, ( 0, 0, 0 ), undefined, ( 0, 0, 0 ), gettime() );
    else if ( level.forceunsetdemeanor.size < 12 )
    {
        var_11 = magicgrenademanual( var_7, self.origin + ( 0, 0, -30 ), ( 0, 0, -200 ), 5 );
        level.forceunsetdemeanor = scripts\engine\utility::array_add( level.forceunsetdemeanor, var_11 );
        var_11 thread mp_rust_patch( var_6 );
    }

    var_6 thread getbeingrevivedinternal( 30 );
}

playerplunderpickupcallback( var_0 )
{
    var_1 = [];

    if ( var_0 <= 0 )
        return var_1;

    var_2 = 360.0 / var_0;
    var_3 = randomfloatrange( -1 * var_2, var_2 );

    for ( var_4 = 0; var_4 < var_0; var_4++ )
    {
        var_5 = randomfloatrange( -0.5 * var_2, 0.5 * var_2 );
        var_6 = var_4 * var_2 + var_5 + var_3;
        var_7 = vectornormalize( rotatepointaroundvector( ( 0, 0, 1 ), ( 0, 0.7, 0.7 ), var_6 ) );
        var_8 = randomfloatrange( 250, 400 );
        var_1[var_1.size] = var_7 * var_8;
    }

    return var_1;
}

mp_rust_patch( var_0 )
{
    self endon( "death" );
    self endon( "missile_dest_failed" );
    self waittill( "missile_stuck", var_1 );
    var_0 scripts\mp\equipment\molotov::molotov_simulate_impact( var_0, self.origin, var_0.angles, undefined, ( 0, 0, 0 ), gettime() );
    level.forceunsetdemeanor = scripts\engine\utility::array_remove( level.forceunsetdemeanor, self );
    self delete();
}

getbeingrevivedinternal( var_0 )
{
    self endon( "death" );

    if ( !isdefined( var_0 ) )
        var_0 = 5;

    wait( var_0 );

    if ( isdefined( self ) )
    {
        if ( isent( self ) )
            self delete();
        else
            self freescriptable();
    }
}

force_interrupt_all_current_combat_actions()
{
    var_0 = 2;
    var_1 = self.origin + ( 0, 0, -96 );
    var_2 = physicstrace( self.origin, var_1 );
    var_3 = var_2 == var_1;
    var_4 = self.owner;

    if ( !isplayer( var_4 ) )
        var_4 = undefined;

    var_5 = "detonateGround";

    if ( var_3 )
        var_5 = "detonateAir";

    var_6 = easepower( "br_carriable_explosion_neurotoxin", self.origin, self.angles );
    var_6 setscriptablepartstate( "carrible_explode_base", var_5 );

    if ( !isdefined( var_4 ) )
        radiusdamage( self.origin, 150, 250, 1, undefined, "MOD_EXPLOSIVE" );
    else
        self radiusdamage( self.origin, 150, 250, 1, var_4, "MOD_EXPLOSIVE" );

    thread warningstartcallbacks( var_6, var_3, var_4 );
}

warningstartcallbacks( var_0, var_1, var_2 )
{
    var_3 = self.forest_barrel_damage_watch;
    var_4 = self.origin;
    var_5 = "groundLoop";

    if ( var_1 )
        var_5 = "airLoop";

    var_0 setscriptablepartstate( "carrible_neurotoxin_loop", var_5 );
    level are_players_nearby_turret( var_4, var_2 );
    wait( float( 30.0 ) );

    if ( isdefined( var_0 ) )
    {
        var_0 setscriptablepartstate( "carrible_neurotoxin_loop", "gasEnd" );
        var_0 thread getbeingrevivedinternal( 5 );
    }
}

are_players_nearby_turret( var_0, var_1 )
{
    if ( !isdefined( level.apc_rus_initcollision ) )
        level.apc_rus_initcollision = [];

    var_2 = spawnstruct();
    var_2.getpreviousplacement = var_0;
    var_2.hudglobalkillcount = gettime() + 30000;
    var_2.owner = var_1;
    level.apc_rus_initcollision = scripts\engine\utility::array_add( level.apc_rus_initcollision, var_2 );

    if ( level.apc_rus_initcollision.size > 1 )
        return;

    level thread warp_player_debug();
}

warp_player_debug()
{
    var_0 = 250;
    var_1 = 9;

    while ( level.apc_rus_initcollision.size > 0 )
    {
        var_2 = [];
        var_3 = [];

        foreach ( var_5 in level.apc_rus_initcollision )
        {
            if ( gettime() > var_5.hudglobalkillcount )
            {
                var_2[var_2.size] = var_5;
                continue;
            }

            var_6 = scripts\mp\utility\player::getplayersinradius( var_5.getpreviousplacement, var_0 );

            foreach ( var_8 in var_6 )
                var_8.getprophealth = var_5;

            var_3 = scripts\engine\utility::array_combine_unique( var_3, var_6 );
        }

        foreach ( var_8 in var_3 )
        {
            if ( !isdefined( var_8.getprophealth ) )
                continue;

            var_12 = 1;

            if ( !isdefined( var_8.getprophealth.owner ) )
            {
                var_8.getprophealth.owner = var_8;
                var_12 = 0;
            }

            if ( isdefined( var_8.getprophealth.owner.team ) && var_8.team == var_8.getprophealth.owner.team && var_8 != var_8.getprophealth.owner )
            {
                var_8.getprophealth = undefined;
                continue;
            }

            javelin_ammo_refill_use_monitor( var_8, var_8.getprophealth.owner, var_1 );

            if ( !var_12 )
                var_8.getprophealth.owner = undefined;

            var_8.getprophealth = undefined;
        }

        if ( var_2.size > 0 )
            level.apc_rus_initcollision = scripts\engine\utility::array_remove_array( level.apc_rus_initcollision, var_2 );

        wait 1;
    }
}

javelin_ammo_refill_use_monitor( var_0, var_1, var_2 )
{
    if ( scripts\cp_mp\gasmask::hasgasmask( var_0 ) )
    {
        var_0 thread warroomtvs();

        if ( isdefined( level.oscope_temps_think ) )
            var_0 [[ level.oscope_temps_think ]]( "carriable_neurotoxin" );
        else if ( !istrue( var_0.gasmaskequipped ) )
            var_0 notify( "toggle_gasmask" );

        var_0 scripts\cp_mp\gasmask::processdamage( var_2 );
    }
    else
    {
        var_0 dodamage( var_2, var_0.origin, var_1, undefined, "MOD_TRIGGER_HURT", "danger_circle_br" );

        if ( var_0 scripts\mp\gametypes\br_public.gsc::hasarmor() )
            var_0 scripts\mp\gametypes\br_public.gsc::damagearmor( var_2 );

        var_0 scripts\mp\gametypes\br_circle.gsc::_id_135A0();
    }
}

warroomtvs()
{
    var_0 = self;
    var_1 = 1100;

    if ( !isdefined( var_0.shouldhumanspawntags ) )
    {
        var_0.shouldhumanspawntags = gettime();

        while ( !istrue( var_0.gasmaskequipped ) || gettime() < var_0.shouldhumanspawntags + var_1 )
            wait 0.5;

        var_0.shouldhumanspawntags = undefined;

        if ( isdefined( level.oscope_sign_think ) )
            var_0 [[ level.oscope_sign_think ]]( "carriable_neurotoxin" );
        else if ( istrue( var_0.gasmaskequipped ) )
            var_0 notify( "toggle_gasmask" );

        return;
    }

    var_0.shouldhumanspawntags = gettime();
}

forcehideottimer( var_0 )
{
    self endon( "death_or_disconnect" );
    self endon( "ascender_solo_cancel" );
    self endon( "last_stand_start" );

    if ( level.ascendstructs[var_0.target].dir == 0 )
    {
        self.footprint_mask_clipheight forcefail( 0 );
        scripts\engine\utility::waittill_notify_or_timeout( "weapon_change", 1 );
        return 0;
    }

    thread scripts\mp\utility\infilexfil::infil_player_rig_updated( "player", self.origin, self.angles );
    self.player_rig hide();
    var_1 = spawnstruct();
    var_1.leaderboarddata = self.footprint_mask_clipheight;
    var_1.player = self;
    var_1.shouldreflect = var_0;
    var_1.bshouldreturnvalue = level.ascendstructs[var_0.target];
    self.shouldskiplaststand = 1;
    var_1.bshouldreturnvalue.tag_to_shoot_from = gettime();
    var_2 = scripts\engine\utility::drop_to_ground( var_1.bshouldreturnvalue.origin, 100, -250 );
    var_1.brtdm_config = spawn( "script_model", var_2 );
    var_1.brtdm_config setmodel( "tag_origin" );
    level.helistoreplunder++;

    if ( self getstance() != "stand" )
        self setstance( "stand" );

    scripts\common\utility::allow_execution_victim( 0 );
    scripts\common\utility::allow_usability( 0 );
    scripts\common\utility::allow_melee( 0 );
    scripts\common\utility::allow_ads( 0 );
    scripts\common\utility::allow_fire( 0 );

    if ( istrue( self.isjuggernaut ) )
    {
        if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "juggernaut", "canUseWeaponPickups" ) )
        {
            var_3 = self [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "juggernaut", "canUseWeaponPickups" ) ]]();

            if ( istrue( var_3 ) )
                self disableweaponswitch();
        }
    }
    else
    {
        scripts\common\utility::allow_killstreaks( 0 );
        self disableweaponswitch();
    }

    var_1.brtdm_config scripts\cp_mp\ent_manager::registerspawncount( 2 );
    var_1.bshouldreturnvalue.inuse = 1;
    self.usingascender = 1;
    var_4 = anglestoforward( var_1.bshouldreturnvalue.angles );
    var_5 = anglestoforward( self.angles );
    var_6 = vectordot( var_5, var_4 );
    var_7 = 0;

    if ( var_6 < 0.5 )
    {
        var_8 = vectorcross( var_5, var_4 );

        if ( var_8[2] < 0 )
            var_7 = 120;
        else
            var_7 = 240;
    }

    var_9 = ( 0, var_7, 0 );
    var_1.brtdm_config dontinterpolate();
    var_1.brtdm_config.origin = var_2;
    var_1.brtdm_config.angles = var_1.bshouldreturnvalue.angles + var_9;
    var_1.brtruck_initdialog = spawn( "script_model", var_1.bshouldreturnvalue.origin );
    var_1.brtruck_initdialog setmodel( "misc_wm_ascender_ch3" );
    var_1.brtruck_initdialog hide();
    var_1.brtruck_initexternalfeatures = spawn( "script_model", var_1.bshouldreturnvalue.origin );
    var_1.brtruck_initexternalfeatures setmodel( "misc_wm_ascender_ch3" );
    var_1.brtruck_initexternalfeatures hide();
    self setclientomnvar( "ui_br_gas_can_status", 0 );
    var_1 thread _id_11DF6();
    var_1 thread forcestuckdamageclear();
    var_1 _id_11CC8();
    var_1._id_13A0D = var_1.player.player_rig gettagorigin( "tag_weapon" );
    var_1 thread cleanupascenduse();

    if ( isdefined( var_1.leaderboarddata ) )
        var_1 thread forbiddencachespawns();

    return 1;
}

forbiddencachespawns()
{
    self.brtdm_config endon( "death" );

    if ( isdefined( self.player ) )
        self.brtruck_initexternalfeatures showtoplayer( self.player );

    c130_drop();
    var_0 = distance( self.bshouldreturnvalue.ascendstructend.origin, self.bshouldreturnvalue.origin );
    var_1 = var_0 / 200;
    var_2 = scripts\cp_mp\auto_ascender::player_waittilljumpedfromc130() * var_1;
    var_3 = scripts\cp_mp\auto_ascender::player_xp() * var_1;
    self.brtdm_config moveto( self.bshouldreturnvalue.ascendstructend.origin, var_1, var_2, var_3 );
    childthread force_ai_to_drop_thermites();
    wait( var_1 );

    if ( isdefined( self.leaderplunderstring ) )
    {
        playfx( scripts\engine\utility::getfx( "vfx_br3_rope_fire" ), self.brtdm_config.origin + ( 0, 0, 70 ), anglestoup( self.brtdm_config.angles ), anglestoforward( self.brtdm_config.angles ) );
        self.leaderplunderstring thread force_maze_ai_state( 1 );
    }

    getaverageangularvelocity();
}

force_ai_to_drop_thermites()
{
    playfx( scripts\engine\utility::getfx( "vfx_br3_rope_fire" ), self.brtdm_config.origin + ( 0, 0, 70 ), anglestoup( self.brtdm_config.angles ), anglestoforward( self.brtdm_config.angles ) );
    wait 1;

    for (;;)
    {
        wait( randomfloatrange( 0.05, 1.0 ) );
        playfx( scripts\engine\utility::getfx( "vfx_br3_rope_fire" ), self.brtdm_config.origin + ( 0, 0, randomfloatrange( 0, 70 ) ), anglestoup( self.brtdm_config.angles ), anglestoforward( self.brtdm_config.angles ) );
    }
}

c130_drop()
{
    self.leaderplunderstring = spawn( "script_model", self._id_13A0D );
    self.leaderplunderstring.angles = ( 0, self.leaderboarddata.angles[1], 0 );
    self.leaderplunderstring setmodel( self.leaderboarddata.forest_barrel_damage_watch.modelname );
    self.leaderplunderstring setcandamage( 1 );
    self.leaderplunderstring.health = 999999;
    self.leaderplunderstring.ownerteam = "any";
    self.leaderplunderstring.forest_barrel_damage_watch = self.leaderboarddata.forest_barrel_damage_watch;
    self.leaderplunderstring.owner = self.leaderboarddata.owner;
    self.leaderplunderstring.onlyexecutefromthefront = 1;
    self.leaderplunderstring thread force_dismount();
    self.leaderplunderstring thread force_spawn_all_dead_players();
    self.leaderplunderstring linkto( self.brtdm_config );
    self.leaderboarddata forest_fire_setup();
    self.leaderboarddata delete();
    thread brvalidatekillcam();
}

_id_11CC8()
{
    self.player endon( "death_or_disconnect" );
    self.player endon( "ascender_solo_cancel" );
    self.player endon( "last_stand_start" );
    self.brtruck_initdialog.animname = "device";
    self.brtruck_initdialog scripts\common\anim::setanimtree();
    self.brtruck_initexternalfeatures.animname = "device";
    self.brtruck_initexternalfeatures scripts\common\anim::setanimtree();
    var_0 = rotatevector( ( -32.415, 0, 0 ), self.brtdm_config.angles );
    var_1 = 0.4;
    self.player.player_rig moveto( self.brtdm_config.origin + var_0, var_1, 0.1, 0.1 );
    var_2 = vectornormalize( var_0 * -1 );
    var_3 = scripts\cp_mp\auto_ascender::vectortoanglessafe( var_2, ( 0, 0, 1 ) );
    self.player.player_rig rotateto( var_3, var_1, 0.1, 0.1 );

    if ( istrue( self.leaderboarddata.onlyexecutefromthefront ) )
    {
        self.leaderboarddata endon( "cancel_fuse" );
        wait( var_1 );
    }
    else
    {
        self.player force_remove_stim( self.leaderboarddata );
        self.leaderboarddata.onlyexecutefromthefront = 1;
    }

    if ( !istrue( self.isjuggernaut ) )
        self.player disableoffhandweapons();

    self.player pulsing();
    self.brtruck_initexternalfeatures show();
    self.brtruck_initexternalfeatures hidefromplayer( self.player );
    self.brtruck_initdialog show();
    self.brtruck_initdialog showonlytoplayer( self.player );
    self.brtruck_initdialog linkto( self.player.player_rig, "tag_accessory_right", ( 0, 0, 0 ), ( 0, 0, 0 ) );
    self.brtruck_initexternalfeatures linkto( self.brtdm_config, "tag_origin", ( 0, 0, 0 ), ( 0, 0, 0 ) );
    self.player.player_rig linkto( self.brtdm_config, "tag_origin", ( 0, 0, 0 ), ( 0, 0, 0 ) );
    self.player.player_rig showonlytoplayer( self.player );
    var_4 = self.player.player_rig gettagorigin( "tag_weapon" );
    var_5 = self.player.player_rig gettagangles( "tag_weapon" );
    self.leaderinteractionthink = spawn( "script_model", var_4 );
    self.leaderinteractionthink.angles = var_5;
    self.leaderinteractionthink setmodel( self.leaderboarddata.forest_barrel_damage_watch.modelname );
    self.leaderinteractionthink showonlytoplayer( self.player );
    self.leaderinteractionthink.forest_barrel_damage_watch = self.leaderboarddata.forest_barrel_damage_watch;
    self.leaderinteractionthink.owner = self.leaderboarddata.owner;
    self.leaderinteractionthink.onlyexecutefromthefront = 1;
    self.leaderinteractionthink linkto( self.player.player_rig, "tag_weapon" );
    self.leaderinteractionthink thread force_spawn_all_dead_players();
    self.brtdm_config scripts\common\anim::anim_first_frame_solo( self.player.player_rig, "carriable_ascender_attach" );
    self.brtdm_config thread scripts\mp\anim::anim_player_solo( self.player, self.player.player_rig, "carriable_ascender_attach" );
    self.brtdm_config thread scripts\common\anim::anim_single_solo( self.brtruck_initexternalfeatures, "carriable_ascender_device_attach" );
    var_6 = getanimlength( level.scr_anim["player"]["carriable_ascender_attach"] );
    wait( var_6 );
    self.player takeweapon( self.player.currentweapon );
    self.player switchtoweaponimmediate( self.player._id_12138 );
    self.player notify( "ascend_solo_complete" );
    self.player notify( "drop_object" );
}

_id_11DF6()
{
    level endon( "game_ended" );
    self.player endon( "ascend_complete" );
    self.player endon( "ascend_solo_complete" );
    self.player endon( "ascender_cancel" );
    self.player waittill( "death_or_disconnect" );

    if ( isdefined( self.player ) )
        self.player stopanimscriptsceneevent();

    thread cleanupascenduse();
    thread getaverageangularvelocity();

    if ( isdefined( self.leaderboarddata ) )
    {
        self.leaderboarddata thread forcefail( 0 );

        if ( istrue( self.leaderboarddata.onlyexecutefromthefront ) )
            self.leaderboarddata thread force_teleport_player_into_plane( self.player );
    }

    if ( isdefined( self.player ) )
        self.player notify( "ascender_cancel" );
}

forcestuckdamageclear()
{
    level endon( "game_ended" );
    self.player endon( "ascend_complete" );
    self.player endon( "ascend_solo_complete" );
    self.player endon( "ascender_cancel" );
    self.leaderboarddata waittill( "explode" );

    if ( isdefined( self.player ) )
        self.player stopanimscriptsceneevent();

    cleanupascenduse();
    getaverageangularvelocity();

    if ( isdefined( self.player ) )
        self.player notify( "ascender_cancel" );
}

brvalidatekillcam()
{
    level endon( "game_ended" );
    self.leaderplunderstring waittill( "explode" );
    getaverageangularvelocity();
}

cleanupascenduse()
{
    if ( self.bshouldreturnvalue.ascender.size == 1 )
        self.bshouldreturnvalue.inuse = 0;

    if ( isdefined( self.player ) && istrue( self.player.usingascender ) )
    {
        self.player.usingascender = 0;
        self.player.tag_convoy_with_objectives = gettime();
        self.player scripts\common\utility::allow_usability( 1 );
        self.player.shouldskiplaststand = undefined;
        self.player scripts\common\utility::allow_execution_victim( 1 );
        self.player scripts\common\utility::allow_melee( 1 );
        self.player scripts\common\utility::allow_ads( 1 );
        self.player scripts\common\utility::allow_fire( 1 );

        if ( istrue( self.player.isjuggernaut ) )
        {
            if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "juggernaut", "canUseWeaponPickups" ) )
            {
                var_0 = self.player [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "juggernaut", "canUseWeaponPickups" ) ]]();

                if ( istrue( var_0 ) )
                    self.player enableweaponswitch();
            }
        }
        else if ( !istrue( self.player.inlaststand ) )
        {
            self.player enableoffhandweapons();
            self.player enableweaponswitch();
            self.player scripts\common\utility::allow_killstreaks( 1 );
        }
        else
            self.player thread scripts\cp_mp\auto_ascender::target_found_speed();

        self.player.player_rig unlink();
        self.player.footprint_mask_clipheight = undefined;
    }

    if ( isdefined( self.brtruck_initdialog ) )
    {
        self.brtruck_initdialog unlink();
        self.brtruck_initdialog delete();
        self.brtruck_initdialog = undefined;
    }

    if ( isdefined( self.leaderinteractionthink ) )
    {
        self.leaderinteractionthink delete();
        self.leaderinteractionthink = undefined;
    }

    waitframe();

    if ( isdefined( self.player ) )
    {
        if ( !self.player hasweapon( self.player.gunnlessweapon ) )
            self.player.gunnlessweapon = undefined;

        self.player thread scripts\mp\utility\infilexfil::takegunless();
        self.player notify( "remove_rig" );
    }
}

getaverageangularvelocity()
{
    if ( isdefined( self.brtruck_initexternalfeatures ) )
    {
        self.brtruck_initexternalfeatures unlink();
        self.brtruck_initexternalfeatures delete();
    }

    if ( isdefined( self.brtdm_config ) )
    {
        self.brtdm_config scripts\cp_mp\ent_manager::deregisterspawn();
        self.brtdm_config delete();
        level.helistoreplunder--;
    }
}

tripledefenderkill()
{
    self.brtdm_config endon( "death" );
    self.brtdm_config endon( "ascender_solo_loop_done" );
    var_0 = "ascender_ext_up_loop";
    var_1 = getanimlength( level.scr_anim["player"][var_0] );

    for (;;)
    {
        if ( !isdefined( self.brtdm_config ) )
            break;

        self.brtdm_config scripts\common\anim::anim_single_solo( self.brtruck_initexternalfeatures, var_0 + "_wm" );

        if ( !isdefined( var_1 ) || var_1 == 0 )
            break;

        wait( var_1 );
    }
}

pulsing()
{
    self endon( "death_or_disconnect" );

    if ( isdefined( self.gunnlessweapon ) )
        return;

    var_0 = getcompleteweaponname( "iw8_gunless_infil" );
    scripts\cp_mp\utility\inventory_utility::_giveweapon( var_0, undefined, undefined, 1 );

    if ( !scripts\common\utility::is_script_weapon_switch_allowed() )
        scripts\common\utility::allow_script_weapon_switch( 1 );

    if ( scripts\cp_mp\utility\inventory_utility::isanymonitoredweaponswitchinprogress() )
        self clearhighpriorityweapon( scripts\cp_mp\utility\inventory_utility::getcurrentmonitoredweaponswitchweapon() );

    self switchtoweaponimmediate( var_0 );
    self.gunnlessweapon = var_0;
    scripts\common\utility::allow_script_weapon_switch( 0 );
}
