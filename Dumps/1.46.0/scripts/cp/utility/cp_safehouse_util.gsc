// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

setteamplunderhud( var_0 )
{
    if ( !isdefined( level.heli_group_nextspawntime ) )
        level.heli_group_nextspawntime = [];
}

heli_hover_between_points( var_0 )
{
    self endon( "death_or_disconnect" );
    self endon( "faux_spawn" );
    self endon( "end_launcher" );
    var_1 = playerkilled_washitbyvehicle( var_0 );
    var_2 = playerkilledspawn( var_1 );
    thread heli_gunner_logic( var_0, var_1, var_2 );
}

getanimsforplanefacing()
{
    self waittill( "end_launcher" );
    wait 6;
    self notify( "cleanupImpactWatcher" );
}

heli_gunner_logic( var_0, var_1, var_2 )
{
    self notify( "cleanupImpactWatcher" );
    self endon( "disconnect" );
    self endon( "cleanupImpactWatcher" );
    childthread getanimsforplanefacing();

    for (;;)
    {
        self waittill( "bullet_first_impact", var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10 );

        if ( var_0 != var_7 )
            continue;

        var_11 = _id_12E13( var_9, var_8, var_1, var_7 );
        relic_amped_in_warning( var_11, var_3, var_4, var_10, var_5, var_6, var_7, var_8, var_9, var_2 );
    }
}

relic_amped_in_warning( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    if ( _id_12B3F( var_5, var_0, var_1, var_2 ) )
        _id_12372( var_0, var_4, var_7, var_8 );
    else if ( _id_12B15( var_1 ) )
    {
        var_0 delete();
        return;
    }
    else if ( _id_12B2C( var_1 ) )
        linktoent( var_0, var_1, var_2 );

    if ( isdefined( var_9 ) )
        [[ var_9 ]]( var_0, var_1, var_2, var_3, var_5 );
}

scriptgoalyaw( var_0, var_1, var_2, var_3, var_4 )
{

}

scriptmover_utils( var_0, var_1, var_2, var_3, var_4 )
{
    var_0 setscriptablepartstate( "effects", "impact" );

    if ( isdefined( var_1 ) && ( isplayer( var_1 ) || isagent( var_1 ) ) )
        var_5 = 1.1;
    else
        var_5 = 2;

    var_0.grenade = magicgrenademanual( "semtex_bolt_mp", var_0.origin, ( 0, 0, 0 ), var_5 );
    var_0.grenade.angles = var_0.angles;
    var_0.grenade linkto( var_0, "tag_origin" );
    thread mediumstatehealthratio( var_0, var_5 );
}

scurrentobjective( var_0, var_1, var_2, var_3, var_4 )
{
    var_0.surfacetype = var_4;
    var_0 setscriptablepartstate( "effects", "impact" );
    thread _id_132D8( var_0, var_1, var_2, var_3 );
    var_5 = 0;

    if ( isdefined( level._id_12ACA ) && [[ level._id_12ACA.docrossbowbolt ]]( var_0 ) )
        var_5 = 1;

    if ( !var_5 )
        thread _id_132D7( var_0, var_1 );

    thread _id_132D6( var_0, var_5 );
}

search_acceleration( var_0, var_1, var_2, var_3, var_4 )
{
    if ( _id_13100( var_1, var_4 ) )
    {
        var_0 setscriptablepartstate( "impact", "active" );

        if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "weapons", "gas_createTrigger" ) )
            thread [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "weapons", "gas_createTrigger" ) ]]( var_0.origin, self, 5, 0.45 );

        thread _id_130FF( var_0 );
    }
    else
        var_0 setscriptablepartstate( "impact", "dud" );
}

mediumstatehealthratio( var_0, var_1 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_0.grenade scripts\engine\utility::_id_13AF3( var_1, "explode" );
    var_0 setscriptablepartstate( "effects", "explode" );
    var_2 = getcompleteweaponname( "semtex_bolt_mp" );
    var_3 = getcompleteweaponname( "semtex_bolt_splash_mp" );
    var_2._id_11AF1 = var_0.weapon;
    var_3._id_11AF1 = var_0.weapon;

    if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
    {
        var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::obj_riverbed();
        var_0.stuckenemyentity dodamage( 110, var_0.origin, self, undefined, "MOD_EXPLOSIVE", var_2 );
        var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::obj_room_fire_01();
    }

    radiusdamage( var_0.origin, 160, 110, 35, self, "MOD_EXPLOSIVE", var_3 );
    wait 0.4;

    if ( !var_0 _id_13828() )
        return;

    var_0 delete();
}

_id_132D8( var_0, var_1, var_2, var_3 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_4 = getcompleteweaponname( "thermite_bolt_mp" );
    var_4._id_11AF1 = var_0.weapon;

    if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
    {
        if ( var_1 scripts\cp_mp\vehicles\vehicle::isvehicle() || isdefined( var_1.classname ) && var_1.classname == "misc_turret" )
            var_5 = 1.0;
        else
            var_5 = 0.25;

        var_6 = int( 4.5 / var_5 );

        while ( isdefined( var_1 ) && isdefined( var_0 ) && isalive( var_1 ) && var_6 > 0 )
        {
            var_1 scripts\cp_mp\utility\damage_utility::obj_riverbed();
            var_1 dodamage( 5, var_0.origin, self, var_0, "MOD_FIRE", var_4, var_3 );
            var_1 scripts\cp_mp\utility\damage_utility::obj_room_fire_01();
            var_6--;
            wait( var_5 );
        }
    }
}

_id_132D7( var_0, var_1 )
{
    self endon( "disconnect" );
    var_0 endon( "entitydeleted" );
    var_2 = int( 18.0 );
    var_3 = getcompleteweaponname( "thermite_bolt_radius_mp" );
    var_3._id_11AF1 = var_0.weapon;
    var_0._id_132DB = var_3.basename;

    while ( var_2 > 0 )
    {
        if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
            var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::adddamagemodifier( "thermiteBoltStuck", 0, 0, ::_id_132CF );

        var_0 radiusdamage( var_0.origin, 50, 5, 3, self, "MOD_FIRE", var_3 );

        if ( isdefined( var_0.stuckenemyentity ) && isalive( var_0.stuckenemyentity ) )
            var_0.stuckenemyentity scripts\cp_mp\utility\damage_utility::removedamagemodifier( "thermiteBoltStuck", 0 );

        var_2--;
        wait 0.25;
    }
}

_id_132CF( var_0, var_1, var_2, var_3, var_4, var_5, var_6 )
{
    if ( !isdefined( var_0 ) )
        return 1;

    if ( !isdefined( var_0._id_132DB ) || var_0._id_132DB != "thermite_bolt_radius_mp" )
        return 1;

    if ( !isdefined( var_0.stuckenemyentity ) || var_0.stuckenemyentity != var_2 )
        return 1;

    return 0;
}

_id_132D6( var_0, var_1 )
{
    var_0 endon( "entitydeleted" );

    if ( !var_1 )
    {
        wait 4.5;

        if ( !var_0 _id_13828() )
            return;

        var_0 setscriptablepartstate( "effects", "burnEnd" );
        wait( randomfloatrange( 0.3, 2.0 ) );

        if ( !var_0 _id_13828() )
            return;
    }

    var_0 circle_back_to_combat();
    var_0 setmodel( "weapon_wm_sn_crossbow_bolt_fire_static_dst" );
}

_id_130FF( var_0 )
{
    var_0 endon( "entitydeleted" );
    wait 0.5;

    if ( !var_0 _id_13828() )
        return;

    var_0 delete();
}

_id_13100( var_0, var_1 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( !isplayer( var_0 ) && !isagent( var_0 ) )
        return 0;

    if ( isdefined( var_0.team ) && self.team == var_0.team )
        return 0;

    if ( var_1 == "riotshield" )
        return 0;

    return 1;
}

_id_12E13( var_0, var_1, var_2, var_3 )
{
    var_4 = playerexecutionsdisable( var_2 );
    var_5 = spawn( "script_model", var_0 );
    var_5 setmodel( var_4 );
    var_5.angles = vectortoangles( var_1 );
    var_5 _id_1297A( var_2 );
    var_5.owner = self;
    var_5.br_practice_match = var_2;
    var_5.weapon = var_3;
    var_5.spawn_deceleration = 1;

    if ( _id_12B31( var_2 ) )
        var_5 = uniquelootcallbacks( var_5 );

    var_5 thread cinderblock_damage_monitor();
    unlockscriptabledoors( var_5 );
    return var_5;
}

_id_12B31( var_0 )
{
    if ( var_0 == "bolt_default" || var_0 == "bolt_stun" )
        return 1;

    return 0;
}

uniquelootcallbacks( var_0 )
{
    var_1 = var_0.origin + anglestoforward( var_0.angles ) * 15;
    var_2 = axistoangles( anglestoup( var_0.angles ), anglestoright( var_0.angles ), anglestoforward( var_0.angles ) );
    var_3 = spawn( "trigger_rotatable_radius", var_1, 0, 64, 79 );
    var_3.angles = var_2;
    var_3.targetname = "bolt_pickup";
    var_3 enablelinkto();
    var_3 linkto( var_0 );
    var_0._id_11C4D = var_3;
    var_0 thread chosencodephones();
    return var_0;
}

_id_124B4( var_0 )
{
    var_0 notify( "removePickup" );

    if ( isdefined( var_0._id_11C4D ) )
        var_0._id_11C4D delete();
}

chosencodephones()
{
    self endon( "entitydeleted" );
    self endon( "removePickup" );
    wait 2;

    for (;;)
    {
        self._id_11C4D waittill( "trigger", var_0 );

        if ( !isplayer( var_0 ) )
            continue;

        if ( !var_0 scripts\cp_mp\utility\player_utility::_isalive() )
            continue;

        if ( isdefined( self.stuckenemyentity ) && isalive( self.stuckenemyentity ) )
            continue;

        var_1 = player_isusingtacmap( var_0 getweaponslistprimaries() );

        if ( !isdefined( var_1 ) )
            continue;

        if ( var_0 chosen_airlock_door( var_1 ) )
            self delete();
    }
}

player_isusingtacmap( var_0 )
{
    foreach ( var_2 in var_0 )
    {
        if ( var_2 hasattachment( "ammo_crossbow" ) && self.br_practice_match == "bolt_default" )
            return var_2;

        if ( var_2 hasattachment( "mag_sn_t9crossbow" ) && self.br_practice_match == "bolt_default" )
            return var_2;

        if ( var_2 hasattachment( "boltstun_crossbow" ) && self.br_practice_match == "bolt_stun" )
            return var_2;
    }

    return undefined;
}

chosen_airlock_door( var_0 )
{
    var_1 = weaponmaxammo( var_0 );
    var_2 = self getweaponammostock( var_0 );

    if ( var_2 >= var_1 )
        return 0;

    var_3 = int( min( var_1, var_2 + 1 ) );
    self setweaponammostock( var_0, var_3 );

    if ( scripts\cp_mp\utility\script_utility::issharedfuncdefined( "damage", "hudIconType" ) )
        self [[ scripts\cp_mp\utility\script_utility::getsharedfunc( "damage", "hudIconType" ) ]]( "crossbowbolt" );

    return 1;
}

_id_1297A( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_fire":
            thread _id_1297B( 5 );
            self.initoperationcratedata = 0;
            break;
        case "bolt_explo":
            self.initoperationcratedata = 0;
            break;
        case "bolt_stun":
            thread _id_1297B( 0.5 );
            self.initoperationcratedata = 0;
            break;
        default:
            self.initoperationcratedata = 1;
    }
}

_id_1297B( var_0 )
{
    self endon( "entitydeleted" );
    wait( var_0 );
    self.initoperationcratedata = 1;
    unlockscriptabledoors();
}

unlockscriptabledoors( var_0 )
{
    if ( isdefined( var_0 ) )
        var_1 = [ var_0 ];
    else
        var_1 = [];

    foreach ( var_3 in level.heli_group_nextspawntime )
    {
        if ( !isdefined( var_3 ) )
            continue;

        if ( isdefined( var_3 ) )
        {
            if ( var_1.size >= 24 && var_3.initoperationcratedata )
            {
                var_3 delete();
                continue;
            }

            var_1[var_1.size] = var_3;
        }
    }

    level.heli_group_nextspawntime = var_1;
}

playerkilled_washitbyvehicle( var_0 )
{
    var_1 = "";

    foreach ( var_3 in var_0.attachments )
    {
        if ( issubstr( var_3, "bolt" ) )
        {
            var_1 = var_3;
            break;
        }
    }

    switch ( var_1 )
    {
        case "boltcarbon_crossbow":
            return "bolt_carbon";
        case "boltexplo_crossbow":
            return "bolt_explo";
        case "boltfire_crossbow":
            return "bolt_fire";
        case "boltstun_crossbow":
            return "bolt_stun";
        default:
            return "bolt_default";
    }
}

playerkilledspawn( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_carbon":
            return ::scriptgoalyaw;
        case "bolt_explo":
            return ::scriptmover_utils;
        case "bolt_fire":
            return ::scurrentobjective;
        case "bolt_stun":
            return ::search_acceleration;
        default:
            return;
    }
}

playerexecutionsdisable( var_0 )
{
    switch ( var_0 )
    {
        case "bolt_carbon":
            return "weapon_wm_sn_crossbow_bolt_carbon_static";
        case "bolt_explo":
            return "weapon_wm_sn_crossbow_bolt_explosive_static";
        case "bolt_fire":
            return "weapon_wm_sn_crossbow_bolt_fire_static";
        case "bolt_stun":
            return "weapon_wm_sn_crossbow_bolt_stun_static";
        default:
            return "weapon_wm_sn_crossbow_bolt_static";
    }
}

_id_12B3F( var_0, var_1, var_2, var_3 )
{
    if ( !isdefined( var_2 ) && isdefined( var_3 ) )
        return 1;

    if ( !isdefined( var_0 ) )
        return 0;

    switch ( var_0 )
    {
        case "glass_solid":
        case "glass_pane":
            return 1;
    }

    if ( var_1.br_practice_match == "bolt_explo" )
        return 0;

    if ( spawnselectionmarker( var_2 ) )
        return 1;

    if ( spawnselectionafktime( var_2 ) )
        return 1;

    switch ( var_0 )
    {
        case "asphalt_wet":
        case "asphalt_dry":
        case "riotshield":
            return 1;
        case "metal_car":
        case "metal_tank":
        case "metal_helicopter":
        case "metal_thin":
        case "metal_thick":
        case "metal_grate":
            if ( var_1.br_practice_match == "bolt_fire" )
                return 0;
            else
                return 1;
        default:
            return 0;
    }
}

_id_12372( var_0, var_1, var_2, var_3 )
{
    var_4 = scripts\engine\math::vector_reflect( var_2, var_1 );
    var_5 = abs( vectordot( var_2, var_1 ) );
    var_6 = scripts\engine\math::factor_value( 2300, 1000, var_5 );
    var_4 = var_4 * var_6;
    var_0 physicslaunchserver( var_3, var_4 );
}

_id_12B15( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isagent( var_0 ) && var_0 so_endgame() && !isalive( var_0 ) && !isdefined( var_0 getcorpseentity() ) )
        return 1;

    return 0;
}

_id_12B2C( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.classname ) && var_0.classname == "weapon_scavenger_bag_mp" )
        return 0;

    return 1;
}

so_endgame()
{
    return istrue( isdefined( self.unittype ) && self.unittype == "suicidebomber" );
}

linktoent( var_0, var_1, var_2 )
{
    if ( ( isplayer( var_1 ) || isagent( var_1 ) ) && !isalive( var_1 ) )
    {
        var_3 = var_1 getcorpseentity();

        if ( isdefined( var_3 ) )
            var_1 = var_3;
    }

    if ( isplayer( var_1 ) && var_0.br_practice_match != "bolt_stun" )
    {
        var_0 hidefromplayer( var_1 );

        if ( isdefined( var_0._id_11C4D ) )
            var_0._id_11C4D hidefromplayer( var_1 );
    }

    if ( isdefined( var_2 ) )
        var_0 linkto( var_1, var_2 );
    else
        var_0 linkto( var_1 );

    if ( failtimedrunquest( var_1 ) )
    {
        var_0.stuckenemyentity = var_1;
        var_0 thread _id_124C5( var_1 );
    }

    var_0 notsolid();
    var_0 thread circle_defaults( var_1 );
    var_0 thread chuckerlogic( var_1, "vehicle_deleted" );
    var_0 thread chuckerlogic( var_1, "detonated" );
    var_0 thread chuckerlogic( var_1, "beginC130" );
}

_id_13828()
{
    if ( isdefined( self ) && istrue( self.spawn_deceleration ) )
        return 1;
    else
    {

    }
}

_id_124C5( var_0 )
{
    self endon( "entitydeleted" );
    var_0 scripts\engine\utility::_id_13ADA( "entitydeleted", "death", "disconnect" );

    if ( !_id_13828() )
        return;

    self.stuckenemyentity = undefined;

    if ( isdefined( var_0 ) && isdefined( var_0.nocorpse ) )
        self delete();
}

circle_defaults( var_0 )
{
    self endon( "entitydeleted" );
    var_0 scripts\engine\utility::_id_13AD9( "entitydeleted", "disconnect" );

    if ( !_id_13828() )
        return;

    circle_back_to_combat();
}

circle_back_to_combat( var_0 )
{
    if ( !isdefined( var_0 ) )
        var_0 = ( 0, 0, 100 );

    if ( self islinked() )
        self unlink();

    if ( !isdefined( self.model ) || self.model == "tag_origin" )
        return;

    self solid();
    self physicslaunchserver( self.origin, var_0 );
}

chuckerlogic( var_0, var_1 )
{
    self endon( "entitydeleted" );
    var_0 waittill( var_1 );

    if ( !_id_13828() )
        return;

    self delete();
}

cinderblock_damage_monitor()
{
    self waittill( "entitydeleted" );

    if ( isdefined( self._id_11C4D ) )
        self._id_11C4D delete();

    if ( isdefined( self.grenade ) )
        self.grenade delete();
}

spawnselectionmarker( var_0 )
{
    if ( !isplayer( var_0 ) )
        return 0;

    if ( scripts\mp\utility\player::isenemy( var_0 ) )
        return 0;
    else
        return 1;
}

spawnselectionafktime( var_0 )
{
    if ( !isagent( var_0 ) )
        return 0;

    if ( isdefined( var_0.agentteam ) && self.team == var_0.agentteam )
        return 1;

    return 0;
}

failtimedrunquest( var_0 )
{
    var_1 = 0;

    if ( isplayer( var_0 ) || isagent( var_0 ) )
        var_1 = 1;

    if ( var_0 scripts\cp_mp\vehicles\vehicle::isvehicle() )
        var_1 = 1;

    if ( isdefined( var_0.classname ) )
    {
        if ( var_0.classname == "misc_turret" )
            var_1 = 1;

        if ( var_0.classname == "script_model" )
        {
            if ( isdefined( var_0.streakinfo ) && ( var_0.streakinfo.streakname == "uav" || var_0.streakinfo.streakname == "gunship" ) )
                var_1 = 1;
        }
    }

    if ( isdefined( var_0.equipmentref ) )
    {
        if ( var_0.equipmentref == "equip_tac_cover" )
            var_1 = 1;
    }

    return var_1;
}
