// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    if ( scripts\mp\utility\game::getgametype() != "br" )
        return;

    _id_123B8( "br_mastery_fiveContracts", ::nearby_ai_combat_via_grenade );
    _id_123B8( "br_mastery_pointBlank_airstrike", ::_id_120B0 );
    _id_123B8( "br_mastery_pointBlank_tomahawk", ::_id_120B1 );
    _id_123B8( "br_mastery_c4VehicleMultKill", ::enemy_is_visible );
    _id_123B8( "br_mastery_ghostRideWhip", ::propclones );
    _id_123B8( "br_mastery_roundKillExecute", ::_id_1263E );
    _id_123B8( "br_mastery_travelogue", ::_id_1349A );
}

_id_123B8( var_0, var_1 )
{
    if ( !isdefined( level.create_race ) )
        level.create_race = [];

    level.create_race[var_0] = var_1;
}

fuelsequencestability( var_0, var_1 )
{
    if ( !isdefined( level.create_race ) )
        return;

    if ( istrue( level.fulton_actors_players ) )
        return;

    var_2 = level.create_race[var_0];

    if ( isdefined( var_2 ) )
        self thread [[ var_2 ]]( var_0, var_1 );
}

cancelgivequest( var_0 )
{
    if ( getdvarint( "scr_br_challenge_debug", 0 ) )
        iprintlnbold( "Mastery Challenge: " + var_0 + " completed!" );

    scripts\cp\utility\cp_controlled_callbacks::zone_stompeenemyprogressupdate( var_0 );
}

update_hint_logic_juggernaut( var_0 )
{
    if ( !isdefined( level.teamdata[var_0] ) )
        return;

    foreach ( var_2 in level.teamdata[var_0]["players"] )
        _id_13499( var_2 );

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) )
    {
        _id_136F4( var_0 );
        wassquadspawned( var_0 );
    }
}

_id_136F4( var_0 )
{
    if ( !isdefined( var_0 ) )
        return;

    var_1 = 0;
    var_2 = 0;
    var_3 = max( 1, level.maxteamsize );

    foreach ( var_5 in level.teamdata[var_0]["players"] )
    {
        var_1 = var_1 + var_5.deaths;

        if ( isdefined( var_5.validtousesticker ) )
            var_1 = var_1 + var_5.validtousesticker;

        if ( scripts\mp\utility\player::isreallyalive( var_5 ) )
            var_2++;
    }

    if ( var_1 == 0 )
    {
        foreach ( var_5 in level.teamdata[var_0]["players"] )
            var_5 cancelgivequest( "br_mastery_untouchable" );
    }
}

wasinlaststand()
{
    if ( !isdefined( self ) || !isdefined( self.team ) )
        return;

    var_0 = self.deaths;
    var_1 = self.team;

    foreach ( var_3 in level.teamdata[var_1]["players"] )
    {
        if ( scripts\mp\utility\player::isreallyalive( var_3 ) )
        {
            if ( !isdefined( var_3.validtousesticker ) )
                var_3.validtousesticker = 0;

            var_3.validtousesticker = var_3.validtousesticker + var_0;
            break;
        }
    }
}

wassquadspawned( var_0 )
{
    var_1 = 7;

    if ( !isdefined( var_0 ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::spotlight_goal_node() )
        return;

    var_2 = 0;

    foreach ( var_4 in level.teamdata[var_0]["players"] )
    {
        var_2 = var_2 + var_4.deaths;

        if ( isdefined( var_4.validtousesticker ) )
            var_2 = var_2 + var_4.validtousesticker;
    }

    if ( var_2 >= var_1 )
    {
        foreach ( var_4 in level.teamdata[var_0]["players"] )
            var_4 cancelgivequest( "br_mastery_neverSayDie" );
    }
}

nearby_ai_combat_via_grenade( var_0, var_1 )
{
    if ( !isdefined( self.team ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::get_int_or_0( self.destroy_intro_tank ) < 5 )
        return;

    var_2 = isdefined( level.gulag ) && !istrue( level.gulag.shutdown );

    foreach ( var_4 in level.teamdata[self.team]["players"] )
    {
        if ( !scripts\mp\utility\player::isreallyalive( var_4 ) )
            return;

        if ( var_2 && var_4 scripts\mp\gametypes\br_public.gsc::spawnboardroom_specialist() )
            return;
    }

    cancelgivequest( var_0 );
}

_id_120B0( var_0, var_1 )
{
    var_2 = var_1._id_11C96;
    var_3 = var_1._id_13244;

    if ( !isdefined( var_2 ) || !isdefined( var_2.bot_destination_spots ) || !isdefined( var_2.streakinfo ) || !isdefined( var_3 ) )
        return;

    var_4 = var_2.streakinfo;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_airstrike_killtracker" );
    self endon( "pointBlank_airstrike_killtracker" );
    scripts\engine\utility::_id_13AED( 10.0, "airstrike_finished_" + var_2.bot_destination_spots );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_4.kills >= 3 )
        cancelgivequest( "br_mastery_pointBlankStreakKill" );
}

_id_120B1( var_0, var_1 )
{
    var_2 = var_1.streakinfo;
    var_3 = var_1._id_13244;

    if ( !isdefined( var_2 ) )
        return;

    if ( !isdefined( var_3 ) )
        return;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_tomahawk_killtracker" );
    self endon( "pointBlank_tomahawk_killtracker" );
    scripts\engine\utility::_id_13AEE( 20, "cluster_strike_finished" );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_2.kills >= 3 )
        cancelgivequest( "br_mastery_pointBlankStreakKill" );

    return;
}

enemy_is_visible( var_0, var_1 )
{
    var_2 = var_1.meansofdeath;
    var_3 = var_1.inflictor;

    if ( !isdefined( var_2 ) || var_2 != "MOD_EXPLOSIVE" )
        return;

    if ( !squadlink_wid( var_3 ) )
        return;

    self endon( "disconnect" );
    level endon( "game_ended" );
    self notify( "updateC4VehicleMultKill" );
    self endon( "updateC4VehicleMultKill" );

    if ( !isdefined( self._id_12335 ) )
        self._id_12335 = 0;

    self._id_12335++;
    wait 4.0;

    if ( isdefined( self._id_12335 ) && self._id_12335 >= 3 )
    {
        cancelgivequest( var_0 );
        var_4 = enemy_groups( var_3 );
        var_5 = isdefined( var_4 ) && isdefined( var_4.team ) && isdefined( self.team ) && var_4.team == self.team;

        if ( var_5 )
            var_4 cancelgivequest( var_0 );
    }

    self._id_12335 = undefined;
}

squadlink_wid( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.vehiclename ) && !var_0 scripts\common\vehicle_code::vehicle_is_stopped() )
    {
        var_1 = var_0 getlinkedchildren();

        foreach ( var_3 in var_1 )
        {
            if ( isdefined( var_3.weapon_name ) && var_3.weapon_name == "c4_mp_p" )
                return 1;
        }
    }
    else if ( isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_5 = var_0 getlinkedparent();

        if ( isdefined( var_5 ) && isdefined( var_5.vehiclename ) && !var_5 scripts\common\vehicle_code::vehicle_is_stopped() )
            return 1;
    }

    return 0;
}

enemy_groups( var_0 )
{
    if ( isdefined( var_0 ) && isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_1 = var_0 getlinkedparent();

        if ( isdefined( var_1 ) )
            return var_1.owner;
    }

    return undefined;
}

_id_1263E( var_0, var_1 )
{
    var_2 = var_1.player;

    if ( !isdefined( var_2 ) )
        return;

    if ( isdefined( var_2.modifiers["execution"] ) && var_2.modifiers["execution"] == 1 )
        var_2 cancelgivequest( "br_mastery_roundKillExecute" );
}

propclones( var_0, var_1 )
{
    self endon( "disconnect" );
    level endon( "game_ended" );

    if ( !isdefined( self ) )
        return;

    var_2 = var_1.maxplunder;
    var_3 = var_1.tutorialprint_number;

    if ( istrue( var_2 ) )
    {
        if ( istrue( self.relicsquadlink ) && self._id_12336 != 0 )
            return;

        self.relicsquadlink = 1;
    }

    if ( !istrue( self.relicsquadlink ) )
        return;

    self notify( "ghostRideWhip" );
    self endon( "ghostRideWhip" );

    if ( !isdefined( self._id_12336 ) )
        self._id_12336 = 0;

    if ( istrue( var_3 ) )
        self._id_12336++;

    wait 4.0;

    if ( !isdefined( self ) )
        return;

    if ( isdefined( self._id_12336 ) && self._id_12336 >= 3 )
        cancelgivequest( var_0 );

    self._id_12336 = undefined;
    self.relicsquadlink = undefined;
}

_id_1349A( var_0, var_1 )
{
    var_2 = -1;

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) == 0 )
        return;

    var_3 = var_1.inflictor;

    if ( !isdefined( var_3 ) )
        return;

    if ( !isplayer( var_3 ) && isdefined( var_3.owner ) && isplayer( var_3.owner ) )
        var_3 = var_3.owner;
    else
        return;

    var_4 = var_1.victim;

    if ( !isdefined( var_4 ) || !isplayer( var_4 ) )
        return;

    var_5 = 0;
    var_6 = var_3 scripts\mp\gametypes\br_callouts.gsc::playerstartpowers( var_3 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    var_6 = var_4 scripts\mp\gametypes\br_callouts.gsc::playerstartpowers( var_4 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    if ( var_5 == 0 )
        return;

    foreach ( var_8 in level.teamdata[var_3.team]["players"] )
    {
        if ( !isdefined( var_8._id_13E3C ) )
            var_8._id_13E3C = 0;

        var_8._id_13E3C = var_8._id_13E3C | var_5;
    }
}

_id_13499( var_0 )
{
    var_1 = 12;

    if ( !isdefined( var_0._id_13E3C ) || var_0._id_13E3C == 0 )
        return;

    var_2 = 0;

    for ( var_3 = 0; var_3 < level.calloutglobals.waittill_usebutton_released_or_time_or_bomb_planted.size; var_3++ )
    {
        var_4 = var_0._id_13E3C & 1 << var_3;

        if ( var_4 )
            var_2++;
    }

    if ( var_2 >= var_1 )
        var_0 cancelgivequest( "br_mastery_travelogue" );

    return;
}
