// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    if ( scripts\mp\utility\game::getgametype() != "br" )
        return;

    registerchallenge( "br_mastery_fiveContracts", ::fivecontracts );
    registerchallenge( "br_mastery_pointBlank_airstrike", ::pointblank_airstrike );
    registerchallenge( "br_mastery_pointBlank_tomahawk", ::pointblank_tomahawk );
    registerchallenge( "br_mastery_c4VehicleMultKill", ::c4vehiclemultkill );
    registerchallenge( "br_mastery_ghostRideWhip", ::ghostridewhip );
    registerchallenge( "br_mastery_roundKillExecute", ::roundkillexecute );
    registerchallenge( "br_mastery_travelogue", ::traveloguekilltracking );
}

registerchallenge( var_0, var_1 )
{
    if ( !isdefined( level.br_challenges ) )
        level.br_challenges = [];

    level.br_challenges[var_0] = var_1;
}

challengeevaluator( var_0, var_1 )
{
    if ( !isdefined( level.br_challenges ) )
        return;

    if ( istrue( level.challengesdisabled ) )
        return;

    var_2 = level.br_challenges[var_0];

    if ( isdefined( var_2 ) )
        self thread [[ var_2 ]]( var_0, var_1 );
}

awardmasterychallenge( var_0 )
{
    if ( getdvarint( "scr_br_challenge_debug", 0 ) )
        iprintlnbold( "Mastery Challenge: " + var_0 + " completed!" );

    scripts\cp_mp\challenges::oncollectitem( var_0 );
}

masterychallengesmatchwincheck( var_0 )
{
    if ( !isdefined( level.teamdata[var_0] ) )
        return;

    foreach ( var_2 in level.teamdata[var_0]["players"] )
        travelogueevaluateandaward( var_2 );

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) )
    {
        untouchabletestandaward( var_0 );
        neversaydietestandaward( var_0 );
    }
}

untouchabletestandaward( var_0 )
{
    if ( !isdefined( var_0 ) )
        return;

    var_1 = 0;
    var_2 = 0;
    var_3 = max( 1, level.maxteamsize );

    foreach ( var_5 in level.teamdata[var_0]["players"] )
    {
        var_1 = var_1 + var_5.deaths;

        if ( isdefined( var_5.migrateddeaths ) )
            var_1 = var_1 + var_5.migrateddeaths;

        if ( scripts\mp\utility\player::isreallyalive( var_5 ) )
            var_2++;
    }

    if ( var_1 == 0 )
    {
        foreach ( var_5 in level.teamdata[var_0]["players"] )
            var_5 awardmasterychallenge( "br_mastery_untouchable" );
    }
}

neversaydiemigratedeaths()
{
    if ( !isdefined( self ) || !isdefined( self.team ) )
        return;

    var_0 = self.deaths;
    var_1 = self.team;

    foreach ( var_3 in level.teamdata[var_1]["players"] )
    {
        if ( scripts\mp\utility\player::isreallyalive( var_3 ) )
        {
            if ( !isdefined( var_3.migrateddeaths ) )
                var_3.migrateddeaths = 0;

            var_3.migrateddeaths = var_3.migrateddeaths + var_0;
            break;
        }
    }
}

neversaydietestandaward( var_0 )
{
    var_1 = 7;

    if ( !isdefined( var_0 ) )
        return;

    var_2 = 0;

    foreach ( var_4 in level.teamdata[var_0]["players"] )
    {
        var_2 = var_2 + var_4.deaths;

        if ( isdefined( var_4.migrateddeaths ) )
            var_2 = var_2 + var_4.migrateddeaths;
    }

    if ( var_2 >= var_1 )
    {
        foreach ( var_4 in level.teamdata[var_0]["players"] )
            var_4 awardmasterychallenge( "br_mastery_neverSayDie" );
    }
}

fivecontracts( var_0, var_1 )
{
    if ( !isdefined( self.team ) )
        return;

    if ( scripts\mp\gametypes\br.gsc::get_int_or_0( self.brmissionscompleted ) < 5 )
        return;

    var_2 = isdefined( level.gulag ) && !istrue( level.gulag.shutdown );

    foreach ( var_4 in level.teamdata[self.team]["players"] )
    {
        if ( !scripts\mp\utility\player::isreallyalive( var_4 ) )
            return;

        if ( var_2 && var_4 scripts\mp\gametypes\br_public.gsc::isplayerinorgoingtogulag() )
            return;
    }

    awardmasterychallenge( var_0 );
}

pointblank_airstrike( var_0, var_1 )
{
    var_2 = var_1.plane;
    var_3 = var_1.targetsite;

    if ( !isdefined( var_2 ) || !isdefined( var_2.airstrikeid ) || !isdefined( var_2.streakinfo ) || !isdefined( var_3 ) )
        return;

    var_4 = var_2.streakinfo;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_airstrike_killtracker" );
    self endon( "pointBlank_airstrike_killtracker" );
    scripts\engine\utility::waittill_any_timeout_1( 10.0, "airstrike_finished_" + var_2.airstrikeid );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_4.kills >= 3 )
        awardmasterychallenge( "br_mastery_pointBlankStreakKill" );
}

pointblank_tomahawk( var_0, var_1 )
{
    var_2 = var_1.streakinfo;
    var_3 = var_1.targetsite;

    if ( !isdefined( var_2 ) )
        return;

    if ( !isdefined( var_3 ) )
        return;

    if ( distancesquared( self.origin, var_3 ) > squared( 432 ) )
        return;

    self endon( "death_or_disconnect" );
    self notify( "pointBlank_tomahawk_killtracker" );
    self endon( "pointBlank_tomahawk_killtracker" );
    scripts\engine\utility::waittill_any_timeout_2( 20, "cluster_strike_finished" );

    if ( !scripts\mp\utility\player::isreallyalive( self ) )
        return;

    if ( var_2.kills >= 3 )
        awardmasterychallenge( "br_mastery_pointBlankStreakKill" );

    return;
}

c4vehiclemultkill( var_0, var_1 )
{
    var_2 = var_1.meansofdeath;
    var_3 = var_1.inflictor;

    if ( !isdefined( var_2 ) || var_2 != "MOD_EXPLOSIVE" )
        return;

    if ( !isvalidinflictorc4vehicle( var_3 ) )
        return;

    self endon( "disconnect" );
    level endon( "game_ended" );
    self notify( "updateC4VehicleMultKill" );
    self endon( "updateC4VehicleMultKill" );

    if ( !isdefined( self.recentc4vehiclekillcount ) )
        self.recentc4vehiclekillcount = 0;

    self.recentc4vehiclekillcount++;
    wait 4.0;

    if ( isdefined( self.recentc4vehiclekillcount ) && self.recentc4vehiclekillcount >= 3 )
    {
        awardmasterychallenge( var_0 );
        var_4 = c4vehiclecooperator( var_3 );
        var_5 = isdefined( var_4 ) && isdefined( var_4.team ) && isdefined( self.team ) && var_4.team == self.team;

        if ( var_5 )
            var_4 awardmasterychallenge( var_0 );
    }

    self.recentc4vehiclekillcount = undefined;
}

isvalidinflictorc4vehicle( var_0 )
{
    if ( !isdefined( var_0 ) )
        return 0;

    if ( isdefined( var_0.vehiclename ) && !var_0 scripts\common\vehicle_code::vehicle_is_stopped() )
    {
        var_1 = var_0 getlinkedchildren();

        foreach ( var_3 in var_1 )
        {
            if ( isdefined( var_3.weapon_name ) && var_3.weapon_name == "c4_mp_p" )
                return 1;
        }
    }
    else if ( isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_5 = var_0 getlinkedparent();

        if ( isdefined( var_5 ) && isdefined( var_5.vehiclename ) && !var_5 scripts\common\vehicle_code::vehicle_is_stopped() )
            return 1;
    }

    return 0;
}

c4vehiclecooperator( var_0 )
{
    if ( isdefined( var_0 ) && isdefined( var_0.weapon_name ) && var_0.weapon_name == "c4_mp_p" )
    {
        var_1 = var_0 getlinkedparent();

        if ( isdefined( var_1 ) )
            return var_1.owner;
    }

    return undefined;
}

roundkillexecute( var_0, var_1 )
{
    var_2 = var_1.player;

    if ( !isdefined( var_2 ) )
        return;

    if ( isdefined( var_2.modifiers["execution"] ) && var_2.modifiers["execution"] == 1 )
        var_2 awardmasterychallenge( "br_mastery_roundKillExecute" );
}

ghostridewhip( var_0, var_1 )
{
    self endon( "disconnect" );
    level endon( "game_ended" );

    if ( !isdefined( self ) )
        return;

    var_2 = var_1.exitdriver;
    var_3 = var_1.maderecentkill;

    if ( istrue( var_2 ) )
    {
        if ( istrue( self.hasexiteddriver ) && self.recentghostridekillcount != 0 )
            return;

        self.hasexiteddriver = 1;
    }

    if ( !istrue( self.hasexiteddriver ) )
        return;

    self notify( "ghostRideWhip" );
    self endon( "ghostRideWhip" );

    if ( !isdefined( self.recentghostridekillcount ) )
        self.recentghostridekillcount = 0;

    if ( istrue( var_3 ) )
        self.recentghostridekillcount++;

    wait 4.0;

    if ( !isdefined( self ) )
        return;

    if ( isdefined( self.recentghostridekillcount ) && self.recentghostridekillcount >= 3 )
        awardmasterychallenge( var_0 );

    self.recentghostridekillcount = undefined;
    self.hasexiteddriver = undefined;
}

traveloguekilltracking( var_0, var_1 )
{
    var_2 = -1;

    if ( getdvarint( "scr_br_core_trios_or_quads", 0 ) == 0 )
        return;

    var_3 = var_1.inflictor;

    if ( !isdefined( var_3 ) )
        return;

    if ( !isplayer( var_3 ) && isdefined( var_3.owner ) && isplayer( var_3.owner ) )
        var_3 = var_3.owner;
    else
        return;

    var_4 = var_1.victim;

    if ( !isdefined( var_4 ) || !isplayer( var_4 ) )
        return;

    var_5 = 0;
    var_6 = var_3 scripts\mp\gametypes\br_callouts.gsc::getlocationindexforplayer( var_3 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    var_6 = var_4 scripts\mp\gametypes\br_callouts.gsc::getlocationindexforplayer( var_4 );

    if ( var_6 != var_2 )
        var_5 = var_5 | 1 << var_6;

    if ( var_5 == 0 )
        return;

    foreach ( var_8 in level.teamdata[var_3.team]["players"] )
    {
        if ( !isdefined( var_8.zonekilltracker ) )
            var_8.zonekilltracker = 0;

        var_8.zonekilltracker = var_8.zonekilltracker | var_5;
    }
}

travelogueevaluateandaward( var_0 )
{
    var_1 = 12;

    if ( !isdefined( var_0.zonekilltracker ) || var_0.zonekilltracker == 0 )
        return;

    var_2 = 0;

    for ( var_3 = 0; var_3 < level.calloutglobals.namelocations.size; var_3++ )
    {
        var_4 = var_0.zonekilltracker & 1 << var_3;

        if ( var_4 )
            var_2++;
    }

    if ( var_2 >= var_1 )
        var_0 awardmasterychallenge( "br_mastery_travelogue" );

    return;
}
