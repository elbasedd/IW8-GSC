// IW8 GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

main()
{
    setup_callbacks();
    setup_bot_koth();
}

setup_callbacks()
{
    level.bot_funcs["gametype_think"] = ::bot_koth_think;
    level.bot_funcs["should_start_cautious_approach"] = ::should_start_cautious_approach_koth;
}

setup_bot_koth()
{
    scripts\mp\bots\bots_util::bot_waittill_bots_enabled();
    var_0 = 0;

    foreach ( var_2 in level.objectives )
    {
        var_2 thread scripts\mp\bots\bots_gametype_common.gsc::monitor_zone_control();
        var_3 = 0;

        if ( istrue( var_2.trigger.trigger_off ) )
        {
            var_2.trigger scripts\engine\utility::trigger_on();
            var_3 = 1;
        }

        var_2.nodes = scripts\mp\bots\bots_gametype_common.gsc::bot_get_valid_nodes_in_trigger( var_2.trigger );

        if ( var_3 )
            var_2.trigger scripts\engine\utility::trigger_off();
    }

    level.bot_set_zone_nodes = 1;

    if ( !var_0 )
    {
        level.bot_hp_allow_predictive_capping = 1;
        var_5 = level.zone;

        if ( !isdefined( var_5 ) )
            var_5 = scripts\engine\utility::random( level.objectives );

        scripts\mp\bots\bots_gametype_common.gsc::bot_cache_entrances_to_zones( [ var_5 ] );
        level.bot_gametype_zones_precached[var_5.trigger getentitynumber()] = 1;
        level.bot_gametype_precaching_done = 1;
        thread bot_cache_entrances_to_other_zones( var_5 );
    }
}

bot_cache_entrances_to_other_zones( var_0 )
{
    for ( var_1 = scripts\engine\utility::array_remove( level.objectives, var_0 ); var_1.size > 0; var_1 = scripts\engine\utility::array_remove( var_1, var_2 ) )
    {
        var_2 = undefined;
        var_3 = level.zone;

        if ( isdefined( var_3 ) && scripts\engine\utility::array_contains( var_1, var_3 ) )
            var_2 = var_3;
        else
            var_2 = scripts\engine\utility::random( var_1 );

        scripts\mp\bots\bots_gametype_common.gsc::bot_cache_entrances_to_zones( [ var_2 ] );
        level.bot_gametype_zones_precached[var_2.trigger getentitynumber()] = 1;
    }
}

bot_koth_think()
{
    self notify( "bot_koth_think" );
    self endon( "bot_koth_think" );
    self endon( "death_or_disconnect" );
    level endon( "game_ended" );

    while ( !isdefined( level.bot_gametype_precaching_done ) )
        wait 0.05;

    self botsetflag( "separation", 0 );
    self botsetflag( "grenade_objectives", 1 );
    var_0 = undefined;
    var_1 = level.zone;

    for (;;)
    {
        wait 0.05;

        if ( self.health <= 0 )
            continue;

        if ( !isdefined( level.zone ) || !isdefined( level.bot_gametype_zones_precached[level.zone.trigger getentitynumber()] ) )
        {
            if ( scripts\mp\bots\bots_util::bot_is_defending() )
                scripts\mp\bots\bots_strategy::bot_defend_stop();

            self.current_zone = undefined;
            self [[ self.personality_update_function ]]();
            continue;
        }

        if ( var_1 != level.zone )
        {
            var_0 = undefined;
            var_1 = level.zone;
        }

        if ( !istrue( level.zone.trigger.trigger_off ) && isdefined( level.zoneendtime ) && !isdefined( var_0 ) && !level.zonerandomlocationorder && level.bot_hp_allow_predictive_capping )
        {
            var_2 = level.zoneendtime - gettime();

            if ( var_2 > 0 && var_2 < 10000 )
            {
                var_3 = level.zone scripts\mp\gameobjects::getownerteam() == self.team;

                if ( !var_3 )
                {
                    var_4 = level.zone.zone_bounds.radius * 6;

                    if ( var_2 < 5000 )
                        var_4 = level.zone.zone_bounds.radius * 3;

                    var_5 = distance( level.zone.zone_bounds.center, self.origin );

                    if ( var_5 > var_4 )
                        var_0 = bot_should_cap_next_zone();
                }
                else
                {
                    var_6 = scripts\mp\bots\bots_util::bot_get_max_players_on_team( self.team );
                    var_7 = ceil( var_6 / 2 );

                    if ( var_2 < 5000 )
                        var_7 = ceil( var_6 / 3 );

                    var_8 = bot_get_num_teammates_capturing_zone( level.zone );

                    if ( var_8 + 1 > var_7 )
                        var_0 = bot_should_cap_next_zone();
                }
            }
        }

        var_9 = level.zone;

        if ( istrue( var_0 ) )
        {
            var_10 = ( level.prevzoneindex + 1 ) % level.objectives.size;

            if ( var_10 == 0 )
                var_10 = 1;

            var_9 = level.objectives[scripts\engine\utility::string( var_10 )];
        }

        if ( !bot_is_capturing_zone( var_9 ) )
            bot_capture_koth_zone( var_9 );
    }
}

bot_should_cap_next_zone()
{
    if ( level.zonerandomlocationorder )
        return 0;
    else
    {
        var_0 = self botgetdifficultysetting( "strategyLevel" );
        var_1 = 0;

        if ( var_0 == 1 )
            var_1 = 0.1;
        else if ( var_0 == 2 )
            var_1 = 0.5;
        else if ( var_0 == 3 )
            var_1 = 0.8;

        return randomfloat( 1.0 ) < var_1;
    }
}

bot_get_num_teammates_capturing_zone( var_0 )
{
    return bot_get_teammates_capturing_zone( var_0 ).size;
}

bot_get_teammates_capturing_zone( var_0 )
{
    var_1 = [];

    foreach ( var_3 in level.participants )
    {
        if ( var_3 != self && scripts\mp\utility\entity::isteamparticipant( var_3 ) && isalliedsentient( self, var_3 ) )
        {
            if ( var_3 istouching( level.zone.trigger ) )
            {
                if ( !isai( var_3 ) || var_3 bot_is_capturing_zone( var_0 ) )
                    var_1[var_1.size] = var_3;
            }
        }
    }

    return var_1;
}

bot_is_capturing_zone( var_0 )
{
    if ( !scripts\mp\bots\bots_util::bot_is_capturing() )
        return 0;

    return self.current_zone == var_0;
}

bot_capture_koth_zone( var_0 )
{
    self.current_zone = var_0;
    var_1["entrance_points_index"] = var_0.entrance_indices;
    var_1["override_origin_node"] = var_0.center_node;
    scripts\mp\bots\bots_strategy::bot_capture_zone( var_0.trigger.origin, var_0.nodes, var_0.trigger, var_1 );
}

should_start_cautious_approach_koth( var_0 )
{
    if ( var_0 )
    {
        var_1 = level.zone scripts\mp\gameobjects::getownerteam();

        if ( var_1 == "neutral" || var_1 == self.team )
            return 0;
    }

    return scripts\mp\bots\bots_strategy::should_start_cautious_approach_default( var_0 );
}
